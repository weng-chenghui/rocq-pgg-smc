# Proposal: PSL(2,11) on twelve colour-only cards, privacy 5 from design strength

Date: 2026-09-14

One-line summary: twelve cards, six hearts and six clubs, shuffled by the group
generated by two label-free operations, "reverse each quarter" and "Monge-shuffle
each half". That group is PSL(2,11), order 660, only 2-transitive, yet every
coalition of at most five cards learns nothing about the secret "which of the
two mirror-image Steiner systems S(5,6,12) holds the heart positions", because
both secret classes are 5-designs. For the colour-only observer it reaches the
same threshold five as M12 with a group 144 times smaller (M12, being
5-transitive, also protects the raw code view at five; PSL(2,11) does not), an enumerated word certificate of the pgl27_mixing.v shape,
and generators that are the M12 shuffles applied to sub-piles.

Companion notes: [[2026-09-13-225500-m12-instance-proposal]] (M12 and the
word-certificate constraint, sections 1-7),
[[20260701-175221-report-shuffle-matters-group-ladder]] (the ladder),
[[20260702-114631-pgl27-orbit-class-ROCQ-formalization-spec]] (the landed
PGL(2,7) shape).

## 1. The search constraints and what they ruled out

Constraints accumulated over the two search rounds:

- C1 a two-class (or more) secret carried by an orbit invariant of the shuffle
  group, with a privacy threshold k >= 3;
- C2 generators that are physical card operations, not the PGL translation /
  scaling / inversion triple;
- C3 an in-kernel enumerated word certificate at some computable length L
  (group order in the thousands at most, L in the hundreds);
- C4 different enough from PGL(2,7) to carry a paper, not a footnote.

Ruled out, with the measured reason:

| candidate | fails | measured reason |
|---|---|---|
| M12 (Monge + reversal, 12 cards) | C3 | 95040 states; Monge alphabets mix at 0.04-0.06 bit per letter, L about 700-1000 for 2^-40 |
| M11 on 11 cards | C2 | no involution of M11 is a block reversal, no contiguous sub-pile cut, faro or Monge lies in M11; the second generator is a table |
| PGL(2,9), PGL(2,11), PGL(2,13), M10 | C2 | the same generator triple (L = 225 / 225 / 245 would have been fine) |
| AGL(3,2) on 8 cards (faro + one swap) | C4 | same deck, same threshold 3 as PGL(2,7); 14 / 56 against 28 / 42 |
| PGL(2,8), PGammaL(2,8), S_n, A_n | C1 | one orbit on (t+1)-subsets, nothing to hide |
| faro groups <I,O> on 8, 10, 12 cards | C1 | orders 24, 1920, 7680, imprimitive; every subset orbit has design strength 1, so k = 1 |
| Monge group <u,z> on 8 cards | C1 | order 32, strength 1 |
| cyclic C_7, C_11 (one cut) | C1 | difference-set orbits give k = 2 and no more |
| AGL(2,3) on 9 (the SET deck), A5 on 6, AGammaL(1,8) | C1 | k <= 3 with a table-driven or PGL-like alphabet; AGammaL(1,8) reaches 3 only with the Frobenius table |

## 2. The criterion that replaces transitivity

Under the all-decks dealer, and under the fixed-representative dealer when
cards of one colour are indistinguishable, a coalition C sees the secret only
through the law of H cap C, H the heart set. If H is drawn from an orbit that
is a t-design, the number of blocks meeting C in a prescribed A subset of C is
fixed by the design parameters for |C| <= t, and lambda_t / b = C(h,t) / C(v,t)
does not depend on the orbit. So two secret classes that are both t-designs
of the same block size give identical coalition laws up to size t, whatever
the tuple-transitivity of the group. Privacy threshold of a secret pair is AT LEAST the
min design strength of the two orbits (design theory gives the lower bound;
the leak at strength + 1 is a separate fact, here the vm_compute'd leak6).
The normalised laws are b-independent, but the in-kernel COUNT certificate
(ledger L6) compares raw block counts and so additionally needs the two
orbits to have the same size; here both are 132. The M12 pair (792, 132)
satisfies the criterion but would fail a raw-count certificate.

Census with this criterion (scratch `notes/probes/design_census.py`), best
secret pair per group:

| group | order | tuple t | subset orbits (size, strength) | k |
|---|---|---|---|---|
| PGL(2,7) on 8, h = 4 | 336 | 3 | (42,3) (28,3) | 3 |
| PSL(2,7) on 8, h = 4 | 168 | 2 | (42,3) (14,3) (14,3) | 3 |
| AGL(3,2) on 8, h = 4 | 1344 | 3 | (56,3) (14,3) | 3 |
| M11 on 11, h = 5 | 7920 | 4 | (396,4) (66,4) | 4 |
| PSL(2,11) on 12, h = 4 | 660 | 2 | (330,3) (165,3) | 3 |
| **PSL(2,11) on 12, h = 6** | **660** | **2** | **(132,5) (132,5)** (330,3) (110,3) x3 | **5** |
| M12 on 12, h = 6 | 95040 | 5 | (792,5) (132,5) | 5 |

The two 132-orbits of PSL(2,11) are the Carmichael Steiner system S(5,6,12),
the orbit of {inf, 1, 3, 4, 5, 9} (infinity plus the nonzero squares of
F_11), and its mirror, the orbit of infinity plus the non-squares. The
non-square scaling z -> 2z, which lies in PGL(2,11) and not in PSL(2,11),
exchanges them. A PGL(2,11) shuffle would therefore destroy the secret; a
PSL(2,11) shuffle preserves it. This is the k = 5 form of the A5-on-6
chirality of the ladder note.

## 3. The two generators as card operations

Positions 0..11 from the top of a face-down pile of twelve cards, six hearts
and six clubs, cards of one colour indistinguishable.

**r4, reverse each quarter.** Split the pile into three piles of four, turn
each over as a block (or deal each into a new pile), restack in place.
Permutation (0 3)(1 2)(4 7)(5 6)(8 11)(9 10), an involution.

**m6, Monge-shuffle each half.** Split the pile into two piles of six,
perform the Monge up shuffle on each (top card to the hand, then alternately
on top and under), restack in place. Permutation (0 3 1 2 4 5)(6 9 7 8 10 11),
order 6. Its inverse is the milk shuffle of each half (Diaconis, Graham and
Kantor 1983, p. 193: the milk shuffle is the inverse of Monge's shuffle), so
the symmetrized alphabet {reverse quarters, Monge halves, milk halves} is
three named physical operations.

Both r4 and m6 lie in M12 = <Monge-12, reverse-12> of the M12 proposal, and
<r4, m6> is a PSL(2,11) inside it: the instance is the M12 shuffles applied to
sub-piles. Of the two Steiner systems, the orbit with representative
{0, 1, 3, 7, 10, 11} is the M12 hexad system and the orbit with representative
{2, 3, 5, 7, 8, 9} is its mirror (verified: the M12-orbit of the first has 132
blocks and equals it, the M12-orbit of the second has 792). The M12 shuffle
moves the mirror decks out of the valid deck set, which is why the shuffle
group must stop at PSL(2,11).

## 4. Facts verified by exhaustive computation (2026-09-14)

Scratch `notes/probes/psl211_physical.py`; every claim is a full enumeration.

- |<r4, m6>| = 660, 2-transitive on the twelve positions, not 3-transitive.
- An explicit relabelling pi with pi <r4, m6> pi^-1 = PSL(2,11) acting on
  P^1(F_11) by Moebius maps: pi = (0,5,11,9,6,2,8,7,4,1,10,3) as a list of
  images of positions 0..11 (11 = infinity). Under pi the 132-orbit A is the
  Carmichael system, the orbit of {inf,1,3,4,5,9}.
- Orbits on 6-subsets: 110, 110, 110, 132, 132, 330 with design strengths
  3, 3, 3, 5, 5, 3.
- Colour view law of a coalition, fixed representative and uniform shuffle,
  equal across the two 132-classes for every coalition of size 1..5, unequal
  at size 6 (all coalitions checked).
- Ten reveals never determine the class: for every pair of hidden positions
  there is a block of each system agreeing on the other ten. Eleven reveals
  determine it, by colour pigeonhole. Ramp: private to 5, leak at 6,
  ambiguous through 10, determined at 11, decoder reads 12.
- Word walk to uniform on the 660 group elements: {r4, m6, m6^-1} reaches
  TV 2^-40 at L = 570 (2^-13.9 at L = 200). The pgl27_mixing.v certificate
  shape encodes TV <= 2^-41 (the 2^-40 view bound after the factor two), and
  in the kernel that form first holds at L = 584 (audit-soundness/
  audit_mixing584.v, Qed, 18.8 s; audit_mixing570.v fails). The instance
  therefore uses L = 584. The two-letter alphabet is not usable (section 5).
  Cayley diameter with the three letters: 12.
- In-kernel cost of the enumerated certificate, MEASURED by probe_mixing.v
  (superseding the five-to-ten-minute estimate of the first draft): closure
  of the 660 table rows 6.5 s, walk tables 3.9 s, walk at L = 100 1.15 s,
  L = 200 2.90 s, L = 300 5.18 s, L = 400 8.13 s, growth between L^1.4 and
  L^2, so L = 570 extrapolates to about 30 s end to end. The expensive
  certificate is elsewhere: the size 1..5 pattern-count check (ledger L6)
  takes 38.7 s and its mutation 29.5 s.
- No other physical operation from the pool of 520 (block reversals, cuts,
  Monge, faro, block swaps, parallel block operations) lies in <r4, m6>, so no
  third physical letter is available to speed the walk up. Script:
  `notes/probes/physical_pool_search.py` (kept after the audit noted the
  first draft cited no script for this claim).

## 5. What the Rocq instance would need beyond the pgl27 shape

- **Deck model.** The scheme, the plug and the run deal distinct codes
  exactly as pgl27 does (`ThresholdScheme bool 'I_12`, `rp_content := id`).
  The colour-only reading lives in the models layer as an OBSERVER, the
  shape of `five_card_exec_colour_view` in instances/kim2025/
  five_card_models.v:277: a function of the executed endpoints through
  `is_heart`. It must not be the plug's `rp_content`: `ar_protocol_correct`
  (reconstruct/algebraic_rigidity.v:409) applies `rp_content` to the
  endpoints and then demands `ts_valid`, whose `deck_ok = uniq` no
  twelve-entry colour tuple satisfies, so a colour-collapsing content would
  make the recovery path vacuous (naming audit finding E3). The first draft's
  claim that transitivity_privacy.v already factors the code view through
  H cap C is false (audit A10): the H cap C factoring is new work, ledger
  rows L20-L23.
- **Privacy bridge.** Not ttrans_private. Either the design-theoretic lemma
  (intersection numbers of a t-design are determined by the parameters) or,
  cheaper, a literal check: for the two 132-row tables and every coalition of
  size <= 5 (1585 coalitions) and every A subset of C, the block counts agree
  (about 7 x 10^6 vm_compute steps). The view law is count / 132, so equal
  counts give equal laws.
- **Invariant.** Two 132-row tables; invariance under r4 and m6 by 264
  membership checks each, induction on words for the group.
- **Word model.** The pgl27_mixing.v shape at 660 states, three letters,
  L = 570. The two-letter alphabet {r4, m6} is NOT available: the landed
  walk lemmas (`symm_ds_TV_bound_inv_closed`, instances/pgl27/
  pgl27_spectral.v:575) need an involutive letter pairing (audit A6).
- **Recovery.** Distinct-code pigeonhole at 11 (the colour pigeonhole also
  holds); the 10-reveal ambiguity witness is the 1-swap pair of blocks,
  transported by 2-transitivity as in pgl27_recovery.v:108. So 2-transitivity
  IS consumed, by this file only; ledger row L19 certifies it in-kernel
  (132 ordered pairs, BFS over the two generators).

## 6. Sources (research-kb ids)

- `wikipedia-steiner-system-s5612`: Carmichael 1937 construction of
  S(5,6,12) as the PSL(2,11)-orbit of {inf,1,3,4,5,9}; 132 blocks; Aut = M12.
- `DiaconisGrahamKantor1983-perfect-shuffles`: Monge up shuffle, milk
  shuffle as its inverse (p. 193), <u, z> = M12 on 12 cards (p. 194).
- `wikipedia-mathieu-group-m11`, `atlas-v3-m11`: M11 data used to rule it out.
- `Hanaki2025-PSL2q-3designs`: orbits of t-homogeneous groups are t-designs
  (the general fact behind the design-strength criterion; PSL(2,11) is only
  2-homogeneous on 12 points, so the strength 5 of its two Steiner orbits is
  a property of those orbits, not of the group).
- `araujo-cameron-2015-khomogeneous`: CFSG list of 3-homogeneous groups.

## 7. Claim ledger (probe-first pass, 2026-09-14)

Carrier, pinned as one expression: the monodromy `psl211_M :=
@Gen_PGGTypes 1 10 psl211_gens` with `psl211_gens : 2.-tuple {perm 'I_12}`,
so `pgg_gT psl211_M = {perm 'I_12}`, `pgg_N' psl211_M = 11`, decks
`12.-tuple 'I_12`, coalitions `{set 'I_12}`, laws `R.-fdist _` for an
arbitrary `R : realType`, the joint sample space `bool * {perm 'I_12}` under
`secretP `x (`U card_G_gt0)` exactly as in transitivity_privacy.v.

Probe files: `notes/probes/2026-09-14-psl211/probe_*.v` (kept, never
imported). Status column filled by the probe run and the audits.

| # | claim | kind | passes when | status |
|---|---|---|---|---|
| L1 | `@Gen_PGGTypes 1 10 psl211_gens` builds the monodromy with `pgg_N' = 11` and generator tuple of length 2 | library object at carrier | Definition elaborates; `pgg_N' psl211_M = 11` by `[]`; `@Gen_PGGTypes 1 9` fails | GO, probe_group: psl211_N', psl211_gens_in_G; both mutations fail on the position and generator counts |
| L2 | r4, m6 tables are injective self-maps of 'I_12 (perm construction as pgl27_group) | precedent shape | `perm` built, `vm_compute` injectivity Qed | GO, probe_group: r4_inj, m6_inj, r4_permE, m6_permE, m6_inv_permE |
| L3 | tblA, tblB are 5-designs: every ascending 5-list of codes below 12 lies in exactly one row of each | new computation | boolean checker Qed by vm_compute; a one-row perturbation makes it false | GO, probe_orbit: design5_okA/B 0.43 s each, design5_mut false |
| L4 | r4, m6 map each table to itself (row-wise membership) | new computation | 264 membership checks Qed by vm_compute | GO, probe_orbit: stable_r4A/B, stable_m6A/B; only the forward inclusion is needed, the converse is cardinality (stab_of_sub) |
| L5 | generator invariance lifts to `pgg_G psl211_M`: `g \in pgg_G -> (g @: S \in setsA) = (S \in setsA)` | precedent shape (pgl27_orbit stabp subgroup) | Qed at 'I_12 | GO, probe_orbit: setsA_invariant, setsB_invariant, setsA_B_disjoint, all closed under the global context |
| L6 | count equality: for every coalition C of size <= 5 and every A subset C, `#rows of tblA with row cap C = A` equals the same for tblB | new computation | boolean checker Qed by vm_compute in acceptable time (target < 5 min); perturbed table fails | GO, probe_orbit: count_okT 38.7 s, count_ok5_mut_false 29.5 s |
| L7 | count equality at size 6 fails (leak witness) | new computation | one coalition and pattern with unequal counts, by vm_compute | GO, probe_orbit: leak6 |
| L8 | equal fibers give equal pushforwards of the uniform law: `(forall v, #|[set x in A | f0 x == v]| = #|[set x in A | f1 x == v]|) -> fdistmap f0 (`U HA) = fdistmap f1 (`U HA)` | new proof shape | Qed generically | GO, probe_bridge: uniform_fdistmap_of_fibres (statement needed the realType ascription on `U); axioms BOOLP3 only |
| L9 | equal conditional laws give independence through `inde_prod_fst` for the colour view at the real carrier | library object at carrier | Qed with the count hypothesis as a premise | GO, probe_bridge: colour_view_indep_of_laws, colour_view_indep_of_fibres, BOOLP3 |
| L10 | fiber of the set action over a point of the orbit has the cardinality of the stabiliser: `y \in orbit to G x -> #|[set g in G | to x g == y]| = #|'C_G[x | to]|` | library object (mathcomp action) | Qed at `'P^*` on `{set 'I_12}` | GO but REDUNDANT: the fiber set is mathcomp `amove` (action.v:198) and `amove_act` + `card_rcoset` prove it in one line; use card_orbit_stab / amove in the plan, drop the lemma |
| L11 | the orbit of a table representative under `pgg_G` is the table's set (as in subset_class_orbitE) | precedent shape (pgl27_orbit code_bfs) | statement typechecks in decomposition; proof deferred to plan | statement typechecks (setsA_orbitE); proof is a plan task, code_bfs shape |
| L12 | ts_private at k = 5 from count equality: existential re-deal | new proof shape | miniature at coalition size <= 1 Qed; full statement in decomposition | full statement psl211_private typechecks and fills ts_private; the size-1 toy in probe_bridge was FALSE as written (redeal_mini_false) and replaced by redeal_mini_family; the toy defect is in the toy, not in L12 |
| L13 | ThresholdScheme bool 'I_12 with ts_k' = 5 and ReconPlug with `rp_content := colour collapse` typecheck | library object at carrier | records built from Admitted supports, decomposition Qed | GO: orbit_scheme, psl211_plug, psl211_PI (no piSMC program needed, as pgl27_PI), psl211_profile all defined; BUT rp_content must be id, see section 5 |
| L14 | elem_bfs closure over {r4, m6, m6i} has 660 keys | new computation | `size elem_table = 660` by vm_compute; closure over {r4} alone has size 2 | GO, probe_mixing: size_elem_table 6.5 s, uniq_elem_keys, elem_closed_okT, size_closure_r4 = 2 |
| L15 | the L-step walk over 660 states is in-kernel computable at L = 570 within ten minutes | cost | walkN timed at L = 100; extrapolated cost recorded | GO and 10x cheaper than estimated: walk100_total 1.15 s, curve to L = 400 measured, L = 570 about 30 s |
| L16 | recovery: eleven reveals determine the class, ten never do (1-swap witness transported by the group) | precedent shape (pgl27_recovery) | statements typecheck in decomposition | statements typecheck, BUT the distinct-code eleven-reveal statement is content-free (any two uniq 12-tuples agreeing at 11 positions are equal, audit_recovery.v deck_eleven); the plan states the COLOUR form colour_eleven (audit_recovery.v, Qed) as the recovery theorem and keeps the code form as a corollary |
| L17 | `#|pgg_G psl211_M| = 660` | precedent shape (pgl27_card) | statement in decomposition; proof deferred | statement typechecks; proof is a plan task (pgl27_card shape) |

Rows added after the audits (2026-09-14):

| # | claim | kind | passes when | status |
|---|---|---|---|---|
| L18 | chirality: the non-square scaling of P^1(F_11), transported to the probe labelling, maps tblA onto tblB (a PGL(2,11) shuffle swaps the two classes) | new computation | `stable_ok`-style check `map_row sc tblA` subset tblB and back, vm_compute | to probe (python-verified) |
| L19 | `<r4, m6>` is 2-transitive on 'I_12 (consumed by the ten-reveal ambiguity transport) | new computation | BFS over ordered pairs reaches all 132 from (0,1), checker re-verifies, as pgl27_group word_bfs at k = 2 | to probe |
| L20 | `#|setsA| = 132` and `#|setsB| = 132` | new computation | list_to_set injective on ascending rows plus size of the tables | to probe (audit compiled the 132 counts) |
| L21 | `setsA_orbitE`: the orbit of a tblA row under `pgg_G` is setsA (and the same for B) | precedent shape (subset_class_orbitE, code_bfs over 924 subsets) | plan task | statement typechecks |
| L22 | fiber counting: `#|[set g in pgg_G | (g @: H) :&: C == A]| = #|[set B in setsA | B :&: C == A]| * #|'C_G[H | 'P^*]|`, via `amove_act`, `card_rcoset` and a partition of the fiber over the orbit | new proof shape | miniature Qed in the plan's first task; uses L20, L21 | to probe |
| L24 | all-decks dealer: the CODE view of every coalition <= 5 is independent of the secret, from the same count certificate (deck count per view = N_A * (6 - #A)! * (6 - #C + #A)!, class sizes equal) | new proof shape, sibling of ttrans_view_indep_alldecks WITHOUT the ntransitive premise | miniature Qed in the plan's bridge task | to probe |
| L25 | `setsB_orbitE`, the B-side of L21; both stabilisers have order 5 = 660 / 132 | precedent shape | plan task | statement to add |
| L23 | the row count `pcount` over `seq nat` equals `#|[set B in setsA | B :&: C == A]|` (list_to_set bijection on ascending rows) | new proof shape (class_count in pgl27_orbit.v:482 is the precedent) | plan task | to probe |

Audit-driven renames (naming audit B1, B2, C1-C13, applied in the plan, not
retroactively in the probes): every landed-vocabulary name that pgl27 already
defines in the pgg_smc namespace gets the `psl211_` prefix (`is_heart`,
`deck_ok`, `heart_set`, `subset_class`, `orbit_class`, `orbit_encode`,
`orbit_valid`, `orbit_scheme`, `orbit_recon_invariant`); `tblA`/`tblB` become
`psl211_mirror_tbl`/`psl211_hexad_tbl` and `setsA`/`setsB`
`psl211_mirror_blocks`/`psl211_hexad_blocks`; `m6i_tbl` becomes
`milk6_tbl`; `pcount` becomes `pattern_count`; `fibre` is spelled `fiber` as
the repo does; no `_of_` connectives (`uniform_fdistmap_fiberE`,
`colour_view_indep_laws`); `total` and `map_row` are renamed away from the
Corelib and matrix.v names they shadow.

Out of scope, not probed: the permutation isomorphism with PSL(2,11) on
P^1(F_11) (not used by any statement), 2-transitivity (not used), the M12
containment (not used).

## 8. Soundness invariants

- No new axiom or assumed constant: every certificate is a vm_compute'd
  boolean over literal tables, as in pgl27_orbit.v and pgl27_mixing.v.
- The distributional privacy claims are exact equalities of laws (variation
  distance zero) in the exact-shuffle model and 2^-39-close laws in the word
  model at L = 584; no computational assumption anywhere.
- Scope: coalitions are static position sets, fixed before the deal; the
  secret prior is arbitrary (`secretP : R.-fdist bool`); the dealer uses a
  fixed representative deck per class and a uniform (or L-word) shuffle; the
  observer is the colour-collapsed content, never the raw code view. The raw
  code view is NOT claimed independent for coalitions of size 3..5 and is
  false there.
- Dealer models, stated precisely (audit finding 5). Fixed-representative
  dealer plus uniform shuffle: the COLOUR view is independent of the secret
  for coalitions <= 5 and dependent at 6; the raw CODE view is dependent
  already at 3 (witness C = {0,1,2}, audit_psl211.out). All-decks dealer
  (deck uniform over the 132 * 6! * 6! valid decks of the class): even the
  CODE view is independent for coalitions <= 5 and dependent at 6, by the
  same count certificate, but NOT through `ttrans_view_indep_alldecks`,
  which carries `ntransitive t` for the whole section and is unreachable at
  t >= 3 (1320 ordered triples against 660 elements, audit_route.v). An
  all-decks lemma with the count premise is ledger row L24.
- The bound goes vacuous nowhere: the group has 660 > 0 elements, both
  classes are populated (132 rows each), every coalition of size <= 5 is
  covered by the count check (1585 coalitions), size 6 is a proven leak.
- Cited library objects, one line each: `Gen_PGGTypes` (pgg_interface.v,
  section template over sigmas); `ThresholdScheme`, `ts_recon_perm_invariant`
  (pgg_sharing_framework.v); `ReconPlug` (covering_scheme.v, four fields);
  `MonodromyProfile` (pgg_monodromy_profile.v, four fields);
  `inde_prod_fst`, `coalition_view`, `dealt_secret`, `profile_view_indep`
  (transitivity_privacy.v); `fdistmap`, `fdist_uniform`/`` `U `` (infotheo
  fdist.v); `orbit`, `'C_G[x | to]`, `'P^*` (mathcomp action.v).

## 9. Audit verdicts and what was folded (2026-09-14)

Three independent Opus agents: the probe run (PROBE-REPORT.md, CHANGES.md),
the soundness audit (AUDIT-SOUNDNESS.md, NO-GO) and the naming audit
(AUDIT-NAMING.md, NO-GO), all under notes/probes/2026-09-14-psl211/. Every
blocking finding is either fixed in the kept probe files or folded above:

- Compile defects in probe_bridge.v and probe_mixing.v (soundness 9, 10, 11;
  naming E4): fixed by the prover, recorded in CHANGES.md; all four
  zero-Admitted probes compile, assumptions at the BOOLP3 floor of
  transitivity_privacy.v, orbit_fibre_card closed under the global context.
- The size-1 toy redeal_mini was false as written (soundness 8, prover C1):
  replaced by redeal_mini_family / redeal_pattern; the full psl211_private
  is unaffected and is now audited true and sharp at 6 (soundness 7).
- rp_content colour collapse (naming E3, soundness 16): withdrawn; content
  is id, the colour observer lives in the models layer (section 5).
- L = 570 (soundness 13): the pgl27 certificate shape needs L = 584
  (section 4).
- The dealer-model sentence of section 8 (soundness 5, 6): rewritten; the
  all-decks code-view result is a new row L24, and the false precedent
  ttrans_view_indep_alldecks is dropped.
- Row-count to element-count link (naming E2, soundness 17, 20): rows
  L20-L25 added; orbit_fibre_card retired in favour of mathcomp amove.
- Overclaims (soundness 2, 3, 15; naming A10, A15): criterion is a lower
  bound; equal orbit sizes needed by the raw-count certificate; "M12 rung"
  qualified to the colour observer; 2-transitivity is consumed by the
  ambiguity transport and gets row L19; the two-letter alphabet is gone.
- Chirality (naming A17): row L18.
- Names (naming B, C): the psl211_ prefix and the renames listed under the
  ledger; applied in the plan, the probes keep their names as evidence.

Not yet probed rows after the fold: L18, L19, L20, L22, L23, L24 (all
vm_compute or one-lemma shapes with named precedents) and the proofs of
L11, L16, L17, L21, L25 (plan tasks). Open by choice: instantiating R at a
closed real type (soundness 19).
