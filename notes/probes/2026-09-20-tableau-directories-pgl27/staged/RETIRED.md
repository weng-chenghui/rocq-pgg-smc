# What the `cp` retires at PGL(2,7), and every reference to it

Checked at HEAD `154c742` on branch `feat/tableau-extensions-probe`.
`git grep -n pgl27_rows` and `git grep -n pgl27_proximity` over the whole
tracked tree, with `notes/` and the agent worktrees under `.claude/`
excluded. Reverse closures computed from the `Require` lines of every file in
a `-R` or `-Q` directory of `_CoqProject`, transitively.

## Files to `git rm`

| Path | Lines | Contents |
|---|---|---|
| `instances/pgl27/pgl27_rows.v` | 751 | 49 declarations, eight of them recorded `Fail`s, all of which land in `instances/pgl27/tableau/` |

## Files replaced in place

| Path | Lines before | Lines after | What leaves |
|---|---|---|---|
| `instances/pgl27/pgl27_proximity.v` | 624 | 286 | 21 of its 26 declarations, and its `Require` of `pgl27_rows`, `pgg_tableau`, `pgg_tableau_syntax` and `pgg_analysis_manifest`; it gains `pgl27_static_obsE` and `pgl27_static_obs_funE` from `pgl27_rows.v` |

The staged reduced copy is `staged/instances/pgl27/pgl27_proximity.v`. It
keeps `pgl27_word_secret`, `pgl27_word_proximity_close`, `pgl27_pow2_40_ge1`,
`pgl27_pow2_40_gt0` and `pgl27_word_uniform_ideal_close_false` with the
section that scopes it, each token-identical to production's, and its
innermost `Local Open Scope` is unchanged.

It gains two declarations rather than an import. `pgl27_word_proximity_close`
rewrites twice with `pgl27_static_obsE`
(`instances/pgl27/pgl27_proximity.v:258`, `:268`), so `pgl27_static_obsE` and
`pgl27_static_obs_funE` come here from `pgl27_rows.v` and the file requires no
tableau module. They are the only two declarations of `pgl27_rows.v` that do
not land in `instances/pgl27/tableau/`.

## References that must change, in code

| File:line | Text at that line | What it becomes |
|---|---|---|
| `_CoqProject:224` | `instances/pgl27/pgl27_rows.v` | removed |
| `_CoqProject:233` | `instances/pgl27/pgl27_proximity.v` | moved up, to sit ahead of all six phase files; see `../STATUS.md` for the lines in order |
| `instances/pgl27/pgl27_proximity.v:121` | `From pgg_smc Require Import pgl27_rows.` | removed, along with `pgg_tableau`, `pgg_tableau_syntax` and `pgg_analysis_manifest`, which nothing that stays uses; the staged reduced copy's import list carries the change |

**The line numbers move.** `224` and `233` are this file's numbers at HEAD
`154c742`. The PSL(2,11) landing is in flight and has already edited
`_CoqProject`, so the main session re-derives both numbers by name at the
time of the `cp` rather than trusting these. Nothing else about the edit
depends on them.

`instances/pgl27/pgl27_proximity.v` is the only file in the tree that
`Require`s `pgl27_rows`. No file `Require`s `pgl27_proximity`. No source
anywhere writes a fully qualified `pgg_smc.<module>.<ident>` for a name this
batch moves, so no cited name changes.

## References that must change, in a comment, DEFERRED

Neither repoint below is applied with this instance. Both wait for one pass
after all four instances have landed, so `manifest/pgg_analysis_manifest.v`
and its reverse closure are rebuilt once rather than four times. The staged
copies sit in `../staged-comments/` and are regenerated from production by
`../stage_edits.py` on every run, so they cannot freeze against a base that
moves.

Two production files cite a file whose contents move. Each staged copy is
generated by `../stage_edits.py` from production by anchored replacement, so
it cannot freeze against a base that moves; each is verified comment-only by
comparing the comment-stripped text of the two copies, and no line exceeds 80
bytes.

| File:line | Text at that line | What it should cite after the move |
|---|---|---|
| `instances/pgl27/pgl27_encoding_r5.v:14` | `(* pgl27_rows.v descend. Every leakage value below is a value of the scheme   *)` | `pgl27_tableau_analysis_bridged.v`, which is where the three `_rowE` lemmas that discharge the manifest's PGL(2,7) rows land |
| `manifest/pgg_analysis_manifest.v:723` | `(*                        instances/pgl27/pgl27_proximity.v, which identifies *)` | `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v`, where `pgl27_prior_viewE` lands |
| `manifest/pgg_analysis_manifest.v:730` | `(*                          instances/pgl27/pgl27_proximity.v, whose          *)` | the same file, where `pgl27_prior_exact_witness` lands |

Reverse closures of the two, so the cost of a comment is visible:

| File | Modules that recompile |
|---|---|
| `instances/pgl27/pgl27_encoding_r5.v` | 4: `pgl27_trace_encoding`, `pgl27_leakage_r5`, `pgl27_leakage_r7`, `pgl27_encoding_compare` |
| `manifest/pgg_analysis_manifest.v` | 17 at this HEAD: `pgg_tableau`, `pgg_tableau_syntax`, `pgg_tableau_arm_relations`, `pgl27_rows`, `pgl27_proximity`, `five_card_rows`, `five_card_proximity`, `psl211_rows`, `psl211_word_proximity`, `psl211_reading_constancy`, the six `s5_tableau_*`, `pgg_analysis_client`. After the move the PGL(2,7) entry becomes six phase files, so 22 |

Neither file is in the forward closure of `instances/psl211/psl211_endpoints.v`:
both sit above it in the `Require` graph, so neither edit can rebuild it.

## References in notes and plans, which are not edited

`docs/superpowers/plans/2026-09-15-psl211-plan-b-alldecks.md`,
`docs/superpowers/plans/2026-09-19-general-dealer-law-landing.md` and
`docs/superpowers/plans/2026-09-19-kim-spectral-landing.md` cite
`pgl27_rows.v` with line numbers. They are dated records of work already
done and are left as written, as the S5 pilot left its own. Nothing under
`paper/`, `paper-wadt2026/`, `paper-wadt2026-baseline-application/`,
`README.md`, `Makefile`, `Makefile.rocq`, `scripts/` or `blueprint/`
mentions either file name. `.Makefile.rocq.d` is generated and untracked.
