--- history/STATUS.2026-09-19-before-fix2.md	2026-09-19 12:24:16
+++ STATUS.md	2026-09-19 13:17:12
@@ -15,21 +15,30 @@
 rewrites the prose the two audits found false; what it did and did not do is
 recorded in "Fix pass 1" below.
 
+Fix pass 2 applied on 2026-09-19 after the two second-round audits returned
+NO-GO (`soundness-audit-2.md`, `naming-audit-2.md`). Every file the pass
+touched was copied to `history/<name>.2026-09-19-before-fix2<ext>` first. The
+pass declares the observer the two bridge corollaries are stated at, gives
+each of them a capability line, renames one facade alias, rebuilds the import
+table from a script, and turns the fidelity file's two second directions into
+derivations from the published certified programs. What it did and did not do
+is recorded in "Fix pass 2" below.
+
 ## Verdict per ledger row
 
 | ID | Verdict | Evidence |
 |---|---|---|
 | L1a | GO | `var_dist_supp.v` compiles alone in 4.6 s at mathcomp plus infotheo, requiring no PGG file. None of its five lemmas duplicates infotheo or the tree. |
-| L2 | GO | `five_card_mixing.v` compiles in 4.3 s, requires neither the manifest, nor `pgg_analysis_status`, nor the Tableau files, nor the facade. Twenty-four declarations: the recomputed cone of twenty-one, the moved tying equation and the two coalition bounds the fix pass adds. |
+| L2 | GO | `five_card_mixing.v` compiles in 4.3 s, requires neither the manifest, nor `pgg_analysis_status`, nor the Tableau files, nor the facade. Twenty-four declarations: the recomputed cone of twenty-one, the moved identification equation and the two coalition bounds the fix pass adds. |
 | L3 | GO | `five_card_analysis.v` compiles in 4.0 s with six theorem aliases, a split status alias and the moved pins. Every changed line listed below. |
 | L4 | GO | `pgg_analysis_manifest.v` compiles in 6.6 s with both rows moved, the five pins following, three new spelled-type pins, both docstrings, the Row 3, 4 and 5 tables and the "Absent capabilities" paragraph rewritten. |
-| L5 | GO | `pgg_tableau.v`, `pgg_tableau_syntax.v`, `s5_rows.v`, `pgl27_rows.v`, `psl211_rows.v` compile against the manifest copy, each with one code hunk and that hunk an import. `pgg_analysis_client.v` has two: the import, and one landing hunk, the section-7 block the fix pass corrects. `psl211_endpoints.v` is only loaded. |
+| L5 | GO | `pgg_tableau.v`, `pgg_tableau_syntax.v`, `s5_rows.v`, `pgl27_rows.v`, `psl211_rows.v` compile against the manifest copy, each with one code hunk and that hunk a `Require` line, so their permanent text is production's. `pgg_analysis_client.v` has two code hunks: the `Require` line, and the section-7 block, which fix pass 1 corrects and fix pass 2 renames one alias in. `psl211_endpoints.v` is only loaded. |
 | L6 | GO | `five_card_rows.v` compiles in 7.2 s with four certificates, four programs, two row equations by `erefl`, three recorded failures, the number lemmas, the repricing identities and a rewritten header. `five_card_row_repeated_at_manifest_level` is gone and nothing else landed on 2026-09-19 is removed. |
 | L7 | GO | Five breakages found by the compiler, all five predicted by SRC S8. No unpredicted error; no predicted error missing. Table below. |
 | L8 | GO | 195 production `.v` files searched by proposition. Every five-card hit is inside one of the three files this landing changes. Table below. |
-| L9 | GO | 38 diff hunks over nine copies; with comments stripped, 23 code hunks, of which 10 are import repoints and 13 are the landing itself. |
-| L10 | GO | 71 `Print Assumptions` blocks: 9 closed under the global context, 62 the classical trio and nothing else. No repository axiom. |
-| L11 | GO | Names as SRC left them after its five naming audits, except the section rename and the fix pass's one rename, both below. No collision with a production name. |
+| L9 | GO | 43 diff hunks over nine copies; with comments stripped, 23 code hunks, of which 10 hold a `Require` line and 13 are the landing itself. `diffs/` is regenerated by `tools/regen_diffs.py`, which prints the table. |
+| L10 | GO | 72 `Print Assumptions` blocks: 10 closed under the global context, 62 the classical trio and nothing else. No repository axiom. |
+| L11 | GO | Names as SRC left them after its five naming audits, except the section rename, fix pass 1's one rename, fix pass 2's rename of `exec_transfer_status` and its new facade alias `static_obs`, all below. No collision with a production name. |
 | L12 | GO | Eleven production files recompile, as S10 predicted for option 2, and `instances/psl211/psl211_endpoints.vo` is in the reverse closure of none of them, so the landing never recompiles it. |
 
 ## Compile table
@@ -45,24 +54,32 @@
 
 | file | rc | time |
 |---|---|---|
-| `var_dist_supp.v` | 0 | 4.2 s |
-| `five_card_mixing.v` | 0 | 4.3 s |
-| `five_card_analysis.v` | 0 | 4.0 s |
-| `pgg_analysis_manifest.v` | 0 | 6.8 s |
-| `pgg_tableau.v` | 0 | 13.9 s |
-| `pgg_tableau_syntax.v` | 0 | 4.6 s |
-| `pgg_analysis_client.v` | 0 | 3.9 s |
-| `s5_rows.v` | 0 | 4.0 s |
-| `pgl27_rows.v` | 0 | 6.4 s |
-| `psl211_rows.v` | 0 | 5.5 s |
-| `five_card_rows.v` | 0 | 7.2 s |
-| `kim_landing_fidelity.v` | 0 | 30.7 s |
+| `var_dist_supp.v` | 0 | 4.5 s |
+| `five_card_mixing.v` | 0 | 4.6 s |
+| `five_card_analysis.v` | 0 | 4.5 s |
+| `pgg_analysis_manifest.v` | 0 | 6.5 s |
+| `pgg_tableau.v` | 0 | 13.5 s |
+| `pgg_tableau_syntax.v` | 0 | 4.7 s |
+| `pgg_analysis_client.v` | 0 | 4.1 s |
+| `s5_rows.v` | 0 | 4.3 s |
+| `pgl27_rows.v` | 0 | 6.7 s |
+| `psl211_rows.v` | 0 | 6.0 s |
+| `five_card_rows.v` | 0 | 7.7 s |
+| `kim_landing_fidelity.v` | 0 | 28.5 s |
 | `l7_fail_messages.v` (diagnostic, expected to fail) | 1 | 7.9 s |
 
-The manifest, the file feared long at 1905 lines, compiles in 6.8 s. The
-eleven landing files together are about 61 s, and the whole list including
-the fidelity file is about 92 s. The fidelity file grew because the fix pass
-adds four restatements and nine assumption prints to it.
+The manifest, the file feared long at 1905 lines, compiles in 6.5 s. The
+eleven landing files together are about 63 s, and the whole list including
+the fidelity file is about 92 s. Figures are from one clean run after fix
+pass 2, read off `-time` output. Four sentences cost over 3 s, and three of
+them are module loads rather than landing text: the manifest's
+`Require Export` of the three unchanged facades at 5.2 s, the client's single
+`Require` at 4.0 s and `pgg_tableau.v`'s untouched `Definition ab_port` at
+4.2 s. The slowest sentence written by the landing is `five_card_rows.v`'s
+`by [ ]` on a row equation at 3.2 s. The manifest's load sentence is the one
+figure over 5 s; in production the same sentence loads a fourth facade, so
+the permanent file pays at least as much, and the split the copy makes is not
+what costs it. The fidelity file does not land.
 
 `l7_fail_messages.v` is a diagnostic and is not in `_CoqProject`. It states,
 without the `Fail` guard, the declaration `five_card_rows.v` keeps as a
@@ -89,6 +106,27 @@
 loaded through `psl211_analysis` and recompiled by nothing the landing
 touches.
 
+What a landing actually copies is therefore smaller than the recompile set.
+Measured by `tools/import_rewrite_table.py` and by a comment-stripped diff of
+every copy against its production file:
+
+- **Five files are not copied at all**: `manifest/pgg_tableau.v`,
+  `manifest/pgg_tableau_syntax.v`, `instances/s5/s5_rows.v`,
+  `instances/pgl27/pgl27_rows.v`, `instances/psl211/psl211_rows.v`. Their
+  copies differ from production in `Require` lines only, and the permanent
+  form of each of those lines is the production line, so the production text
+  is already the permanent text. They are recompiled and nothing else.
+- **`manifest/pgg_analysis_client.v` is copied with its import block
+  restored to production's.** Its copy differs in four non-`Require` lines:
+  the section-7 comment, the two added `Check`s and the renamed status alias.
+- **The two new files are created**: `lib/var_dist_supp.v` and
+  `instances/kim2025/five_card_mixing.v`.
+- **Three files are copied and have a genuinely different import block**:
+  `instances/kim2025/five_card_analysis.v`,
+  `manifest/pgg_analysis_manifest.v` and
+  `instances/kim2025/five_card_rows.v`. Their permanent blocks are spelled in
+  full below.
+
 ## L7: the breakages, against SRC S8's predictions
 
 The manifest row change was applied first and the pins reverted to their
@@ -111,8 +149,9 @@
 occur. The two pins S8 says keep their values,
 `apr_completion five_card_row_biased = AnalysisBridged` and the two
 `apr_assumptions` pins, did keep them, and
-`FiveCardAnalysis.exec_transfer_status = StaticExecutedOnly` kept its value
-too, because the split leaves that name on the uniform path (below).
+the uniform path's status pin kept its value `StaticExecutedOnly` too,
+because the split leaves that value on the uniform path. Fix pass 2 renames
+that alias to `uniform_transfer_status`; the value and the pin are unchanged.
 
 `pgg_analysis_client.v` is safe, as S8 says: its four `Check`s name terms and
 assert nothing about their values. Confirmed by compiling it unchanged except
@@ -211,21 +250,37 @@
 the existing `centi_sample`, `centi_cut_distE`, `centi_bundle` and
 `biased_family`.
 
-The status alias split keeps the name `exec_transfer_status` on the **uniform**
-path and adds `biased_transfer_status` for the single-biased path. The
-residual is that the name says "exec" and no longer says which of the three
-executed paths it means; its docstring says which. The constraint that
-originally forced the name, the bare
-`Check FiveCardAnalysis.exec_transfer_status.` in
-`manifest/pgg_analysis_client.v`, no longer forces it, because the fix pass
-makes that file an edited file. The rename is nevertheless **left undone and
-recorded**: the soundness audit's preferred option adds a second alias for one
-value, which the facade's own contract of one alias per public value refuses
-and whose docstring would have to narrate the earlier name, and the
-alternative renames a public facade alias, which is the owner's call. Both
-options cost pin changes only inside the landing, so either is available
-whenever the owner decides.
+The status alias split puts the uniform path's status on
+`uniform_transfer_status` and adds `biased_transfer_status` for the
+single-biased path, beside `repeated_transfer_status`. Fix pass 1 left the
+uniform one under its production name `exec_transfer_status`, which named an
+execution stage all three paths share; fix pass 2 renames it, on the ground
+that the other two are named after their path's model family and the facade's
+own name for the uniform model is `uniform_sample`. `exact_transfer_status`
+is taken, by `instances/psl211/psl211_analysis.v`.
 
+A whole-word search for `exec_transfer_status` over every `.v` and `.md` file
+of the repository outside `.claude/`, `history/` and the audit files finds
+five sites, every one of them in a file the landing already edits:
+`instances/kim2025/five_card_analysis.v` (the docstring, the `Definition` and
+the `erefl` pin in the retention block),
+`manifest/pgg_analysis_manifest.v` (one `erefl` pin) and
+`manifest/pgg_analysis_client.v` (one bare `Check`). Plus one
+`Print Assumptions` line in `kim_landing_fidelity.v`, which does not land.
+**No site outside the landing's file set.** A whole-word search for the new
+name `uniform_transfer_status` over the same population returns nothing.
+
+The facade gains one observer alias, `static_obs`, in section 3, for the
+static coalition reading `static_coalition_obs` at `five_card_algebra` and
+`five_card_params`. It is the reader the two bridge corollaries are stated
+at, and the rows of the manifest that name those corollaries now declare it.
+Its body is written qualified, `pgg_instance.static_coalition_obs`, because
+`pgg_instance` is loaded but not imported at the facade and the landing adds
+no import to that file. The manifest pins it by spelled type, beside the
+other observer pins. A whole-word search for `static_obs` as a facade alias
+finds no collision: the facade's existing `static_obs_const` is a different
+identifier, and `PGL27Analysis` and `PSL211Analysis` use `static_view`.
+
 ## Fix pass 1
 
 Applied 2026-09-19 after `soundness-audit.md` and `naming-audit.md`. What
@@ -247,26 +302,31 @@
   that the mixing theorem with the constancy theorem is what gives
   `IdealFinite`; two spelled-type pins follow, in the manifest's existing pin
   style. `mixing` stays under **bound or certificate**.
-- **The certified proposition and the corollary are interderivable, in both
-  directions.** `kim_landing_fidelity.v` proves
-  `landing_centi_prop_of_indist`, `landing_centi_indist_of_prop`,
-  `landing_biased_prop_of_indist` and `landing_biased_indist_of_prop`. Neither
-  direction is a bare `exact:`, and the reason is mathematical rather than
-  performance: a certified program's proposition is stated at
+- **The certified proposition and the corollary are checked against each
+  other.** `kim_landing_fidelity.v` gained four lemmas. Fix pass 2 replaces
+  two of them, because one direction went through its own sibling; the file
+  as it now stands is described under "Fix pass 2" below. What fix pass 1
+  established and fix pass 2 keeps is that neither direction is a bare
+  `exact:`, and that the reason is mathematical rather than performance: a
+  certified program's proposition is stated at
   `sa_cut_dist (amf_sample ...)` and the corollary at the bundle's
-  `sw_rho_dist`, and those two are equal by the certificate's tying equation,
-  a `Qed` lemma, so they are not convertible. Each direction therefore
-  rewrites with that tying equation first, `kim_centi_cut_distE` for the
-  repeated row and `kim_biased_sample_cut_witnessE` for the one-cut row, and
-  closes by `exact` or `apply:`. No attempt hung and no attempt was abandoned.
-- **The one-cut tying equation moved down.** `kim_biased_sample_cut_witnessE`
+  `sw_rho_dist`, and those two are equal by the certificate's identification
+  equation, a `Qed` lemma, so they are not convertible.
+- **The one-cut identification equation moved down.**
+  `kim_biased_sample_cut_witnessE`
   leaves `five_card_rows.v` for `five_card_mixing.v`, beside
   `kim_biased_marginal_bound`, as a `Lemma ... Qed.`; opacity costs nothing,
   every row equation and certificate still compiles. It is stated at the
   adapter `kim_single_sample` at bias one hundredth, which is the shape
-  `kim_centi_cut_distE` has, and not at `amf_sample kim_biased_family`: that
-  form needs `pgg_analysis_status`, which `five_card_mixing.v` does not import
-  and must not. The facade aliases it as `biased_sample_cut_witnessE`, Row 4's
+  `kim_centi_cut_distE` has, and not at `amf_sample kim_biased_family`, which is
+  the shape `kim_centi_cut_distE` has, so the file states both identification
+  equations in one form. The `amf_sample` form follows by conversion:
+  `sa_cut_dist (amf_sample kim_biased_family R tt)` and
+  `sa_cut_dist (@kim_single_sample R (1/100) ...)` close by `by []`, because
+  `kim_biased_family` is that adapter at bias one hundredth and `amf_sample`
+  beta-reduces at any index. That is why `kim_biased_cert`'s identification
+  field still typechecks against `sc_Hd`, which the second soundness audit
+  compiled and this probe records rather than infers. The facade aliases it as `biased_sample_cut_witnessE`, Row 4's
   distribution-to-observer bridges cell and model-transfer cell name it, and
   it has its own spelled-type pin.
 - **One rename.** `five_card_row_biased_ideal_tableau`, `_rowE` and
@@ -300,9 +360,9 @@
   `kim_biased_sample_cut_witnessE`, is indexed in `five_card_mixing.v`
   instead, because that is where it now lives.
 
-Left undone, and why.
+Left undone by fix pass 1, and why.
 
-- The `exec_transfer_status` rename, recorded under "Naming" above.
+- The `exec_transfer_status` rename. Fix pass 2 applies it.
 - `var_dist_le2` stays with no proof consuming it. Soundness invariant 3 of
   the spec cites it as the scale the published numbers are read against, and
   two docstrings and the fidelity file name it, so it is kept under the
@@ -310,25 +370,205 @@
 - `instances/kim2025/five_card_kim.v` carries the same barred term in a
   parenthesis glossing `var_dist`. The landing does not edit that file.
 
-## Import rewrite at cp time
+## Fix pass 2
 
-`cp` alone does not produce the permanent files. Every `From kim_landing_probe
-Require ...` line resolves to this directory and must become `pgg_smc`. The
-list is exhaustive: no other line of any copy differs from the permanent file
-by a logical path.
+Applied 2026-09-19 after `soundness-audit-2.md` and `naming-audit-2.md`. What
+changed, by finding.
 
-| file | line as it is in the copy | line as it must be in the permanent file |
-|---|---|---|
-| `five_card_mixing.v` | `From pgg_smc Require Import perm_uniform pgg_interface pgg_collusion_bound.` and, last of the block, `From kim_landing_probe Require Import var_dist_supp.` | `From pgg_smc Require Import perm_uniform var_dist_supp.` followed by `From pgg_smc Require Import pgg_interface pgg_collusion_bound.`, and the `kim_landing_probe` line deleted. `var_dist_supp` is a `lib/` file and belongs in the `pgg_smc` library block, not appended after `algebraic_rigidity`. |
-| `five_card_analysis.v` | `From kim_landing_probe Require Import five_card_mixing.` | `From pgg_smc Require Import five_card_mixing.` |
-| `pgg_analysis_manifest.v` | `From pgg_smc Require Export pgl27_analysis s5_analysis psl211_analysis.` and `From kim_landing_probe Require Export five_card_analysis.` | the production form restored: `From pgg_smc Require Export pgl27_analysis five_card_analysis s5_analysis` with the continuation `                            psl211_analysis.` |
-| `pgg_tableau.v` | `From pgg_smc Require Import pgg_instance.` and `From kim_landing_probe Require Import pgg_analysis_manifest.` | the production form restored: `From pgg_smc Require Import pgg_instance pgg_analysis_manifest.` |
-| `pgg_tableau_syntax.v` | one `From kim_landing_probe Require ...` line | the same line at `pgg_smc` |
-| `pgg_analysis_client.v` | one `From kim_landing_probe Require ...` line | the same line at `pgg_smc` |
-| `s5_rows.v`, `pgl27_rows.v`, `psl211_rows.v` | one `From kim_landing_probe Require ...` line each | the same line at `pgg_smc` |
-| `five_card_rows.v` | `From kim_landing_probe Require Import five_card_mixing.`, `From kim_landing_probe Require Import pgg_analysis_manifest pgg_tableau.`, `From kim_landing_probe Require Import pgg_tableau_syntax.` | the same three lines at `pgg_smc` |
-| `kim_landing_fidelity.v` | six `From kim_landing_probe Require ...` lines | this file does not land: it is the probe's own evidence file |
+- **Rows 4 and 5 declare the observer their bridge corollary is stated at.**
+  The facade gains `static_obs` in section 3, the alias of
+  `static_coalition_obs` at the five-card algebra and parameters, with a
+  spelled-type pin in the manifest beside the other observer pins. Both rows'
+  observers cells name `FiveCardAnalysis.static_obs` with its carrier, Row 4
+  beside `colour_view` and Row 5 beside the seat distribution and
+  `verifier_endpoints`. Row 4's level justification no longer says the
+  corollary is at "this row's own distribution and observer" as though that
+  observer were `colour_view`: it names `static_obs` as a second declared
+  observer and says `colour_view_leak_bound` reaches the level at the
+  executed reader. No sentence claims any relation between `static_obs` and
+  `colour_view`; the only result relating a static reading to an executed one
+  at this development is `five_card_static_obsE`, which lives in the rows
+  file, above the manifest, and goes to the colour fill of the executed view.
+- **Each of the two bridge corollaries gains a capability line**, in the
+  grammatical shape of Row 2's line for `word_view_indist_via_transfer`, with
+  the row's own cut law as the distribution, `static_obs` as the observer,
+  and "approximate privacy at twice the bundle's number, derived from
+  var_dist_fdistmap_transfer and <the row's mixing alias>" as the notion. The
+  vocabulary is not widened: "approximate privacy" is already in convention
+  (3)'s closed list. No decimal is written, because no file proves one.
+- **Row 5's model-transfer cell names its identification equation**,
+  `centi_cut_distE`, as Row 4's names `biased_sample_cut_witnessE`.
+- **The uniform status alias is renamed** from `exec_transfer_status` to
+  `uniform_transfer_status`, at all five sites plus the fidelity file's
+  `Print Assumptions` line. The search behind that count is under "Naming".
+- **The import table is rebuilt by a script.**
+  `tools/import_rewrite_table.py` produces it, and its output corrected three
+  counts and found one change no audit had: `five_card_rows.v` also gains
+  `Require Import Lia.`, `From mathcomp Require Import zify.` and `lra` on
+  its reals line. The exhaustiveness sentence is gone; what the script prints
+  is what is claimed.
+- **The fidelity file's two second directions now come from the programs.**
+  `landing_centi_indist_of_program` and `landing_biased_indist_of_program`
+  replace `landing_centi_indist_of_prop` and `landing_biased_indist_of_prop`.
+  Each takes the proposition the published certified row delivers, through
+  `view_indist_of` applied to
+  `five_card_row_repeated_spectral_tableau` and to
+  `five_card_row_biased_spectral_tableau` at the row's real field and index,
+  rewrites with the certificate's identification equation and nothing else,
+  and concludes the corollary's statement. Neither mentions
+  `kim_centi_static_obs_indist` or `kim_biased_static_obs_indist`. The other
+  two, `landing_centi_prop_of_indist` and `landing_biased_prop_of_indist`, go
+  the other way, from the corollary to `SpectralPropAt` of the certificate at
+  the row's arguments, by the same equation. Both program-side lemmas
+  compiled at the first attempt; the fallback through `spectral_tail` was not
+  needed, so **both directions are derived from the published rows and
+  nothing was left underived**.
+- **Sentences.** The direction of what the bound says is corrected in the
+  mixing file and in the facade: the number bounds every advantage the
+  coalition has in telling the two committed pairs apart, which is what a
+  bound on a variation distance gives. "and no more" is gone from the one-cut
+  docstring; it now says the number is about three percent of the ceiling
+  `var_dist_le2` gives, the figure `kim_biased_cert_eps_lt2` already uses.
+  The mixing file's header gives the real reason the corollary sits below the
+  facade: the manifest sits below the file that certifies the rows and holds
+  no theorem of its own, so the theorem it names for a row's level has to be
+  a facade alias. "the seven-cut cut" becomes "the seven-cut law". The
+  certificate's second field is called the **identification** equation in all
+  five landing files and here, which is `manifest/pgg_tableau.v`'s own word
+  for it, and the earlier surface form is gone. Both remaining unrestricted
+  statements of the
+  constancy in `five_card_rows.v` carry the at-most-one-seat restriction.
+  `five_card_rows.v` no longer cites `manifest/pgg_analysis_status.v` by line
+  number. Every comment box line the pass wrote closes at the box width, and
+  `var_dist_supp.v`'s five docstrings are reflowed.
 
+Left undone by fix pass 2, and why.
+
+- `kim_biased_exact_le_eps` (naming audit A19) **stays**. It states that the
+  exact one-cut distance of `kim_one_cut_centiE`, one fiftieth, is under the
+  spectral number the certificate publishes, which is the premise of the
+  sentence the rows file makes about the price of quoting the bundle's number
+  rather than the exact one. Under the repository's "claimed or premise"
+  standard that is a premise, so it is kept and the decision is recorded here
+  rather than the lemma dropped.
+- Naming audit A16, the uniform counterpart `sample_cut_witnessE` in the
+  facade's check table. **Not applied.** That table is checked against the
+  phase-H1 minimum list, so adding an entry the list does not ask for widens
+  the minimum rather than closing an asymmetry.
+- Naming audit A14 and A18, "ceiling" used for a mutual-information bound and
+  "endpoint marginal bound" against the closed vocabulary's "endpoint
+  marginal mixing". **Not applied**, as the audit itself says: both are
+  untouched production text, outside the landing.
+- Naming audit A17, two sentences of SRC landing text that judge which
+  transfer status is "the honest one". **Not applied**: the audit records
+  them for one decision over all three and asks for no change.
+- Soundness note N3, the placement of the identification-equation line in the
+  facade's check table. **Applied**: the line now sits with the model
+  entries, where its alias lives, and is spelled "one-cut identification
+  equation".
+- Soundness note N1, the word "colour" for a reading whose carrier is
+  `'I_5`. **Not applied** as a word change; the conflation it warns of is
+  what the observer declaration above removes.
+
+## Import rewrite at cp time
+
+`cp` alone does not produce the permanent files. This table is not written by
+hand: `tools/import_rewrite_table.py` strips the comments of every copy and of
+its production file, normalizes the whitespace of every `From ... Require ...`
+sentence, allows a sentence to span several lines, and prints per file the
+production sentences the copy drops and the copy sentences production does not
+have. The rows below are that output, read back into the permanent form each
+file needs. The script's own output is the evidence; nothing here is claimed to
+be exhaustive beyond what it prints.
+
+Six copies differ from production in `Require` sentences only, and in every
+one of them the difference is the logical path. Their permanent import block is
+**the production import block, unchanged, byte for byte**:
+`pgg_tableau.v`, `pgg_tableau_syntax.v`, `pgg_analysis_client.v`,
+`s5_rows.v`, `pgl27_rows.v`, `psl211_rows.v`. Restoring production's text
+settles two things the earlier hand-written table got wrong: `s5_rows.v` and
+`pgl27_rows.v` each carry **two** `From kim_landing_probe Require ...` lines,
+not one, and `psl211_rows.v` carries **three** lines where production has two,
+because the copy split `From pgg_smc Require Import pgg_analysis_status
+pgg_analysis_manifest.` in half.
+
+Four copies have an import block that genuinely differs. Their permanent
+blocks in full:
+
+`instances/kim2025/five_card_mixing.v`, new:
+
+```
+From HB Require Import structures.
+From mathcomp Require Import all_boot all_order all_algebra fingroup perm.
+From mathcomp Require Import morphism.
+From mathcomp Require Import boolp reals.
+From infotheo Require Import realType_ext fdist proba variation_dist.
+From pgg_smc Require Import perm_uniform var_dist_supp.
+From pgg_smc Require Import pgg_interface pgg_collusion_bound.
+From pgg_smc Require Import pgg_weighted_words.
+From pgg_smc Require Import pgg_monodromy_profile.
+From pgg_smc Require Import pgg_instance pgg_functionality pgg_sample_adapter.
+From pgg_smc Require Import five_card_group five_card_program.
+From pgg_smc Require Import five_card_scheme_I5.
+From pgg_smc Require Import five_card_kim five_card_family.
+From pgg_smc Require Import den_boer_encoding den_boer_profile.
+From pgg_smc Require Import five_card_exec five_card_models.
+From pgg_reconstruct Require Import algebraic_rigidity.
+```
+
+`var_dist_supp` is a `lib/` file and joins the `pgg_smc` library block rather
+than being appended after `algebraic_rigidity`.
+
+`instances/kim2025/five_card_analysis.v`: production's block with its last
+imported-cone line followed by
+
+```
+From pgg_smc Require Import five_card_mixing.
+```
+
+`manifest/pgg_analysis_manifest.v`: production's block restored exactly, the
+single `Require Export` sentence
+
+```
+From pgg_smc Require Export pgl27_analysis five_card_analysis s5_analysis
+                            psl211_analysis.
+```
+
+in place of the copy's two sentences.
+
+`instances/kim2025/five_card_rows.v`:
+
+```
+Require Import Lia.
+From mathcomp Require Import zify.
+From mathcomp Require Import ssreflect ssrbool eqtype ssrnat seq.
+From mathcomp Require Import fintype tuple finfun finset fingroup perm.
+From mathcomp Require Import reals boolp lra.
+From infotheo Require Import fdist proba entropy.
+From pgg_smc Require Import pgg_analysis_status.
+From pgg_smc Require Import pgg_instance pgg_functionality.
+From pgg_smc Require Import pgg_sample_adapter pgg_trace_secrecy.
+From pgg_smc Require Import five_card_group five_card_program.
+From pgg_smc Require Import five_card_kim five_card_family.
+From pgg_smc Require Import den_boer_encoding den_boer_run.
+From pgg_smc Require Import five_card_leakage five_card_exec five_card_models.
+From pgg_smc Require Import kim_input_privacy.
+From pgg_smc Require Import five_card_mixing.
+From pgg_smc Require Import pgg_analysis_manifest pgg_tableau.
+From pgg_smc Require Import pgg_tableau_syntax.
+```
+
+Against production that is four additions, `Lia`, `zify`, `lra` on the reals
+line and `five_card_mixing`, besides the three repointed lines. The first
+three are what `kim_biased_exact_le_eps`, `kim_biased_cert_eps_lt2`,
+`five_card_inv50_split` and `five_card_reprice_inv25_lt2` need, and the
+earlier hand-written table did not list them, which is the second thing the
+script settles.
+
+Two files of this directory land nowhere: `l7_fail_messages.v`, the
+diagnostic, and `kim_landing_fidelity.v`, the probe's evidence file. The
+script prints both as having no permanent file.
+
 `_CoqProject` gains two lines. The production file is not in dependency order,
 so the rule is its own grouping convention, and both anchors were checked in
 the production `_CoqProject` as it stands:
@@ -385,22 +625,23 @@
 
 ## L9: the diffs
 
-Nine copies of existing files, 38 hunks with comments, 23 with comments
-stripped. The `diffs/` directory was regenerated after fix pass 1, so the
-tables below are true of the final copies. The comment diffs carry two lines
-of context and the code diffs one, which is what reproduces the counts.
+Nine copies of existing files, 43 hunks with comments, 23 with comments
+stripped. `diffs/` is regenerated by `tools/regen_diffs.py`, which was run
+after fix pass 2, so the tables below are true of the final copies. The
+comment diffs carry two lines of context and the code diffs one, which is what
+reproduces the counts; the script prints the table it writes.
 
-| copy | hunks | code hunks | what the code hunks are |
-|---|---|---|---|
-| `pgg_tableau.v` | 1 | 1 | the import of the manifest, repointed |
-| `pgg_tableau_syntax.v` | 1 | 1 | the same |
-| `s5_rows.v` | 1 | 1 | the same |
-| `pgl27_rows.v` | 1 | 1 | the same |
-| `psl211_rows.v` | 1 | 1 | the same |
-| `pgg_analysis_client.v` | 3 | 2 | the import; and the section-7 block, which gains a `Check` on a bridge alias and on the biased typed status |
-| `five_card_analysis.v` | 6 | 5 | the import of `five_card_mixing`; the tying-equation alias; three transfer aliases and the status split; the two bridge aliases; one spelled-type pin; two status pins |
-| `pgg_analysis_manifest.v` | 15 | 7 | the import of the facade copy; the two row definitions; the tying-equation pin; five spelled-type pins; the facade status pins; the two biased-row pins; the two repeated-row pins |
-| `five_card_rows.v` | 9 | 4 | three import lines; the deletion of `five_card_row_repeated_at_manifest_level` and of the tying equation, which moves down to `five_card_mixing.v`; the twenty-five new declarations and three recorded failures |
+| copy | hunks | code hunks | code hunks holding a `Require` | what the code hunks are |
+|---|---|---|---|---|
+| `pgg_tableau.v` | 1 | 1 | 1 | the import of the manifest, repointed |
+| `pgg_tableau_syntax.v` | 1 | 1 | 1 | the same |
+| `s5_rows.v` | 1 | 1 | 1 | the same, two lines |
+| `pgl27_rows.v` | 1 | 1 | 1 | the same, two lines |
+| `psl211_rows.v` | 1 | 1 | 1 | the same, three lines where production has two |
+| `pgg_analysis_client.v` | 3 | 2 | 1 | the import; and the section-7 block, which gains a `Check` on a bridge alias and on the biased typed status and carries the renamed uniform status |
+| `five_card_analysis.v` | 9 | 5 | 1 | the import of `five_card_mixing`; the identification-equation alias and the new observer alias; three transfer aliases and the status split; the two bridge aliases; one spelled-type pin; two status pins |
+| `pgg_analysis_manifest.v` | 17 | 7 | 1 | the import of the facade copy; the two row definitions; the identification-equation pin; the observer pin; five spelled-type pins; the facade status pins; the two biased-row pins; the two repeated-row pins |
+| `five_card_rows.v` | 9 | 4 | 2 | the import lines, `Lia`, `zify`, `lra`, `five_card_mixing` and the three repointed; the deletion of `five_card_row_repeated_at_manifest_level` and of the identification equation, which moves down to `five_card_mixing.v`; the twenty-five new declarations and three recorded failures |
 
 Every other code line is identical after comments are stripped. The
 comment-only hunks are the header, table and docstring rewrites listed under
@@ -412,18 +653,22 @@
 
 ## Changed lines, file by file
 
-`instances/kim2025/five_card_analysis.v`, six edits:
+`instances/kim2025/five_card_analysis.v`, eight edits, the last two by fix
+pass 2:
 
 1. `:16-17`, the header sentence "Section 7 is empty for this development and
    is documented as empty rather than omitted" becomes four lines naming what
    the section carries.
-2. `:56-57`, the phase-H1 check table gains three rows, for the two cut-law
-   distances and the reading constancy.
+2. The phase-H1 check table gains five rows: the two cut-law distances, the
+   reading constancy, the coalition reading bound at two pairs and the
+   one-cut identification equation, and fix pass 2 adds a sixth for the
+   static coalition observer.
 3. `:86`, one import line added, `five_card_mixing`.
 4. `:351-358`, the section 7 header, nine lines replaced.
 5. `:361-368`, the two status definitions replaced by three theorem aliases
-   and three typed statuses. `exec_transfer_status` keeps
-   `StaticExecutedOnly` and gains a docstring saying it is the uniform path's;
+   and three typed statuses. The uniform path's status keeps
+   `StaticExecutedOnly` and gains a docstring saying which path it is, and is
+   renamed `uniform_transfer_status` by fix pass 2;
    `biased_transfer_status := IdealFinite` is new;
    `repeated_transfer_status` moves from `NoModelComparison` to
    `IdealFinite`.
@@ -431,8 +676,13 @@
    `centi_cut_mixing` as the section's representative (the file's own contract
    at `:372-382` asks for one per section), and three status pins in place of
    two.
+7. The new observer alias `static_obs` in section 3, and the "Six carriers"
+   sentence of that section's header becoming "Seven carriers".
+8. The uniform status alias renamed, in its docstring, its `Definition` and
+   its `erefl` pin.
 
-`manifest/pgg_analysis_manifest.v`, eighteen edits:
+`manifest/pgg_analysis_manifest.v`, twenty-two edits, the last four by fix
+pass 2:
 
 1. `:75-76`, the `Require Export` line split, the facade coming from the copy.
 2. `:66`, the closed capability vocabulary of convention (3) is left as
@@ -463,6 +713,14 @@
 17. `:1779-1780`, `apr_transfer five_card_row_biased`.
 18. `:1787-1789`, `apr_completion` and `apr_transfer` of
     `five_card_row_repeated`.
+19. Rows 4 and 5 observers cells, each gaining `FiveCardAnalysis.static_obs`
+    with its carrier.
+20. Rows 4 and 5 capability tables, each gaining one line for the row's
+    bridge corollary.
+21. Row 5's model-transfer cell, gaining `centi_cut_distE` as the
+    identification of `centi_cut_mixing`'s law.
+22. The observers pin block, gaining one spelled-type `Check` on
+    `FiveCardAnalysis.static_obs`.
 
 `instances/kim2025/five_card_rows.v`, fourteen edits: the three import lines,
 the file-title line `:4`, the header passage `:28-50`, the two index blocks
@@ -494,10 +752,11 @@
 ## L10: assumptions
 
 `kim_landing_fidelity.v` imports the copies beside the production vocabulary
-its restatements are written in, restates the two row equations, derives each
-certified program's own proposition and the corollary the manifest's bridge
-cell names from the other, and prints the assumptions of all 71 declarations
-the landing adds or moves. Nine are closed under the global context; 62
+its restatements are written in, restates the two row equations, checks each row's
+bridge corollary against the proposition its published certified
+program delivers, in both directions, and prints the assumptions of all 72
+declarations the landing adds or moves. Ten are closed under the global
+context; 62
 report the classical trio `propositional_extensionality`,
 `functional_extensionality_dep`, `constructive_indefinite_description` and
 nothing else. No repository axiom, no `Admitted`, no `Abort`, no `Parameter`,
@@ -520,9 +779,11 @@
    begin with the identifier". It does not: every docstring in that file
    begins with a descriptive phrase, for instance `(** The repeated row: ...`
    and `(** A model built over one run does not sample another. ...`. The new
-   declarations follow the file as it is. `five_card_analysis.v`,
-   `pgl27_mixing.v` and the `lib/` files do begin with the identifier, and
-   the new declarations in those two places do too.
+   declarations follow the file as it is. `five_card_analysis.v` and
+   `pgl27_mixing.v` do begin with the identifier, and the new declarations
+   there do too. The `lib/` files do not: all 40 docstrings under `lib/` open
+   with a descriptive phrase, which is why fix pass 1 rewrote
+   `var_dist_supp.v`'s five to match.
 2. Decision 2 of the spec says the mixing file takes "the two form-2 mixing
    statements ... and whatever THEIR cone needs". Their cone needs three
    further declarations beyond the two named in the task's parenthesis, not
@@ -542,15 +803,19 @@
    settles the question for a bound of this shape by giving it no capability
    line, so the widening is reverted.
 6. SRC S8 says the facade's status alias "must split in two". It does not say
-   that `manifest/pgg_analysis_client.v:48` pins the name
-   `exec_transfer_status` with a bare `Check`. That pin is no longer a
-   constraint, because the same file carries a false sentence about section 7
-   that the landing has to correct anyway.
-7. The naming audit says the one-cut tying equation can move to
+   that `manifest/pgg_analysis_client.v` pins the uniform status alias by
+   name with a bare `Check`. That pin is no longer a constraint, because the
+   same file carries a false sentence about section 7 that the landing has to
+   correct anyway, which is what let fix pass 2 rename the alias.
+7. The naming audit says the one-cut identification equation can move to
    `five_card_mixing.v` because "everything it needs, `five_card_models` and
-   `five_card_exec`, is already imported there". Its statement as SRC writes
-   it also needs `amf_sample`, which comes from `manifest/pgg_analysis_status.v`
-   and which `five_card_mixing.v` deliberately does not import. Restating the
-   equation at the adapter `kim_single_sample`, which is the shape
-   `kim_centi_cut_distE` already has, is what makes the move possible without
-   that import.
+   `five_card_exec`, is already imported there". That is right, and the
+   reason fix pass 1 recorded for restating the equation, that `amf_sample`
+   is out of reach there, is not a fact about the tree:
+   `instances/kim2025/five_card_models.v` imports `pgg_analysis_status` and
+   `five_card_mixing.v` imports `five_card_models`, so `amf_sample` and
+   `kim_biased_family` are both in scope. The reason that holds is the one
+   recorded under "Fix pass 1" above: the equation is stated at the adapter
+   `kim_single_sample`, which is the shape `kim_centi_cut_distE` has, so the
+   file states both identification equations in one form, and the
+   `amf_sample` form follows by conversion.
