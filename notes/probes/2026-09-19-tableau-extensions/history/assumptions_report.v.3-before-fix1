(* infotheo: information theory and error-correcting codes in Rocq            *)
(* Copyright (C) 2025 infotheo authors, license: LGPL-2.1-or-later            *)
(******************************************************************************)
(* The assumption floor of every statement this probe proves                  *)
(*                                                                            *)
(* Each lemma added or reproved in stage A is printed here with the constants *)
(* its proof term rests on. The repository's floor is the three boolp         *)
(* classical constants, which reach every statement whose type mentions an    *)
(* fdist record; a statement that closes by conversion alone is Closed.       *)
(******************************************************************************)

From mathcomp Require Import ssreflect ssrbool eqtype ssrnat seq.
From mathcomp Require Import fintype finfun finset reals.
From infotheo Require Import fdist.
From pgg_smc Require Import pgg_analysis_status pgg_instance.
From pgg_smc Require Import pgg_analysis_manifest.
From tableau_ext_probe Require Import pgg_tableau pgg_tableau_syntax.
From tableau_ext_probe Require Import pgl27_rows t0_sampled_branch.

(* The composition law of the reprice terminal, reproved at an inequality. *)
Print Assumptions port_reprice.

(* The claim readers, which are definitions and carry no proof. *)
Print Assumptions port_claim.
Print Assumptions stack_claim.
Print Assumptions security_claim_of.

(* The refutation of the terminal's obligation below the proved bound. *)
Print Assumptions pgl27_word_reprice41_false.

(* The surface and the bind build one term. *)
Print Assumptions pgl27_row_word39E.

(* The four statements about the branch at Sampled. *)
Print Assumptions t0_row_uniform_rowE.
Print Assumptions t0_row_uniform_manifestE.
Print Assumptions t0_row_uniform_atE.
Print Assumptions t0_row_uniform_row_neq.
