# Stage A of the Tableau extensions probe

Directory `notes/probes/2026-09-19-tableau-extensions/`, logical path
`tableau_ext_probe`. Branch `feat/tableau-extensions-probe`, nothing committed,
no permanent file edited, `instances/psl211/psl211_endpoints.v` never touched
and never compiled.

Stage A covers ledger rows T0, C1, C2, C3, K1, G1, G2, and the start of G3
and D2. The IdealProximity arm and the instance programs are later stages.

## Verdict table

| Row | Verdict | Where |
|---|---|---|
| T0 | GO, no framework change needed | `t0_sampled_branch.v`, `t0_sampled_branch_pgl27.v` |
| C1 | GO | `pgg_tableau.v`, `ConcludePayload` and `port_conclude` |
| C2 | GO, three payloads change in the tree | `pgl27_rows.v` and, since the Kim landing, `five_card_rows.v` |
| C3 | GO | `pgl27_rows.v`, `pgl27_reprice41` and `pgl27_word_reprice41_false`, with the `Fail pgl27_row_word41` beside them |
| G2 | GO, `conclude` costs no keyword | `pgg_tableau_syntax.v`, the `conclude` notation rule before the `publish` one |
| K1 | GO | `pgg_tableau.v`, four rows copies |
| G1 | GO at baseline, after C, and after K | compile table below |
| G3 | the header sentences listed below | list below |
| D2 | list below | built by searching the tree |

## Step 1. The copies, and the baseline (G1)

Six full copies, keeping their base names:

| copy | source | import lines changed |
|---|---|---|
| `pgg_tableau.v` | `manifest/pgg_tableau.v` | none |
| `pgg_tableau_syntax.v` | `manifest/pgg_tableau_syntax.v` | `:110` split |
| `s5_rows.v` | `instances/s5/s5_rows.v` | `:115` split, `:116` |
| `pgl27_rows.v` | `instances/pgl27/pgl27_rows.v` | `:101` split, `:102` |
| `psl211_rows.v` | `instances/psl211/psl211_rows.v` | `:94` |
| `five_card_rows.v` | `instances/kim2025/five_card_rows.v` | `:152` split, `:153` |

A line naming both a production module and a Tableau module was split in two:
`From pgg_smc Require Import pgg_analysis_manifest pgg_tableau.` became
`From pgg_smc Require Import pgg_analysis_manifest.` followed by
`From tableau_ext_probe Require Import pgg_tableau.`

Isolation, by a script over the Require lines
(`scratchpad/isolate_check.py`): the production import closure of each copy,
computed from the production `_CoqProject` graph, contains neither
`pgg_tableau` nor `pgg_tableau_syntax`.

```
pgg_tableau.v            prod-closure=29 CLEAN
pgg_tableau_syntax.v     prod-closure=25 CLEAN
s5_rows.v                prod-closure=50 CLEAN
pgl27_rows.v             prod-closure=43 CLEAN
psl211_rows.v            prod-closure=41 CLEAN
five_card_rows.v         prod-closure=52 CLEAN
VERDICT: isolated
```

The reverse-dependants of `pgg_tableau` in the tree are exactly
`pgg_tableau_syntax` and the four rows files, confirmed by a grep over
`manifest protocol instances security smc lib reconstruct groups legacy`.

Untouched copies saved under `history/` with suffix `.0-baseline`;
`history/pgg_tableau.v.1-after-C` and `history/pgg_tableau.v.2-after-K`.

## Step 2. T0, one named `Tableau Sampled` value continued twice

**GO, with nothing needed.** File `t0_sampled_branch.v`, a new file Requiring
the copies of `five_card_rows.v` and `s5_rows.v`, so that the rows copies stay
at their baseline text for the C and K measurements.

What the file shows is one named `Tableau Sampled` value continued twice.
Both continuations write `certify ExactIndependence five_card_exact_witness`,
so they certify the same arm with the same witness: this is one security claim
published under two pairs of statuses, together with a branch point whose
payload type reduces through the name. It is not two claims over one model,
and `five_card_row_uniform_branch_atE` proves it is not, since the two
continuations hold one `AnalysisBridged` coordinate.

`t0_sampled_branch_pgl27.v` runs the same shape at PGL(2,7), where the exact
family's index is `unit` and the word family's is `R.-fdist bool`, and where
the word branch point is continued by a **spectral** payload once plainly and
once through `conclude ... by ...`. So the reduction through the name holds at
a payload type whose statement mentions the index, and at a non-unit index,
neither of which the five-card file reaches. Its two mutations are rejected
between two models of **one** instance, which is finer than the cross-instance
mutation of `t0_sampled_branch.v`.

As compiled:

```coq
Definition five_card_uniform_sampled : Tableau Sampled :=
  five_card_committed
    sample five_card_uniform_family.

Definition five_card_row_uniform_branch : PublishedRow :=
  five_card_uniform_sampled
    certify ExactIndependence five_card_exact_witness
    |> publish StaticExecutedOnly BaselineClassicalOnly.

Definition five_card_row_uniform_branch_ideal : PublishedRow :=
  five_card_uniform_sampled
    certify ExactIndependence five_card_exact_witness
    |> publish IdealFinite BaselineClassicalOnly.

Lemma five_card_row_uniform_branch_rowE :
  published_row five_card_row_uniform_branch = five_card_row_uniform.
Proof. by []. Qed.

Lemma five_card_row_uniform_branch_atE :
  [/\ published_at five_card_row_uniform_branch
      = published_at five_card_row_uniform_tableau,
      published_at five_card_row_uniform_branch_ideal
      = published_at five_card_row_uniform_tableau
    & published_at five_card_row_uniform_branch
      = published_at five_card_row_uniform_branch_ideal].
Proof. split; exact: erefl. Qed.

Lemma five_card_row_uniform_branch_transfer_neq :
  published_row five_card_row_uniform_branch
  <> published_row five_card_row_uniform_branch_ideal.
Proof. by move=> H; move: (congr1 apr_transfer H). Qed.
```

**The hazard of design audit h1 did not fire.** No `Arguments ... : simpl
never`, no extra transparent definition, no type ascription on the payload.
`sp_f (tableau_at five_card_uniform_sampled)` reduces to
`five_card_uniform_family` by delta and iota alone, so
`five_card_exact_witness` unifies with
`ExactPayload (tableau_at five_card_uniform_sampled)` where the clause is
written, and `five_card_row_uniform_branch_rowE` holds by `erefl` at the first
attempt.

Mutation, recorded `Fail`, first message line:

```
Fail Definition s5_cross_instance : Tableau AnalysisBridged :=
  s5_rand_sampled certify ExactIndependence five_card_exact_witness.
-> The term "five_card_exact_witness" has type
```

with `cannot unify "ExactWitness (amf_sample five_card_uniform_family R idx)"
and "ExactWitness (amf_sample (sp_f (tableau_at s5_rand_sampled)) R idx)"`. The
mirror mutation, the S5 witness on the five-card branch point, is rejected the
same way. Both families are indexed by `unit`, so `idx` unifies and the two
sample adapters are what the message compares; the PGL(2,7) file's two
mutations stop one binder earlier, at the index type itself.

**A hang shape, found by recompiling from source and removed.** The first
version of `t0_sampled_branch_pgl27.v` stated each pair of coordinate
equations as one conjunction closed by `by []`. One sentence, the `by [].` of
`pgl27_row_word_branch_atE`, cost **683.103 secs (670.702u, 8.966s)**. The
cause is not conversion. Measured on 2026-09-19 against the built `.vo`, so
that only the proof sentences are timed:

| goal | tactic | time |
|---|---|---|
| `published_at pgl27_row_word_branch = published_at pgl27_row_word_tableau` | `exact: erefl` | 0.133 s |
| the same | `by []` | 0.087 s |
| `published_at pgl27_row_word_branch = published_at pgl27_row_word_branch39` | `exact: erefl` | 0.133 s |
| the same | `by []` | did not return in 130 s |
| the conjunction of the two | `split; exact: erefl` | 0.145 s |
| the conjunction of the two | `by split` | did not return in 200 s |

So the slow equation is the second, the branch point against its own
**concluded** continuation, and the slow tactic is `by []`. ssreflect's `done`
spends its search before it reaches reflexivity, and on a goal whose two sides
are `published_at` of a row without `conclude` and of a row with it that
search did not return inside the bounds in the table above, 130 s for the
equation and 200 s for the conjunction. The conversion the goal actually
needs is a tenth of a second. Neither equation needs weakening: both are
provable, and provable fast.

Each conjunction is now two lemmas closed by `exact: erefl`:
`pgl27_row_exact_branch_atE` and `pgl27_row_exact_branch_ideal_atE`,
`pgl27_row_word_branch_atE` and `pgl27_row_word_branch39_atE`. The file's
slowest sentence is now `exact: erefl` at 0.149 s, and its only sentences over
one second are two `Require Import` lines. Scratch measurements:
`scratchpad/t0b_eq1.v`, `t0b_conj.v`, `t0b_eq2done.v`.

**Open for stage B.** Two shapes stage A does not exercise. One named
`Sampled` value continued through two *different* arms is the first of them,
and it is stage B's first ledger item. A mutation between two adapters that
share an index type within one instance is the second: both cross-model
mutations of `t0_sampled_branch_pgl27.v` are rejected on the index type,
because the two PGL(2,7) families happen to have different index types, and
stage B's ideal and actual families over one instance will share one.

## Step 3. C, `conclude` at an inequality

### C1, the framework

`pgg_tableau.v`, `ConcludePayload` and `port_conclude`, equality weakened to
`<=`:

```coq
Definition ConcludePayload (c : Reprice) (q : StackAt AnalysisBridged) : Type :=
  forall (R : realType) (idx : amf_index (ab_f q) R),
    match ab_port q R idx with
    | ExactIndependence _ => unit
    | SpectralDecay cert => cert_eps cert <= odflt (cert_eps cert) (c R)
    end.

Lemma port_conclude (c : Reprice) (R : realType) (A : PGGAlgebraic)
    (E : ExecutionParams A) (sa : SampleAdapter R (instance_exec E))
    (p : SecurityPort sa) :
  PortProp no_reprice p ->
  (match p with
   | ExactIndependence _ => unit
   | SpectralDecay cert => cert_eps cert <= odflt (cert_eps cert) (c R)
   end) ->
  PortProp c p.
Proof.
case: p => [w|cert] //= H1 H2 C x x' HC.
exact: Order.POrderTheory.le_trans (H1 C x x' HC) H2.
Qed.
```

`conclude` itself is unchanged. One name had to be qualified: bare `le_trans`
is not in scope in this file, which imports `GRing.Theory Num.Theory` and not
`Order.Theory`.

### C2, the payloads in the importers

`conclude` is used in two production files outside the framework. The search
over `manifest protocol instances security smc lib reconstruct groups legacy`
for `RepricePayload`, `port_reprice`, `cert_eps`, `Reprice`, `no_reprice` and
`conclude` was run before the Kim landing and returned hits outside
`manifest/pgg_tableau.v` in `instances/pgl27/pgl27_rows.v` alone, at `:371`,
`:382` and `:393`, beside the English word in a `legacy/reconstruct/ag_code.v`
comment and a second at `security/pgg_mixing.v:304`. Re-run after that
landing, it returns `instances/kim2025/five_card_rows.v` as well. Three
payloads change under C and not one.

In the copy of `pgl27_rows.v`:

| | text |
|---|---|
| old (`:382`) | `;;; conclude pgl27_reprice39 of (fun R _ => pow2_split R)` |
| new (`:382`) | `\|> conclude pgl27_reprice39 by (fun R _ => ssr_ext.eqW (pow2_split R))` |

In the refreshed copy of `five_card_rows.v`, two more, both written in the
keyword surface. The one-cut row takes the same one-token fix; the repeated
row takes the route of the fix pass 3 section below and needs no wrapper.

`ssr_ext.eqW : x = y -> (x <= y)%O` is infotheo's, reachable under its
qualified name from this file's existing `From infotheo Require Import fdist`.
Searched first: mathcomp has no bare `x = y -> x <= y` in `Order` or
`Num.Theory` (`Search (_ = _ -> (_ <= _)%O)` returns `eq_leq` for `nat` only),
so a name was not guessed.

`pgl27_word_bridge` and the two `restate` uses are not affected: `restate`
takes a `RestatePayload` and never a `ConcludePayload`.

The baseline's recorded `Fail`, `pgl27_row_word39_bare` on a bare
`pow2_split`, still failed after C, but for two reasons at once instead of
one, so fix pass 1 replaced it rather than keeping it. What the copy records
is `pgl27_row_word39_unindexed` and `pgl27_row_word39_unindexed_bind`; the
payload, the message and the reason are in the D8 entry below.

### C3, a number below the proved one is refutable

```coq
Definition pgl27_reprice41 : Reprice := fun R => Some (2%:R^-41 : R).

Lemma pgl27_word_reprice41_false (R : realType) (secretP : R.-fdist bool) :
  ~~ (cert_eps (@pgl27_word_cert R secretP)
      <= odflt (cert_eps (@pgl27_word_cert R secretP)) (pgl27_reprice41 R)).
Proof.
rewrite /cert_eps /= pow2_split -Order.TotalTheory.ltNge.
rewrite ltf_pV2 ?posrE ?exprn_gt0 //.
by rewrite ltr_eXn2l ?ltr1n.
Qed.
```

and the row at that number is rejected where its terminal is written:

```
Fail Definition pgl27_row_word41 : PublishedRowAt pgl27_reprice41 := ...
-> The term "ssr_ext.eqW (pow2_split R)" has type
   "is_true (2 ^- 40 + 2 ^- 40 <= 2 ^- 39)"
   while it is expected to have type
   "... | SpectralDecay cert =>
       is_true (cert_eps cert <= odflt (cert_eps cert) (pgl27_reprice41 R))"
```

`@pgl27_word_cert R secretP` needs the `@`: `R` occurs in the type of
`secretP`, so `Set Implicit Arguments` makes it implicit and the bare
application is read as `pgl27_word_cert secretP`.

### The three comments rewritten

`ConcludePayload`, `port_conclude` and `conclude` each said the terminal renames
a number. Rewritten so that each says it publishes an upper bound, and that a
row may not publish a number its certificate does not reach.

## Step 4. G2, the `conclude` notation

Added to `pgg_tableau_syntax.v`, immediately before the `publish` rule:

```coq
Notation "s |> 'conclude' c 'by' p" := (s ;;; conclude c of p)
  (at level 90, left associativity, c at level 0, p at level 0).
```

`publish` after `conclude` typechecks through the notation exactly as in the
raw form: `conclude` returns `TableauAt AnalysisBridged (BridgedProp c)`, and
`|> publish t a` binds at the same level and associates to the left, so the
row reads as one chain. `pgl27_row_word39` in the copy:

```coq
Definition pgl27_row_word39 : PublishedRowAt pgl27_reprice39 :=
  pgl27_dealt
    sample  pgl27_word_family
    certify SpectralDecay pgl27_word_cert
    |> conclude pgl27_reprice39 by (fun R _ => ssr_ext.eqW (pow2_split R))
    |> publish IdealFinite BaselineClassicalOnly.

Definition pgl27_row_word39_bind : PublishedRowAt pgl27_reprice39 :=
  pgl27_dealt
    ;;; sample_step of pgl27_word_family
    ;;; certify_spectral of pgl27_word_cert
    ;;; conclude pgl27_reprice39 of (fun R _ => ssr_ext.eqW (pow2_split R))
    ;;; publish BaselineClassicalOnly of IdealFinite.

Lemma pgl27_row_word39_bindE : pgl27_row_word39 = pgl27_row_word39_bind.
Proof. by []. Qed.
```

`pgl27_row_word39_unindexed`, in the new surface, fails, and the bind
spelling of the same rejection is kept beside it.

### The keyword measurement

Two words are written in the rule, `conclude` and `by`. Measured on
2026-09-19, in a file requiring the extended surface
(`g2_keyword_measure.v`, and `scratchpad/q_kw.v` for the negative half):

| token | position in the rule | binds as an identifier? |
|---|---|---|
| `conclude` | right after the literal `\|>` | yes |
| `by` | right after the slot `c` | no, and it did not before either |
| `sample` (control) | right after a slot, existing rule | no |

- `Check conclude.` prints `conclude : Reprice -> forall q : StackAt
  AnalysisBridged, ...`, and
  `Definition conclude_stays_bindable (conclude : nat) : nat := conclude.`
  compiles. So the rule adds **no** new global keyword, and the header's
  count of nineteen is unchanged.
- `Definition kw_sample (sample : nat) : nat := sample.` is
  `Syntax error: [name] expected after '(' (in [closed_binder])`, which is
  what a keyword costs.
- `Definition kw_by (by : nat) : nat := by.` is the same syntax error in a
  file requiring nothing but `From mathcomp Require Import ssreflect`, so
  `by` was a keyword before this surface, as `at` was. The rule therefore
  needs no alternative word, and `at ... with` and `of` were not needed.
- A parse error is raised before `Fail` can see the command, so the two
  negative measurements are recorded here and not as `Fail`s in the file.
- No existing program broke: all six copies compile, and
  `pgl27_row_word39_bindE` says the two surfaces build one term.

## Step 5. K1, the arm a finished row carries

Added to `pgg_tableau.v`, all by `erefl` at every use:

```coq
Variant SecurityArm := ExactIndependenceArm | SpectralDecayArm.

Definition port_arm (R : realType) (A : PGGAlgebraic)
    (E : ExecutionParams A) (sa : SampleAdapter R (instance_exec E))
    (p : SecurityPort sa) : SecurityArm :=
  match p with
  | ExactIndependence _ => ExactIndependenceArm
  | SpectralDecay _ => SpectralDecayArm
  end.
Arguments port_arm {R A E sa} p.

Definition ab_arm (q : StackAt AnalysisBridged) (R : realType)
    (idx : amf_index (ab_f q) R) : SecurityArm :=
  port_arm (ab_port q R idx).
Arguments ab_arm : clear implicits.

Definition security_arm_of (c : Reprice) (r : PublishedRowAt c)
    (R : realType) (idx : amf_index (ab_f (published_at r)) R)
  : SecurityArm :=
  ab_arm (published_at r) R idx.
Arguments security_arm_of {c} r R idx.
```

**How the reader deals with the `forall R idx`: it takes them as arguments.**
The port is a function of the field and the index, so the arm is read at the
same two arguments the port is written at. Returning a `SecurityArm` for a
quantified port would need a choice of field, which the arm's uniformity
exists to avoid. Both arguments are explicit, because `Set Implicit Arguments`
would otherwise make `q` and `R` implicit (they occur in the type of `idx`).

That the answer does not depend on either argument is now a theorem and not a
property of the shape. Four lemmas in `pgg_tableau.v`, all `by []`:

```coq
Lemma certify_exact_armE ... :
  ab_arm (tableau_at (@certify_exact x q p)) R idx = ExactIndependenceArm.
Lemma certify_spectral_armE ... :
  ab_arm (tableau_at (@certify_spectral x q p)) R idx = SpectralDecayArm.
Lemma conclude_armE ... :
  ab_arm (tableau_at (conclude c q pf p)) R idx = ab_arm q R idx.
Lemma publish_armE ... :
  security_arm_of (publish a q pf t) R idx = ab_arm q R idx.
```

The first two are quantified over the data, the payload, the field and the
index, so every row the two certify statements build has an arm constant in
`R` and `idx`, and the two terminals carry it through unchanged.

One named lemma per published program, beside its `rowE`, each
`Proof. by []. Qed.`:

| file | lemma | statement |
|---|---|---|
| `pgl27_rows.v` | `pgl27_row_exact_armE` | `security_arm_of pgl27_row_exact_tableau R idx = ExactIndependenceArm` |
| `pgl27_rows.v` | `pgl27_row_word_armE` | `security_arm_of pgl27_row_word_tableau R idx = SpectralDecayArm` |
| `pgl27_rows.v` | `pgl27_row_word39_armE` | `security_arm_of pgl27_row_word39 R idx = SpectralDecayArm` |
| `five_card_rows.v` | `five_card_row_uniform_armE` | `security_arm_of five_card_row_uniform_tableau R idx = ExactIndependenceArm` |
| `s5_rows.v` | `s5_row_rand_armE` | `security_arm_of s5_row_rand_tableau R idx = ExactIndependenceArm` |
| `psl211_rows.v` | `psl211_row_alldecks_armE` | `security_arm_of psl211_row_alldecks_tableau R idx = ExactIndependenceArm` |

Each spells its index type, as the `Fail`s in the same files do. What these
six record is the value the paper's table column prints for each row; the four
general lemmas are why that value can be read off the `certify` line. A named
lemma and not a `Check`, because a `Check` puts nothing in the environment for
the manifest, the facades or the paper to cite, and `Print Assumptions` cannot
reach one. Isolating counter-probe, in `pgl27_rows.v`:

```
Fail Definition pgl27_row_word_arm_neq ... :=
  (erefl : security_arm_of pgl27_row_word_tableau R idx
           = ExactIndependenceArm).
-> cannot unify "security_arm_of pgl27_row_word_tableau R idx" and
   "ExactIndependenceArm"
```

The existing `Fail pgl27_word_arm_is_not_exact`, in the section of
`pgl27_rows.v` that reads the two arms off the manifest, still fails,
unchanged.

## Step 6. G1, G3, D2

### G1, the compile table

These are the measurements taken at the baseline, after C and after K. The
numbers for the ten files as they stand are in the fix pass 3 section below;
this table is kept for the three-point comparison and not as the current
state.

Real return codes from a Python driver, one file at a time through the
machine-wide lock, `rocq compile -time`. Peak resident memory from
`RUSAGE_CHILDREN` of a fresh child per file.

| file | baseline | after C | after K |
|---|---|---|---|
| `pgg_tableau.v` | rc=0 13.0 s 1715 MB | rc=0 13.7 s 1715 MB | rc=0 12.7 s 1714 MB |
| `pgg_tableau_syntax.v` | rc=0 4.5 s 1644 MB | rc=0 4.9 s 1644 MB | rc=0 4.7 s 1644 MB |
| `pgl27_rows.v` | rc=0 6.2 s 1656 MB | rc=0 7.0 s 1654 MB | rc=0 6.3 s 1660 MB |
| `five_card_rows.v` | rc=0 4.2 s 1622 MB | rc=0 5.0 s 1622 MB | rc=0 4.1 s 1622 MB |
| `s5_rows.v` | rc=0 4.5 s 1613 MB | rc=0 4.5 s 1612 MB | rc=0 4.0 s 1612 MB |
| `psl211_rows.v` | rc=0 6.0 s 1730 MB | rc=0 6.0 s 1730 MB | rc=0 5.5 s 1730 MB |

Probe files added on top: `t0_sampled_branch.v` rc=0 3.9 s 1614 MB,
`g2_keyword_measure.v` rc=0 3.8 s 1601 MB, `assumptions_report.v` rc=0 8.5 s
1848 MB.

No sentence anywhere reached 5 s, at the baseline or after either change, so
the 20 s defect threshold was never approached. One run of `pgg_tableau.v`
measured 55.4 s; that is lock wait inside the wrapper, and the repeat under
an idle lock is 12.7 s.

### Print Assumptions

Every new or reproved statement, plus the three arm readers and the published
rows a landing would cite: `port_conclude`, `port_arm`, `ab_arm`,
`security_arm_of`, `certify_exact_armE`, `certify_spectral_armE`,
`conclude_armE`, `publish_armE`, `pgl27_row_exact_armE`,
`pgl27_row_word_armE`, `pgl27_row_word39_armE`, `five_card_row_uniform_armE`,
`s5_row_rand_armE`, `psl211_row_alldecks_armE`,
`pgl27_word_reprice41_false`, `pgl27_row_word39_bindE`,
`five_card_row_uniform_branch_rowE`, `five_card_row_uniform_branch_atE`,
`five_card_row_uniform_branch_transfer_neq`,
`pgl27_row_exact_branch_atE`, `pgl27_row_exact_branch_ideal_atE`,
`pgl27_row_exact_branch_rowE`, `pgl27_row_word_branch_atE`,
`pgl27_row_word_branch39_atE`, `pgl27_row_word_branch_armE`, and the rows
`pgl27_row_word39`, `five_card_row_uniform_branch`,
`five_card_row_uniform_branch_ideal`, `pgl27_row_exact_branch`,
`pgl27_row_word_branch39`.

Twenty-nine of the thirty print the same three constants and nothing
else: `propositional_extensionality`, `functional_extensionality_dep`,
`constructive_indefinite_description`. The floor is the documented boolp trio,
which reaches every statement whose type mentions an fdist record.

**One target prints a fourth constant**, and it is production's.
`Print Assumptions s5_row_rand_armE` also reports

```
rigidity_s5_instance.s5_group_order_eq :
  #|pgg_G (Gen_PGGTypes (n:=3) (pgg_raag_path.path_gen_tuple 3))| = 120%R
```

declared as an `Axiom` at `instances/s5/rigidity_s5_instance.v:179`, which is
in the tree today. It reaches the report through the S5 instance's own
witness, not through anything the Tableau or this probe adds. Checked, not
assumed: a scratch file requiring the **production** `s5_rows` reports the
same axiom for `s5_row_rand_tableau`, `s5_rand_exact_witness` and
`s5_row_rand_rowE` (`scratchpad/prod_s5_assum.v`, rc=0). So the probe
introduces no axiom: it adds no `Admitted`, no `Abort`, no `Parameter`, no
`admit` and no `Axiom`, and the one non-boolp constant in the report pre-dates
it. A landing that cites `s5_row_rand_armE` inherits that axiom exactly as the
production S5 row already does.

An earlier version of this file claimed every block was within the boolp trio. The scan behind that claim matched axiom names with `\w+`, which
does not match the dot of a module-qualified name, so a qualified `Axiom`
passed it silently. Any future scan of `Print Assumptions` output must allow
dots in the name.

### G3, the header sentences the changes made false or incomplete

Production line numbers.

`manifest/pgg_tableau.v`:

| line | current text | why it fails | rewritten as |
|---|---|---|---|
| `:28-29` | "A row commits to one of them and claims nothing about the other." | incomplete: nothing says the commitment is now readable | "...and security_arm_of names which of the two a finished row committed to." |
| `:43-45` | "it leaves the data and the arms untouched and moves only the real the spectral arm's proposition mentions." | incomplete: the real now moves only upward | "...and moves the real the spectral arm's proposition mentions to any upper bound of it." |
| `:50` | `SecurityPort == the arm an instance certifies` | incomplete as an index: three new names follow it | two lines added for `SecurityArm` and `port_arm` |
| `:60` | `conclude == the terminal republishing the bound` | false: it publishes an upper bound, not the same number renamed | `conclude == the terminal publishing an upper bound of the accumulated bound` |
| `:63` | (index) | no change | `ab_arm` is a level accessor, and the index lists none of them, not `ab_port`, not `ab_f`, not `sp_f` |
| `:68` | `view_indist_of == the same statement, spectral-arm name` | incomplete | `security_arm_of` line added after it |
| `:74` | `port_reprice == a port's proposition at a renamed bound` | false: at an upper bound, and the name is the operation `conclude` owns | `port_conclude == a port's proposition at a number above its own bound` |

`:17` "There are five statements." stays true: Stage A adds no statement.

`manifest/pgg_tableau_syntax.v`:

| line | current text | why it fails | rewritten as |
|---|---|---|---|
| `:36` | "The publish separator is \|>." | incomplete: `\|>` now carries two terminal rules | "The separator of the two terminal rules is \|>." |
| `:68-71` | "The tokens inputs, terminates, publish, vm_compute, ExactIndependence and SpectralDecay follow a literal and stay identifiers ... at follows a literal too and was a keyword of Rocq before this file." | incomplete once: `conclude` belongs in that list, and `by` follows a slot and is named nowhere | list gains `conclude`; `at` keeps its own clause, with the two literals it follows named; a new clause says `by` follows the slot `L` of the encoded rule, the slot `k` of the leaks rule and the slot `c` of the conclude rule, and is spent already by ssreflect |

The `by` gap at `:68-71` is **pre-existing**: `by` already follows the slot
`L` of the encoded rule and the slot `k` of the leaks rule, so the header was
incomplete on it before this probe. The new `conclude` rule makes a third, the
slot `c`. In the execute rule `by` follows the literals `terminates`,
`endpoints` and `recon`, not a slot; the first version of this file said
otherwise and was wrong. `at` follows a literal in both the rules that use it,
the literal `leaks` and the literal `SpectralDecay`, and never a slot. The
count of nineteen keywords at `:60-64` stays correct.

### D2, what a landing would change in the production files

Built by searching the tree, not from memory. Every hit of `conclude`,
`Reprice`, `RepricePayload`, `port_reprice`, `cert_eps`, `no_reprice`,
`word39`, `reprice39` over `manifest protocol instances security smc lib
reconstruct groups legacy`, plus every `^Fail ` in the five Tableau files.

**`manifest/pgg_tableau.v`** (reverse closure five: `pgg_tableau_syntax` and
the four rows files)

| line | current text | change |
|---|---|---|
| `:28-29` | header sentence on committing to one arm | one clause added |
| `:43-45` | header sentence on `conclude` | one clause changed |
| `:50`, `:60`, `:63`, `:68`, `:74` | Definitions and Key results index | four lines added, two rewritten |
| `:149-152` | `Variant SecurityPort ...` | `SecurityArm` and `port_arm` added after it |
| `:241-245` | `ab_port` and its `Arguments` | `ab_arm` added after it |
| `:607-609` | comment "one numeric identity for a spectral port" | rewritten |
| `:614` | `SpectralDecay cert => cert_eps cert = odflt (cert_eps cert) (c R)` | `=` becomes `<=` |
| `:618-621` | comment "Renaming proves nothing new about the coalition" | rewritten |
| `:628` | same equality in `port_reprice`'s statement | `=` becomes `<=` |
| `:631` | `Proof. by case: p => [w\|cert] //= H1 H2; rewrite -H2. Qed.` | two-line proof by transitivity |
| `:74`, `:610`, `:616`, `:639`, `:622`, `:632`, `:643` | `RepricePayload` (3 occurrences) and `port_reprice` (4) | renamed `ConcludePayload` and `port_conclude`, by E2; no other production file holds either name |
| `:634-637` | comment on `conclude` | rewritten |
| `:722` | end of file, after `Arguments view_indist_of {c} r.` | `security_arm_of` added |

**`manifest/pgg_tableau_syntax.v`** (reverse closure four)

| line | current text | change |
|---|---|---|
| `:36` | "The publish separator is \|>." | rewritten |
| `:68-71` | the identifier-cost sentence | rewritten |
| `:393-396` | the comment and rule of `\|> publish` | the `conclude` rule and its comment inserted before it |

**`instances/pgl27/pgl27_rows.v`**

| line | current text | change |
|---|---|---|
| `:53-54` | `pgl27_reprice39 == the name 2^-39 for the word row's bound`, `pgl27_row_word39 == the word row republished at that name` | "republished at that name" becomes "concluded at that number"; index lines added for `pgl27_row_word39_bind`, `pgl27_reprice41`, the three `_armE` lemmas, `pgl27_row_word39_bindE` and `pgl27_word_reprice41_false` if C3 and K1 land here |
| `:373-377` | "the identity that adds the two copies is pow2_split" | rewritten: the identity plus `eqW` |
| `:378-383` | `pgl27_row_word39`, raw form | rewritten in the `\|> conclude ... by ... \|> publish` surface; the raw spelling kept as `pgl27_row_word39_bind` with an `erefl` lemma |
| `:382` | `;;; conclude pgl27_reprice39 of (fun R _ => pow2_split R)` | `\|> conclude pgl27_reprice39 by (fun R _ => ssr_ext.eqW (pow2_split R))` |
| `:385-388` | "The reprice obligation is one identity per real field" | "one inequality per real field" |
| `:389-394` | `Fail pgl27_row_word39_bare` | replaced: after C a bare `pow2_split` is wrong twice over, so the recorded rejection becomes the indexed-payload one of D8, in the conclude surface and in the bind form |
| `:320-330` | the two `rowE` lemmas | three claim pins added beside them |
| `:574-582` | the comment and `Fail pgl27_word_arm_is_not_exact` | still true and still failing; its comment becomes incomplete, since the difference between the arms is now also readable through `security_arm_of` |

**`instances/kim2025/five_card_rows.v`**: `five_card_row_uniform_armE` and
its index line after `five_card_row_uniform_rowE`; and, since the Kim landing,
the two `conclude` payloads, the recorded failure, the vocabulary sites listed
in the fix pass 3 section and the surface of the two repriced rows. Named and
not numbered: that file moved twice on 2026-09-19.

**`instances/s5/s5_rows.v`**: `:285-287`, `s5_row_rand_armE` and its index
line after `s5_row_rand_rowE`.

**`instances/psl211/psl211_rows.v`**: `psl211_row_alldecks_armE` and its index
line after `psl211_row_alldecks_rowE`. Named and not numbered: the PSL landing
rewrote that file's header on 2026-09-19.

**Not changed, checked by searching**

- `manifest/pgg_analysis_manifest.v`: no hit for any reprice name and no hit
  for `pgl27_row_word39`. The manifest sits below the programs, so its
  `Check (erefl : ...)` pins and its header tables are untouched by C, by K
  and by the notation.
- `manifest/pgg_analysis_client.v`: no hit for `pgg_tableau`.
- the facade files: no alias of any reprice name.
- `instances/psl211/psl211_endpoints.v`: not in either closure; never opened.
- `legacy/reconstruct/ag_code.v:163` contains the English word "conclude" in
  a comment and is not a use.
- the recorded `Fail`s of the four rows files: eleven of the twelve are
  untouched. The twelfth was replaced in fix pass 1; see the D8 entry below.

## What in the brief or the spec turned out wrong

1. The spec's ledger row C2 expects "every existing `conclude` in the five
   importer copies" to need a payload change. There is exactly one `conclude`
   in the whole tree outside the framework, and one `Fail` beside it.
2. The brief's example payload `fun R _ => eq_le (pow2_split R)` names a
   lemma that does not exist. MathComp has no bare `x = y -> x <= y`; the
   lemma that does this is infotheo's `ssr_ext.eqW`.
3. The brief's `~ (cert_eps cert <= 2^-41)` is best stated with `~~`, since
   both sides are booleans.
4. The design audit's h1 hazard, that `sp_f` might not reduce through a named
   `Tableau Sampled` value, did not fire at all.
5. `Fail` does not catch a parse error, so the negative half of a keyword
   measurement cannot be recorded as a `Fail` inside a compiling file.

## Fix pass 1, after the naming and the soundness audits

Both audits returned NO-GO on sentences and names, and both found the Rocq
content sound. Snapshots of every file touched are under `history/` with the
suffix `.3-before-fix1`.

### D1. `SecurityClaim` becomes `SecurityArm`

The file's own word for the alternative a row commits to is *arm*, used forty
times in `pgg_tableau.v` before stage A. One word per concept:

| old | new |
|---|---|
| `SecurityClaim` | `SecurityArm` |
| `ExactViewIndependence` | `ExactIndependenceArm` |
| `SpectralViewDecay` | `SpectralDecayArm` |
| `port_claim` | `port_arm` |
| `stack_claim` | `ab_arm` |
| `security_claim_of` | `security_arm_of` |
| `pgl27_word_claim_is_not_exact` | `pgl27_row_word_arm_neq` (fix pass 2) |

The constructors are the surface literal plus `Arm`, so later stages form
`IdealProximityArm`, `LeakageCeilingArm` and `DependenceArm` by the same rule,
and no tag is spelled one word away from a port constructor. `ab_arm` joins
the block of `ab_Ht`, `ab_He`, `ab_Hr`, `ab_obs`, `ab_f`, `ab_port`, and its
index line is removed, since the index lists no level accessor at all; that
also settles the ordering the soundness audit noted. The word *claim* is kept
in prose only where it means a security claim, and `pgl27_rows.v`'s "the two
arms are different claims" becomes "different statements", the header's own
phrasing.

The soundness audit's objection that "View" names the executed reader in one
constructor and the static reading in the other is answered by the rename: the
tag now mirrors the surface literal and adds no word of its own.

What a new arm costs in the arm plumbing, so that stage B can price that
part: a constructor in `SecurityPort`, a constructor in `SecurityArm`, a
branch in `port_arm`, a branch in `PortProp`, a branch in `ConcludePayload`
and a case in `port_conclude`, whose proof splits on the port. Beside the
plumbing the arm owes its own mathematics and its own surface: a certificate
record, the arm's proposition, a payload type, a `certify` statement, a
composition law ending in `Qed`, a fifth general `_armE` lemma, one `certify`
rule in `pgg_tableau_syntax.v` with the keyword measurement repeated for each
new word, one index line in the header of `pgg_tableau.v` per new name, and
the sentences of G3 that count the arms and the statements. `SecurityArm` is
a second variant and not a projection of the first because `SecurityPort` is
indexed by the real field, the algebra, the run parameters and the sample
adapter, and a tag the manifest's table prints must be closed in none of
them.

### D2. The pins become named lemmas, over four general ones

See step 5 above for the statements. Six per-row `_armE` lemmas and four
general `_armE` lemmas, all `Proof. by []. Qed.`, all reached by
`Print Assumptions`.

### D3. `_raw` becomes `_bind`

`pgl27_row_word39_raw` is `pgl27_row_word39_bind` and
`pgl27_row_word39_bare_raw` is `pgl27_row_word39_unindexed_bind`. The tree's
word for the form these are written in is the bind, from
`pgg_tableau_syntax.v` and from the operator's own name `tableau_bind`;
`_raw` was a new word and collided in shape with `_bare`, which named a
rejection.

### D4. The keyword sentence, checked against the notation rules

`at` occurs in two rules of the copy and follows a **literal** in both, the
literal `leaks` and the literal `SpectralDecay`. `by` occurs in five rules and
follows a **slot** in three of them: the slot `L` of the encoded rule, the slot
`k` of the leaks rule, and the slot `c` of the new conclude rule. In the
execute rules it follows the literals `terminates`, `endpoints` and `recon`.
The rewritten paragraph says exactly that.

Both audits were wrong here in opposite directions. The soundness audit names
"the execute rule, the leaks rule and the conclude rule"; `by` follows a
literal in the execute rule. The naming audit names the encoded and the leaks
rules only, which counts the production file and misses the conclude rule this
probe adds.

### D5. The `SecurityArm` comment

"two rows over one model publish the same manifest row" gains the qualifier
"and one pair of statuses", which is what
`five_card_row_uniform_branch_transfer_neq` proves and
what the `security_arm_of` comment already said. The two comments now agree.

### D6. The `ab_arm` comment

The old reason, that a row with two different arms at two arguments is not
expressible because one port function answers for all of them, is false: the
port has type `forall R idx, SecurityPort (amf_sample f R idx)`, whose result
type does not mention the constructor, so a function may return one
constructor at one index and the other at another. What actually prevents it
is that `certify_exact` and `certify_spectral` are the only statements that
build a port and each writes one constructor at every argument. The comment
now states that fact about the statements. No counter-example was compiled:
no instance of the tree holds both an `ExactWitness` and a `SpectralCert` over
one sample adapter, so the branching port cannot be written today.

### D7. T0 says what it showed

The header, the two comments and the step heading above now say: one named
`Tableau Sampled` value continued twice, both continuations certifying the
same arm with the same witness, hence one security claim published under two
pairs of statuses, plus a branch point whose payload type reduces through the
name. `t0_sampled_branch_pgl27.v` adds the spectral payload and the non-unit
index, and its two mutations separate two models of one instance.

### D8. The isolating rejection

The `Fail` whose comment claimed to isolate the quantification no longer did
so after C, because a correctly quantified payload of the old *equality*
relation is now rejected too. It is replaced by the payload the soundness
audit measured as isolating: `(fun R => ssr_ext.eqW (pow2_split R))`, which
has the right relation and no index binder. Both the surface form
(`pgl27_row_word39_unindexed`) and the bind form
(`pgl27_row_word39_unindexed_bind`) are recorded, and both were checked by
compiling a scratch copy without the `Fail`, since `rocq compile` is silent on
a passing one. The reason, identical for the two:

```
The term "ssr_ext.eqW (pow2_split R)" has type
 "is_true (2 ^- 40 + 2 ^- 40 <= 2 ^- 39)"
while it is expected to have type
 "forall idx : amf_index (ab_f (tableau_at (pgl27_dealt sample
    pgl27_word_family certify SpectralDecay pgl27_word_cert))) R,
  match ab_port ... R idx with
  | ExactIndependence _ => unit
  | SpectralDecay cert =>
      cert_eps cert <= odflt (cert_eps cert) (pgl27_reprice39 R)
  end".
```

Scratch files `scratchpad/extfix_fail_a.v` and `scratchpad/extfix_fail_b.v`.
The other eleven recorded failures keep their text; `rocq compile` returning 0
on each rows file is itself the check that every `Fail` in it still fails.

### D9. `conclude`'s payload is a second door, and the monotonicity condition

The paragraph of `pgg_tableau.v` that said an instance's mathematics enters
only through a certify payload now names `conclude`'s payload as the second
entry and says what it carries at each arm: at a spectral port an inequality
between the number the row's own certificate proved and the number the row
publishes, and at an exact port nothing. The sentence on
`certify_spectral` gains the same clause. `port_conclude` and `ConcludePayload`
now state the condition the `<=` obligation rests on, that the spectral arm's
proposition is monotone in its number, and that an arm whose proposition is
not monotone in the number it carries owes a different obligation. Every
remaining "republish" is gone: `StackProp`, `TableauAt`, the `pgl27_rows.v`
section heading and its index line now say *conclude* or *concluded at*.

### D10, D11. The D2 seed list, added items

**Sentences the tree states without naming a searched identifier**, so a name
search misses them. Production line numbers, from the soundness audit,
rechecked in that file:

| file:line | what moves |
|---|---|
| `manifest/pgg_tableau.v:39-41` | "the mathematics ... enters only as the witness or the certificate a certify statement takes": `conclude`'s payload is a second entry |
| `manifest/pgg_tableau.v:329-330` | "a terminal can restate a finished row at the number a paper cites": after C, at any upper bound; and it uses "restate" for what `conclude` does, colliding with the `restate` terminal |
| `manifest/pgg_tableau.v:387` | "the family a terminal leaves when it republishes a row": the retired word |
| `manifest/pgg_tableau.v:408` | "because a terminal that republishes the bound hands back a record": the retired word |
| `manifest/pgg_tableau.v:590-591` | "The two certify statements are the only lines through which an instance's own mathematics enters a row" |
| `instances/pgl27/pgl27_rows.v:39-41` | the header paragraph on the reprice, beside the index line already listed |
| `security/pgg_mixing.v:304` | a second English-word "conclude" hit, beside `legacy/reconstruct/ag_code.v:163` |

**Code site.** `notes/probes/2026-09-19-kim-spectral-arm/kim_spectral_rows_probe.v`
holds three further `conclude`s, and two of them are rows that stop compiling
under the weakened obligation, at `:288` and `:365`. Each supplies an equality
payload where `<=` is now asked for, and each needs `ssr_ext.eqW`, the same
one-token fix `pgl27_rows.v` took. The recorded `Fail` at `:299` still fails.

The same two rows are in production. The Kim landing put them in
`instances/kim2025/five_card_rows.v` as `five_card_row_repeated39` and
`five_card_row_biased_inv25`, each with an equality payload under its
`conclude` line and with the recorded `Fail` `five_card_row_repeated39_bare`
between them, beside a third equality payload in that landing probe's
`pgl27_rows.v`. That `Fail` is the same shape and the same defect stage A
renamed `pgl27_row_word39_unindexed` for, so the two probes owed one word
between them, and the refreshed copy in this directory spends it. The sites
are named and not numbered because that file moved twice today.

**The better end state for the Kim landing.** Rather than wrapping both
payloads, the repeated row can publish `2^-39` from `kim_centi_cert` itself,
by `Order.POrderTheory.ltW (kim_centi_cert_eps_lt R idx)`. That makes
`kim_centi_cert40` and its two supporting declarations unnecessary and makes
the two rows' `published_at` coordinates one term by `erefl`, where the
equality obligation had forced two certificates and two coordinates. The rows
themselves are at two `Reprice` indices and so are of two types, and no
equation between them is well formed. Compiled by the
soundness auditor as `scratchpad/extA_audit/a3_kim_reprice.v`, rc=0, with
`Print Assumptions` giving the three boolp constants and nothing else. **Not
copied into this probe:** that file Requires `kim_spectral_rows_probe`, a file
of the Kim probe directory another agent is editing, so copying it here would
couple the two probes. The path above is the record.

**Known properties of the framework, no change proposed.**

- An exact row may be typed at any `Reprice` with a `tt` payload, so a
  published *type* may name a number no conjunct of the row mentions. The
  exact branch of `ConcludePayload` is `unit` at the baseline and after C
  alike, so this is pre-existing; it is newly relevant because a paper's table
  now has a column for the number, and that column must be read through
  `security_arm_of`. Evidence: `scratchpad/extA_audit/a4_exact_reprice.v`,
  rc=0.
- A number at or above 2, the ceiling a variation distance has, is
  publishable, and the statement is then true and vacuous. Nothing in the
  framework should guard it: a guard would attach an obligation about
  infotheo's variation distance to every conclude, would have to be repeated
  in the `unit` branch and in every future numeric arm, and would put an
  instance's numeric judgement inside a framework the Tableau keeps free of
  instance mathematics. The discipline belongs where the Kim probe puts it,
  one lemma per published number comparing it with 2. Evidence:
  `scratchpad/extA_audit/a1_c_soundness.v`, rc=0. Note for a landing that
  `var_dist_le2` exists only in
  `notes/probes/2026-09-19-kim-spectral-arm/var_dist_injective_probe.v:41`.

**Open naming question for the owner, production names.** The soundness audit
finds that "Decay" names no decay, the spectral bound being a fixed number and
not a quantity falling with a parameter, and that the pre-existing projection
`view_indist_of` calls the spectral statement a statement about a view while
`SpectralPropAt` bounds `fdistmap (static_coalition_obs C x) (sa_cut_dist sa)`,
the static reading. Both are production names this probe does not touch. If
they are renamed, `SpectralDecay`, `SpectralDecayArm` and `view_indist_of`
move together, and the surface literal of the spectral rule moves with them.

### D12. Naming SHOULD items, applied and not applied

Applied: the `port_arm` comment (fact and position, not how a pin closes); the
`ConcludePayload`, `conclude` and `port_conclude` comments; the positional
pointers "the row above" and "the line above them" replaced by the names
`pgl27_row_word_tableau` and "the certify statement", and
`pgg_tableau_syntax.v`'s "the statements above it" by "the statements of a
row"; "publishing a smaller number beside it" by "publishing a number the
certificate does not prove"; "the reprice terminal" by "the conclude terminal"
in the syntax header and in `assumptions_report.v`; the index gloss of
`SecurityArm`, `port_arm`, `security_arm_of` and `port_conclude`; the four
missing `pgl27_rows.v` index entries plus the six `_armE` ones, and an `_armE`
entry in each of the other three rows files.

Not applied, with reasons:

- The landing names for the ledger-prefixed declarations of
  `t0_sampled_branch.v`. They are a landing decision, not a probe one, and the
  probe files keep their ledger prefixes, which the naming audit itself calls
  correct there. The table stands in the audit. **Reversed in fix pass 2**:
  the second naming audit showed that `t0_sampled_branch_pgl27.v`, written in
  the same pass, carries no ledger prefix on any of its declarations, so one
  of the two files had to move and the five-card one took the landing names.
- `pgl27_word_reprice41_false` keeps `_false` and `pgl27_row_word41` keeps its
  name: both are the audit's own verdict. `pgl27_row_word39E` kept its name
  here on the same ground and is renamed in fix pass 2, where the second
  naming audit found the nearer precedent in the same file.
- The three inherited notation lines of `pgg_tableau_syntax.v` stay over 80
  bytes. They are copied verbatim from production and shortening them here
  would put the copy out of step with the file it is a copy of.

## Fix pass 2, after the second naming and soundness audits

Both second-round audits returned NO-GO, the naming one on a suffix that meant
two different statements in the two T0 files and on a recorded failure that
took the `E` suffix, the soundness one on a comment naming a mechanism the
compiler did not use and on this note's own account of a replaced `Fail`. Both
again found the Rocq content sound. Snapshots of every file touched are under
`history/` with the suffix `.5-before-fix2`.

### E1. The renames

| old | new | ground |
|---|---|---|
| `pgl27_row_word_arm_exactE` | `pgl27_row_word_arm_neq` | no recorded failure in the tree ends in `E`; `pgl27_inline_neq` is the same shape |
| `pgl27_row_word39E` | `pgl27_row_word39_bindE` | `pgl27_row_word_certE` is the file's precedent for a surface-against-bind equation and carries the coordinate word |
| `t0_row_uniform` | `five_card_row_uniform_branch` | matches `pgl27_row_exact_branch` in the sibling file |
| `t0_row_uniform_transfer` | `five_card_row_uniform_branch_ideal` | matches `pgl27_row_exact_branch_ideal`, and the status published is `IdealFinite` |
| `t0_row_uniform_atE` | `five_card_row_uniform_branch_atE` | same |
| `t0_row_uniform_manifestE` | `five_card_row_uniform_branch_rowE` | `_rowE` means `published_row <program> = <manifest constant>` in production and in the sibling file |
| `t0_row_uniform_rowE` | withdrawn | see below |
| `t0_row_uniform_row_neq` | `five_card_row_uniform_branch_transfer_neq` | the difference recorded is the transfer, and the name now says so |
| `t0_s5_sampled` | `s5_rand_sampled` | the family is `s5_rand_family`; instance, model, level, as in `five_card_uniform_sampled` |
| `t0_cross_instance` | `s5_cross_instance` | matches `pgl27_cross_model`, whose prefix is the branch point's |
| `t0_cross_instance_back` | `five_card_cross_instance_back` | matches `pgl27_cross_model_back` |
| `RepricePayload` | `ConcludePayload` | see E2 |
| `port_reprice` | `port_conclude` | see E2 |

`five_card_uniform_sampled` keeps its name: it already reads instance, model,
level.

**Collisions: zero.** Every new name was scanned with Python `re` and `\b`
over 252 `.v` files, being production, the Kim landing probe and the PSL
landing probe, with this probe excluded. The scanner was control-tested first:
`view_indist_of` returns 3 files, `SecurityPort` 2, `pgl27_reprice39` 2.

**The two file names stay.** `t0_sampled_branch.v` and
`t0_sampled_branch_pgl27.v` are probe files named after the ledger row a
reader looks them up by; only the declarations inside them take landing names.

**One lemma withdrawn rather than renamed.** `t0_row_uniform_rowE` stated
`published_row t0_row_uniform = published_row five_card_row_uniform_tableau`.
It follows from `five_card_row_uniform_branch_rowE` and the production
`five_card_row_uniform_rowE` by transitivity, nothing in the probe cited it,
and `t0_sampled_branch_pgl27.v` does without it. Its text is in
`history/t0_sampled_branch.v.5-before-fix2`. The report prints thirty
assumption blocks in place of thirty-one.

### E2. `Reprice` as a word for the operation

The obligation type and the composition lemma named the operation, which the
first naming audit's ruling reserves for `conclude`. In the copy they are now
`ConcludePayload` and `port_conclude`. `Reprice`, `no_reprice`,
`pgl27_reprice39` and `pgl27_reprice41` keep their names, since each names the
number a row is concluded at, and the four rows files print those names.

D2 seed items, measured over
`manifest protocol instances security smc lib reconstruct groups legacy`:

| carrier | occurrences | where |
|---|---|---|
| `RepricePayload` | 3 | `manifest/pgg_tableau.v`, and nowhere else |
| `port_reprice` | 4 | `manifest/pgg_tableau.v`, and nowhere else |

The Kim landing probe's copy of `pgg_tableau.v` holds the same 3 and 4 and
moves with them. The PSL landing probe has no `.v` occurrence and mentions
both carriers in its prose. Searched only: no file outside this probe was
edited.

### E3. Why the two cross-model rejections hold

The comment above `pgl27_cross_model` said the two sample adapters are
compared where the clause is written. They are not. Compiled without `Fail`,
from a scratch copy of each mutation:

```
pgl27_word_sampled certify ExactIndependence pgl27_exact_witness
-> The term "pgl27_exact_witness" has type
   "forall (R : realType) (idx : unit),
    ExactWitness (amf_sample pgl27_exact_family R idx)"
   while it is expected to have type
   "ExactPayload (tableau_at pgl27_word_sampled)"
   (cannot unify "amf_index (sp_f (tableau_at pgl27_word_sampled)) R" and
   "unit").

pgl27_exact_sampled certify SpectralDecay pgl27_word_cert
-> The term "pgl27_word_cert" has type
   "forall (R : realType) (secretP : {fdist bool}%fdist),
    SpectralCert (amf_sample pgl27_word_family R secretP)"
   while it is expected to have type
   "SpectralPayload (tableau_at pgl27_exact_sampled)"
   (cannot unify "amf_index (sp_f (tableau_at pgl27_exact_sampled)) R" and
   "{fdist bool}%fdist").
```

Unification stops at the index type of the family, one binder before any
adapter is compared, so the comment now says that and draws the consequence
for stage B, whose ideal and actual families over one instance will share an
index type.

The same sentence in `t0_sampled_branch.v` was checked the same way and is
true there, so it stays. Both five-card and S5 families are indexed by `unit`,
so `idx` unifies and the adapters are what the message compares:

```
s5_rand_sampled certify ExactIndependence five_card_exact_witness
-> cannot unify "ExactWitness (amf_sample five_card_uniform_family R idx)" and
   "ExactWitness (amf_sample (sp_f (tableau_at s5_rand_sampled)) R idx)"
```

### E4. The hang shape, second pass

No `published_at` equation anywhere in the probe is closed by `done` any more.
`five_card_row_uniform_branch_atE` was `Proof. by split. Qed.`, instant today
because no side of any conjunct is concluded, and one added conjunct away from
the shape that did not return. It is `Proof. split; exact: erefl. Qed.`, the
spelling measured at 0.145 s on the PGL(2,7) conjunction. The non-rendered
note of `t0_sampled_branch_pgl27.v` said `done` "does not return" and now says
it did not return inside the time bound, which is what was measured.

### E5. The SHOULD items

Applied: the four verdict-table citations, which are citations by content
rather than by line, since the probe's line numbers moved under fix pass 1 and
will move again; the Kim code site, which now names the landing probe's
`five_card_rows.v` at `:745`, `:788` and `:751-757`, its `pgl27_rows.v:382`,
and the recorded failure `five_card_row_repeated39_bare` there; the
type-honest statement of what the better Kim end state makes equal, which is
the two rows' `published_at` coordinates and not the rows; the cost of a new
arm, in six places, under D1; the header sentence on `conclude`'s payload,
which now says the payload is an inequality at the spectral arm and nothing at
the exact one; the comment of `certify_exact_armE`, which no longer credits
one lemma with what needs `conclude_armE` and `publish_armE` as well; the two
positional pointers of `certify_spectral_armE` and `publish_armE`, which now
name the lemmas they mean; the comment of `security_arm_of`, which gives its
own position instead of restating `SecurityArm`'s; a comment for
`pgl27_row_word39_bindE`, moved off the `_bind` definition; the position
sentence on `five_card_row_uniform_branch_rowE`; the axiom citation of
`assumptions_report.v`, which drops the line number; and four alignment
defects, a 79-column divider, an index continuation one column short in
`g2_keyword_measure.v`, and the two `==` columns of `t0_sampled_branch.v`,
which now sit at 30 in both blocks as the sibling file's do.

Not applied:

- Swapping the two sides of `pgl27_row_exact_branch_ideal_atE` and
  `pgl27_row_word_branch39_atE` so the named subject is on the left. It
  changes a statement and not a comment, an index or an alignment, which is
  outside what this pass was given. The finding stands in the audit for the
  landing.
- Renaming the Kim landing probe's `five_card_row_repeated39_bare`. That file
  belongs to another probe, which this pass may not edit. It is recorded in
  the D10 and D11 seed list instead.
- Wrapping the equality payload of that probe's `five_card_row_repeated39`,
  for the same reason. Recorded, relayed.
- `psl211_rows.v`'s file-local docstring convention, which opens eleven
  baseline comments with the identifier and an em-dash. The one comment stage
  A adds there does not use it, and the convention is the owner's to keep or
  drop, not this probe's to propagate.
- A composite lemma in `pgg_tableau.v` chaining the four general ones. The
  comment fix closes the finding on its own, and adding a declaration to the
  framework copy is a statement change, outside this pass.
- The two notes on the shape of this document, the `:63` row of the G3 table
  and the two off-by-one starts of the D2 tables. Both are NOTE and not
  SHOULD.

### E6. Names that no longer exist

Every back-quoted Rocq name in this note that is not one of these exists as a
whole word in a `.v` file of the probe or of production, checked mechanically
over 198 production and 10 probe files. The check exempts what is not a Rocq
name: file names, directory and branch names, the suffix fragments this note
quotes as fragments, and Rocq commands.

| name | what it was |
|---|---|
| `port_claim`, `stack_claim`, `security_claim_of` | the arm readers before fix pass 1 |
| `pgl27_word_claim_is_not_exact` | that pass's name for the recorded failure beside them |
| `pgl27_row_word39_raw`, `pgl27_row_word39_bare_raw` | the bind spellings before fix pass 1 |
| `pgl27_row_word39E`, `pgl27_row_word_arm_exactE` | renamed in fix pass 2, see E1 |
| `t0_row_uniform`, `t0_row_uniform_transfer`, `t0_row_uniform_rowE`, `t0_row_uniform_manifestE`, `t0_row_uniform_atE`, `t0_row_uniform_row_neq`, `t0_s5_sampled`, `t0_cross_instance`, `t0_cross_instance_back` | the five-card T0 declarations before fix pass 2, see E1 |
| `eq_le` | a lemma the brief named and MathComp does not have |
| `kw_sample`, `kw_by` | binders written in a scratch file for the keyword measurement, never in a probe file, since a parse error is raised before `Fail` can see the command |
| `closed_binder` | a token of the parse error quoted above, not a declaration |
| `five_card_row_repeated39`, `five_card_row_repeated39_bare`, `kim_centi_cert`, `kim_centi_cert40`, `kim_centi_cert_eps_lt` | declarations of the Kim probes, cited here as seed items |
| `kim_spectral_rows_probe` | the Kim arm probe's module, cited as a code site |
| `var_dist_le2` | a lemma of the Kim arm probe, cited for a landing |
| `SecurityClaim`, `ExactViewIndependence`, `SpectralViewDecay` | the tag and its two constructors before fix pass 1, in the D1 rename table |
| `IdealProximityArm`, `LeakageCeilingArm`, `DependenceArm` | names D1 proposes for the arms of later stages, written nowhere yet |
| `IdealProximity`, `certify_idealproximity` | stage B's port constructor and its certify statement, named here for the price in D1 |

### E7. The compile table after fix pass 2

Superseded by the table in the fix pass 3 section below, which measures the
same ten files after the two copies were refreshed.

All ten files of the probe's `_CoqProject`, in its order, one at a time through
the machine-wide lock, `rocq compile -time`, return codes from a Python driver.

| file | rc | wall | slowest sentence |
|---|---|---|---|
| `pgg_tableau.v` | 0 | 14.2 s | 4.230 s, `Definition ab_port` |
| `pgg_tableau_syntax.v` | 0 | 5.0 s | 1.545 s, a `Require Import` line |
| `pgl27_rows.v` | 0 | 6.9 s | 2.110 s, a `Require Import` line |
| `five_card_rows.v` | 0 | 4.5 s | 1.885 s, a `Require Import` line |
| `s5_rows.v` | 0 | 4.4 s | 1.932 s, a `Require Import` line |
| `psl211_rows.v` | 0 | 6.0 s | 1.531 s, a `Require Import` line |
| `t0_sampled_branch.v` | 0 | 4.3 s | 1.884 s, a `Require Import` line |
| `t0_sampled_branch_pgl27.v` | 0 | 4.8 s | 2.305 s, a `Require Import` line |
| `g2_keyword_measure.v` | 0 | 4.2 s | 2.033 s, a `Require Import` line |
| `assumptions_report.v` | 0 | 48.6 s | 20.953 s, `Print Assumptions psl211_row_alldecks_armE` |

No sentence outside a `Print Assumptions` command reaches 5 s. The report
prints thirty assumption blocks. Twenty-nine hold the three boolp constants
alone; the thirteenth, `s5_row_rand_armE`, holds those three and
`rigidity_s5_instance.s5_group_order_eq`, which is production's and reaches
the report through the S5 instance's own witness.

## Fix pass 3, after the third audit and two landings

Two production files moved under this probe on 2026-09-19: the PSL landing
rewrote the header of `instances/psl211/psl211_rows.v` and added
`instances/psl211/psl211_spectral_constancy.v`, and the Kim landing put Kim's
two certified rows and their two repriced forms into
`instances/kim2025/five_card_rows.v`. Both copies here were rebuilt from
today's production file, so that the diff a landing reads is stage A's changes
and nothing else. `pgl27_rows.v`, `s5_rows.v`, `pgg_tableau.v` and
`pgg_tableau_syntax.v` were compared against production and are unchanged
since the copies were taken.

### F1. The refreshed `psl211_rows.v`

Rebuilt from production. Stage A's changes to it are three: both imports of
the framework repointed to `tableau_ext_probe`, one index line, and
`psl211_row_alldecks_armE` after `psl211_row_alldecks_rowE`. Nothing else
differs from production, checked by a whole-file diff.

### F2. The refreshed `five_card_rows.v`, and the repeated row's route

Rebuilt from production, then made to compile under the `<=` obligation.

**The repeated row continues from `kim_centi_cert`.** The certificate at the
bundle's own spectral number already proves a strict inequality against the
constant the row publishes, `kim_centi_cert_eps_lt`, whose statement is
`cert_eps (kim_centi_cert R idx) < 2%:R ^- 39` at every real field and index.
Weakened by `Order.POrderTheory.ltW` it is exactly the payload `conclude`
asks for, so the row needs no second certificate and no wrapper:

```
Definition five_card_row_repeated39 : PublishedRowAt five_card_reprice39 :=
  five_card_committed
    sample  kim_centi_family
    certify SpectralDecay kim_centi_cert
    |> conclude five_card_reprice39
       by (fun R idx => Order.POrderTheory.ltW (kim_centi_cert_eps_lt R idx))
    |> publish IdealFinite BaselineClassicalOnly.
```

Because the certificate is the one the row at its own number carries, the
two rows accumulate one stack, which `five_card_row_repeated39_atE` states and
conversion decides. The constant a text cites and the expression the
certificate proved are then two readings of one security claim.

**That equation is closed by `reflexivity` and not by `exact: erefl`.** Both
close it by the kernel's conversion and only one reaches it: measured in this
file, `exact: erefl` spent 147.07 s and left the file at 151.6 s, while
`reflexivity` spends 0.068 s and leaves the file at 5.0 s. Unfolding both row
definitions first does not help, 151.50 s. The probe's rule against `by []`
and `done` on a `published_at` goal stands, and its reason, that `done` spends
a search before it reaches reflexivity, is not the reason here: the refine
path `exact: erefl` takes is what costs the time.

**The one-cut row keeps its second certificate.** `conclude` may publish only
a number at least the certificate's, and the one-cut bundle's number is
sqrt 5 over forty, about 0.0559, while the constant the row publishes is one
twenty-fifth, 0.04. The inequality runs the wrong way, so the one-cut row
cannot be concluded at one twenty-fifth from `kim_biased_cert`, and
`kim_biased_cert_exact` at the exact number stays. `kim_biased_exact_le_eps`
is the tree's statement of that direction, `1 / 50 <= sw_bound_eps
(kim_biased_marginal_bound R)`, and it is the one that makes the exact
certificate the honest carrier of the constant. That row's payload takes the
one-token fix, `ssr_ext.eqW (five_card_inv50_split R)`.

**What a landing of stage A may withdraw.** With the repeated row on this
route, `kim_centi_cert40`, `kim_centi_cert40_epsE`,
`kim_centi_marginal_bound40` and `kim_centi_cut_mixing40` are used by no row.
They are left in the copy. Their uses, measured over the tree:

| name | production | elsewhere |
|---|---|---|
| `kim_centi_cert40` | `instances/kim2025/five_card_rows.v`, its definition, its index line, and the two row lines a landing removes | the Kim arm probe, the Kim landing probe and that probe's two fidelity files |
| `kim_centi_cert40_epsE` | the same file, its statement and its index line | the same three probes |
| `kim_centi_marginal_bound40` | `instances/kim2025/five_card_mixing.v`, its definition, its index line, and `kim_centi_cut_mixing40`'s statement | the same three probes |
| `kim_centi_cut_mixing40` | the same file, its statement and its index line | the same three probes |

Withdrawing them is a Kim decision and not stage A's: a landing that takes
this route should say so in the Kim landing's own note first.

**The recorded failure.** `five_card_row_repeated39_bare` becomes
`five_card_row_repeated39_unindexed`, the shape stage A already records at
PGL(2,7): a payload with the relation `conclude` asks for and no index binder.
Compiled without `Fail`, the message is

```
The term "Order.POrderTheory.ltW (kim_centi_cert_eps_lt R tt)" has type
 "is_true (cert_eps (kim_centi_cert R tt) <= 2 ^- 39)"
while it is expected to have type
 "forall idx : amf_index (ab_f (tableau_at (five_card_committed
    sample kim_centi_family certify SpectralDecay kim_centi_cert))) R,
  match ab_port (...) R idx with
  | ExactIndependence _ => unit
  | SpectralDecay cert =>
      cert_eps cert <= odflt (cert_eps cert) (five_card_reprice39 R)
  end"
```

so the relation is right and the quantification over the index is what is
missing, which is the defect the record is kept for. The file has one surface
and so one record, where `pgl27_rows.v` has two of each.

**The five items the Kim landing's note lists.** Its first, the recorded
failure's docstring, now says one inequality per real field and per index. Its
third, the two splitting identities, now says each identity bounds its sum by
a single constant rather than names it. Its fifth is the payload count, which
C2 above now carries. Its fourth, the index line
`five_card_reprice39, five_card_reprice_inv25 == the names 2^-39 and 1/25 for
a bound`, needs no change: a `Reprice` still names a bound under `<=`, and the
copy of `pgl27_rows.v` keeps the identical gloss for `pgl27_reprice39`. Its
second is a sentence of the Kim landing's own note, which belongs to that note
and is not edited here. Beside the four, the index line for the two repriced
rows now says concluded at rather than repriced to.

**Vocabulary.** The refreshed copy inherited production's *republish* and its
*reprice* as a word for the operation, in the file header, in four index
lines, in the section banner over the two repriced rows, in the closed-form
bound's docstring, in each repriced row's own docstring and in the docstring
of the one-cut row's ceiling lemma. Each is rewritten to *conclude*,
*concluded at* or *publish*, by E2's rule, so the copy reads in one vocabulary
with the framework file it imports. A landing of stage A carries those
rewrites into production; this paragraph adds them to the D2 seed list.

### F3. The third audit's other findings

**A5**, applied: `ConcludePayload`'s comment says concluding a row where it
said a reprice. **A8**, applied: `five_card_row_uniform_branch_atE` is split
into the two lemmas the PGL(2,7) file has, `five_card_row_uniform_branch_atE`
and `five_card_row_uniform_branch_ideal_atE`, each closed by `exact: erefl`,
with the header index and `assumptions_report.v` following. Round-3 **N4**,
applied: the missing article production leaves at the head of `Reprice`'s
comment. Round-3 **N7**, applied: the forward pointer in
`t0_sampled_branch.v` now names the T0 step of this note.

**A9**, not applied, and the audit's own ruling followed. Swapping the sides
of `pgl27_row_exact_branch_ideal_atE` and `pgl27_row_word_branch39_atE` would
put each name's subject on the left, which is the tree's `E` rule, and the
audit declines to overturn the earlier decline. There is a second reason to
land it rather than spend it here: the source note in `t0_sampled_branch.v`
says of the sibling file that `done` did not return on a goal of this shape
**whose right side is a concluded row**, and that sentence is true of
`pgl27_row_word_branch39_atE` only in its present orientation. Whoever swaps
the statements rewrites that sentence in the same pass.

**A12**, answered here rather than by a new lemma: the pair
`published_row A <> published_row B` for two continuations of one branch point
is recorded once, at the five-card instance, as
`five_card_row_uniform_branch_transfer_neq`. The PGL(2,7) file's exact branch
would admit the same statement. The fact is the framework's and not the
instance's, so one witness carries it.

**The round-3 notes.** N1 is C2's sentence, rewritten above. N2 is E6's
universal, narrowed to Rocq names with two rows added for the names of later
stages. N3 is E2's per-file prose counts, dropped, the `.v` counts kept. N5
and N6 carry nothing owed and are left as the audit leaves them: the two
baseline sentences N5 names are in the D2 seed list, and N6's preference for
naming the lemmas a comment depends on stands from round 2.

**Round-2 soundness N1**, seen and nothing owed: that `conclude` cannot be
applied twice is a property of the terminal's type, `PublishedRowAt` being the
result and `StackAt AnalysisBridged` the argument, so no sentence of the note
or of the framework asserts it and none needs to.

### F4. The sentences a third arm makes false

Sixteen sentences of the copy were true of a two-arm Tableau and would have
become false when stage B adds `IdealProximity`. Fifteen are the audit's own
list; its sixteenth, "There are five statements", carries a count and is in
the Counts list below, and the refreshed five-card header supplied one more. Each is now written so that
it is true today and stays true at three arms: "the two arms" becomes "the
arms", "both arms" becomes "each arm", "which of the two" becomes "which arm",
"the two certify statements" becomes "the certify statements", and "neither
arm" becomes "no arm". The sites, checked one by one against the code they
describe:

| file | what it said | what it says |
|---|---|---|
| `pgg_tableau.v`, the header on the statements | "a security witness of one of the two arms" | "a security witness of one arm" |
| the same paragraph | "Both arms speak only of a coalition below the privacy threshold" | "Each arm speaks only of a coalition below the privacy threshold" |
| the same paragraph | "claims nothing about the other ... which of the two a finished row committed to" | "claims nothing about the rest ... which arm a finished row committed to" |
| the same header, on the composition laws | "the two laws are where the mathematics of the row sits" | "those laws are where the mathematics of the row sits" |
| the index line of `security_arm_of` | "which of the two arms a published row carries" | "which arm a published row carries" |
| `SecurityPort`'s comment | "Which of the two arms an instance certifies ... the two are different statements" | "Which arm an instance certifies ... the arms are different statements" |
| `SecurityArm`'s comment | "Which of the two arms a row commits to" | "Which arm a row commits to" |
| `ab_arm`'s comment | "The two certify statements build a port" | "The certify statements build a port" |
| `certify_spectral`'s comment | "The two certify statements are the only lines" | "The certify statements are the only lines" |
| `security_arm_of`'s comment | "Which of the two arms a published row carries" | "Which arm a published row carries" |
| `certify_spectral_armE`'s comment | "the two statements are the only ones that build a port" | "the certify statements are the only ones that build a port" |
| `assumptions_report.v`, the arm block | "the two certify statements write it" | "the certify statements write it" |
| `pgl27_rows.v`, above the recorded failure | "The two arms are different statements" | "The arms are different statements" |
| `five_card_rows.v`, the header on AnalysisBridged | "one of the two arms of certify" | "an arm of certify" |
| the same header, on the two kinds of bound | "Neither arm of certify takes a bound of either kind" | "No arm of certify takes a bound of either kind" |
| `five_card_rows.v`, the biased row's docstring | "neither arm of certify takes a bound of that kind" | "no arm of certify takes a bound of that kind" |

**Counts stage B changes.** Two sentences carry a count that is the point of
the sentence, so the count is kept and the number is what stage B edits:

- `pgg_tableau.v`, the header's "There are five statements." Stage A adds no
  statement; stage B's `certify_idealproximity` makes it six.
- `pgg_tableau_syntax.v`, the count of nineteen keywords in its header. Stage
  B's `certify` rule changes it only if the rule writes a word after a slot.

`five_card_rows.v`'s "no arm of certify takes a bound of that kind" is kept
as a statement about every arm, and it stays true at three arms: the new arm
takes a variation distance to an ideal and not a conditional mutual
information.

### F5. The compile table after fix pass 3

All ten files of the probe's `_CoqProject`, in its order, one at a time
through the machine-wide lock, `rocq compile -time`, return codes from a
Python driver. Every production `.vo` was rebuilt on 2026-09-19, so every
probe `.vo` was stale and all ten were recompiled from source.

| file | rc | wall | slowest sentence |
|---|---|---|---|
| `pgg_tableau.v` | 0 | 14.1 s | 4.22 s, `Definition ab_port` |
| `pgg_tableau_syntax.v` | 0 | 5.3 s | 1.69 s, a `Require Import` line |
| `pgl27_rows.v` | 0 | 7.0 s | 2.14 s, a `Require Import` line |
| `five_card_rows.v` | 0 | 5.0 s | 1.50 s, a `Require Import` line |
| `s5_rows.v` | 0 | 4.5 s | 1.98 s, a `Require Import` line |
| `psl211_rows.v` | 0 | 5.9 s | 1.54 s, a `Require Import` line |
| `t0_sampled_branch.v` | 0 | 4.3 s | 1.93 s, a `Require Import` line |
| `t0_sampled_branch_pgl27.v` | 0 | 4.7 s | 2.31 s, a `Require Import` line |
| `g2_keyword_measure.v` | 0 | 4.1 s | 2.02 s, a `Require Import` line |
| `assumptions_report.v` | 0 | 53.2 s | 21.91 s, `Print Assumptions psl211_row_alldecks_armE` |

No sentence outside a `Print Assumptions` command reaches 5 s. The report
prints thirty-four assumption blocks, four more than after fix pass 2:
`five_card_row_repeated39_atE`, `five_card_row_uniform_branch_ideal_atE` and
the two refreshed five-card programs, `five_card_row_repeated39` and
`five_card_row_biased_inv25`. Thirty-three hold the three boolp constants
alone; `s5_row_rand_armE` holds those three and
`rigidity_s5_instance.s5_group_order_eq`, which is production's and reaches
the report through the S5 instance's own witness. The scan allows dots in a
constant name.
