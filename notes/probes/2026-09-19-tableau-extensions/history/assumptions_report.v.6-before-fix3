(* infotheo: information theory and error-correcting codes in Rocq            *)
(* Copyright (C) 2025 infotheo authors, license: LGPL-2.1-or-later            *)
(******************************************************************************)
(* The assumption floor of every statement this probe proves                  *)
(*                                                                            *)
(* Each lemma added or reproved in stage A is printed here with the constants *)
(* its proof term rests on, together with the published rows a landing would  *)
(* cite. The repository's floor is the three boolp classical constants, which *)
(* reach every statement whose type mentions an fdist record; a statement     *)
(* that closes by conversion alone is Closed.                                 *)
(*                                                                            *)
(* One target prints a fourth constant. s5_row_rand_armE rests also on        *)
(* rigidity_s5_instance.s5_group_order_eq, the order of the five-seat group,  *)
(* declared as an Axiom in instances/s5/rigidity_s5_instance.v. It            *)
(* reaches this report through the S5 instance's own witness and not through  *)
(* anything the Tableau or this probe adds: the production s5_row_rand_tableau*)
(* and s5_rand_exact_witness rest on it already.                              *)
(******************************************************************************)

From mathcomp Require Import ssreflect ssrbool eqtype ssrnat seq.
From mathcomp Require Import fintype finfun finset reals.
From infotheo Require Import fdist.
From pgg_smc Require Import pgg_analysis_status pgg_instance.
From pgg_smc Require Import pgg_analysis_manifest.
From tableau_ext_probe Require Import pgg_tableau pgg_tableau_syntax.
From tableau_ext_probe Require Import pgl27_rows five_card_rows s5_rows.
From tableau_ext_probe Require Import psl211_rows.
From tableau_ext_probe Require Import t0_sampled_branch.
From tableau_ext_probe Require Import t0_sampled_branch_pgl27.

(* The composition law of the conclude terminal, reproved at an inequality. *)
Print Assumptions port_conclude.

(* The arm readers, which are definitions and carry no proof. *)
Print Assumptions port_arm.
Print Assumptions ab_arm.
Print Assumptions security_arm_of.

(* Where a row's arm is decided: the two certify statements write it and the
   two terminals carry it through. *)
Print Assumptions certify_exact_armE.
Print Assumptions certify_spectral_armE.
Print Assumptions conclude_armE.
Print Assumptions publish_armE.

(* The arm each published row of the four instances carries. *)
Print Assumptions pgl27_row_exact_armE.
Print Assumptions pgl27_row_word_armE.
Print Assumptions pgl27_row_word39_armE.
Print Assumptions five_card_row_uniform_armE.
Print Assumptions s5_row_rand_armE.
Print Assumptions psl211_row_alldecks_armE.

(* The refutation of the terminal's obligation below the proved bound. *)
Print Assumptions pgl27_word_reprice41_false.

(* The surface and the bind build one term. *)
Print Assumptions pgl27_row_word39_bindE.

(* The three statements about the branch at Sampled. *)
Print Assumptions five_card_row_uniform_branch_rowE.
Print Assumptions five_card_row_uniform_branch_atE.
Print Assumptions five_card_row_uniform_branch_transfer_neq.

(* The same branch shape at PGL(2,7), under a spectral payload. *)
Print Assumptions pgl27_row_exact_branch_atE.
Print Assumptions pgl27_row_exact_branch_ideal_atE.
Print Assumptions pgl27_row_exact_branch_rowE.
Print Assumptions pgl27_row_word_branch_atE.
Print Assumptions pgl27_row_word_branch39_atE.
Print Assumptions pgl27_row_word_branch_armE.

(* The published rows a landing would cite, with the proof each carries. *)
Print Assumptions pgl27_row_word39.
Print Assumptions five_card_row_uniform_branch.
Print Assumptions five_card_row_uniform_branch_ideal.
Print Assumptions pgl27_row_exact_branch.
Print Assumptions pgl27_row_word_branch39.
