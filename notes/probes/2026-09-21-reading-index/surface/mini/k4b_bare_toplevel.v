(* K4(b), the keyword question, second half: the bare leading token as a
   top-level identifier in a file requiring the notation. *)

From mathcomp Require Import ssreflect ssrfun ssrbool eqtype ssrnat.
From surface_mini Require Import k4_core k4b_kind.

Set Implicit Arguments.
Unset Strict Implicit.

Definition InputDistinguishability : nat := 0.

Check InputDistinguishability.
