GO-WITH-CHANGES

Scope note: while I audited, the main session committed the landing as `35329dc` ("checkpoint before the comment audit") on top of `29a601e`. I audited `git diff 29a601e..35329dc` plus the new file. I compiled nothing and edited nothing.

Headline: the Rocq is sound. Every statement I checked says what the domain wants, the reading indexes the right conjuncts, the number bound and the exclusion lemma stand at one reading on both sides, the four direction lemmas of `pgg_tableau_reading.v` all run the right way, and the PSL(2,11) numbers (threshold six, three seats, 660, 1/660, 1/1320) check out against the tree. What fails is the prose: **three comments state the monotonicity direction backwards**, one file header states a fact the landing itself made false, one docstring sits on the wrong declaration and still describes the retired record, and one naming MUST (N5) was not carried out.

| id | class | file:line | finding | rule / evidence | exact replacement |
|---|---|---|---|---|---|
| R1 | MUST | `manifest/pgg_tableau_syntax.v:575-577` | The obstruction terminal without `of r`: "The reading is the coalition's own endpoints, the finest, so a program written this way publishes the obstruction that refutes the most." Backwards. `input_distinguishability_prop_finer` runs coarse to fine, so an obstruction at the finest reading is the weakest one and refutes certificates at that reading only. The sibling terminal's own comment ten lines below states the correct direction. | `manifest/pgg_tableau_reading.v:160-175`; soundness audit S23; the domain frame | `   for at that number. The reading is the coalition's own endpoints, the`<br>`   finest, so what a program written this way publishes refutes`<br>`   certificates at that reading and at no other. An obstruction written at`<br>`   a coarser reading refutes certificates there and at every reading it`<br>`   factors through, the endpoint reading among them. *)` |
| R2 | MUST | `instances/psl211/psl211_reading_constancy.v:793-796` | `psl211_alldecks_input_distinguishability`: "so this is the strongest of the distinguishability statements about the model and every coarser reading's obstruction implies it". The two halves contradict each other, and the first is false: a statement implied by all the others is the weakest. | same as R1; S23 asked for exactly this direction to be stated | `    two of its values. The reading is the coalition's own endpoints, the`<br>`    finest, so this is the weakest of the distinguishability statements`<br>`    about the model: an obstruction at any coarser reading implies it, and`<br>`    it implies none of them. *)` |
| R3 | MUST | `manifest/pgg_tableau_security_property_relations.v:341-343` | `coalition_reading_cst_unit`: "It is the coarsest reading of all, every reading factoring through it." Factorisation reversed. In this tree `X factors through Y` means X is computed from Y, so the constant reading factors through every reading, not the other way round. `reading_factors coalition_reading_cst_unit r f` is not provable. | `manifest/pgg_tableau_reading.v:97-111` (`reading_factors`, `reading_factors_coalition_endpoint_reading`) | `(** The reading whose value type is unit at every coalition: a coalition is`<br>`    granted one value and reads it whatever the deal. It is the coarsest`<br>`    reading of all, factoring through every reading of this algebra. *)` |
| R4 | MUST | `manifest/pgg_tableau_security_property_relations.v:346-354` | `exact_witness_cst_reading` says a program certified here says nothing about the model, but never says that `reading_of` is the coordinate separating it from a program at the endpoint reading. That sentence is the whole point of soundness finding S6 and of design ruling 5. | S6; RULINGS design ruling 5(d) | append before the closing `*)`: `nothing. A program built on this witness publishes the same manifest`<br>`    path and carries the same security property as one built at the`<br>`    coalition's own endpoints, and reading_of is the only coordinate that`<br>`    separates the two. *)` |
| R5 | MUST | `instances/psl211/psl211_reading_constancy.v:73-74` and `:1059-1060` | "The dealt parameters carry no endpoints statement, so there is no program and no path over them either." False as of this landing: `psl211_models.v` now holds `psl211_dealt_endpoints` and `psl211_dealt_observed`, and `psl211_tableau_dealt.v` holds two programs and two paths. The identical sentence was corrected in `psl211_colour_reading.v` and missed in these two places. | S14; the landing's own `psl211_models.v:357-374` and `psl211_tableau_dealt.v` | `The dealt parameters read their endpoints through profile_endpointsE, so instances/psl211/psl211_models.v carries an endpoints statement and an observed execution for them, and instances/psl211/tableau/psl211_tableau_dealt.v carries two programs and two paths over them.` (reflow to the 80-column box at :73) |
| R6 | MUST | `instances/psl211/psl211_colour_reading.v:187` | Section banner still reads "The colour view as a static reading". The retired record is gone from the code and this names it in prose. | RULINGS design ruling 1 (retire `StaticReading`) | `(*     The colour view as a reading of a coalition's endpoints                *)` |
| R7 | MUST | `instances/psl211/psl211_colour_reading.v:189-196` | The docstring above `psl211_colour_of_reading` is the OLD `psl211_colour_reading` docstring, left in place when the definition was inserted under it. It opens with the wrong declaration name, describes a three-argument reading over "the dealer-dealt execution" that no longer exists, and the declaration it now sits on takes a coalition and an endpoint map. | N40/N41; the diff shows the definition inserted above an unmoved comment | `(** psl211_colour_of_reading — the colour map on a coalition's endpoints:`<br>`    send each position of the coalition to the colour, heart or club, of the`<br>`    card that position holds, and every position outside the coalition to`<br>`    false. It is the read function of psl211_colour_reading below, and it is`<br>`    a function of the coalition as well as of the endpoint map, because a`<br>`    coalition's endpoints give card zero outside the coalition and card zero`<br>`    is a heart. *)` |
| R8 | MUST | `instances/psl211/psl211_colour_reading.v:40` | Header index entry for `psl211_colour_reading` says "as a static reading". Same retired notion; a ruling applied to a header's prose applies to its index. | RULINGS design ruling 1; TEMPLATE third addendum | `(*                                a coalition, as a reading of its endpoints  *)` |
| R9 | MUST | `instances/psl211/psl211_colour_reading.v:283-285` | `psl211_colour_indistinguishability_of_coalition_reading` says "The factorisation it consumes is psl211_colour_of_reading_obsE". It is not: the proof now passes `(fun C v => erefl)`, the factorisation holding by conversion. The cited lemma plays no part. | the proof body at `:296-302` | `    reading gives the same number at the colour reading. The factorisation`<br>`    it consumes holds by conversion, psl211_colour_of_reading being the read`<br>`    function of the colour reading itself. That map is not injective, so`<br>`    this is the data processing inequality applied at a genuine coarsening`<br>`    and not at a renaming. It transports a bound and produces none.` |
| R10 | MUST | `instances/psl211/tableau/psl211_tableau_dealt.v:40-70` | The header index omits three non-`Fail` declarations: `psl211_dealt_sampled_viewE` (:132), `psl211_dealt_number_gt0` (:279), `psl211_dealt_obstruction_pf` (:284). The template requires the two tables to index every non-`Fail` declaration. | `notes/probes/2026-09-20-tableau-directories-s5/staged/TEMPLATE.md`, "What a header must say" | add to Key results after `psl211_dealt_sampled`: `(*   psl211_dealt_sampled_viewE                                               *)` / `(*                          == the link lemma of the Sampled level, for both  *)` / `(*                             programs                                       *)`; and `(*   psl211_dealt_number_gt0  == the number is above zero                     *)`; and `(*   psl211_dealt_obstruction_pf                                              *)` / `(*                          == its proof at every field and prior             *)` |
| R11 | MUST | `instances/psl211/tableau/psl211_tableau_dealt.v:210-212` | Proof strategy in a docstring: "Each mass is pinned in a statement naming one chirality, and the two are brought together afterwards: a rewrite with a mass lemma in a goal holding both chiralities searches a goal holding both deck tables." | statement-comment rule: no proof strategy in a rendered body; the file already has the right home for it at `:240-245` | delete the paragraph from the docstring and put it as a source comment immediately after `Proof.`: `(* each mass is pinned in a statement naming one chirality, and the two are`<br>`   brought together afterwards: a rewrite with a mass lemma in a goal`<br>`   holding both chiralities searches a goal holding both deck tables *)` |
| R12 | MUST | `instances/psl211/tableau/psl211_tableau_analysis_bridged.v:477-479` | `psl211_alldecks_obstruction` gained a reading in its constructor (`:482`) and its docstring names none. | N40/N41, extended by the brief to every object that gained a reading | `(** psl211_alldecks_obstruction — the obstruction, at every real field and at`<br>`    the one index of the all-decks family: at the coalition's own endpoint`<br>`    reading the model is input distinguishable at 1/660, the reciprocal of`<br>`    the order of the shuffle group. *)` |
| R13 | MUST | `instances/psl211/tableau/psl211_tableau_analysis_bridged.v:518-525` | "What it refutes. Every number at which an input-indistinguishability program over this model states its proposition is at least 1/660, whatever the certificate ... it also rules out every certificate whose ideal cut sits within eps ...". Both universals now hold only of certificates at the endpoint reading. English stronger than the Rocq. | `indistinguishability_number_ge_of_input_distinguishability` and `no_indistinguishability_cert_ideal_close_of_input_distinguishability` both bind one reading on both sides | `    What it refutes, at the coalition's own endpoint reading. Every number at`<br>`    which an input-indistinguishability program over this model and that`<br>`    reading states its proposition is at least 1/660, whatever the`<br>`    certificate, by psl211_alldecks_indistinguishability_number_ge. Through`<br>`    the general form of the tail lemma,`<br>`    indistinguishability_prop_of_ideal_close, it also rules out every`<br>`    certificate at that reading whose ideal cut sits within eps of this`<br>`    model's own cut law once eps added to itself stays below 1/660, which is`<br>`    no_indistinguishability_cert_ideal_close_of_input_distinguishability at`<br>`    this model. A certificate at a coarser reading is bounded only through a`<br>`    factorisation, by indistinguishability_number_ge_across_readings.` |
| R14 | MUST | `instances/psl211/tableau/psl211_tableau_analysis_bridged.v:566-570` | `psl211_alldecks_published_input_distinguishability` gained `(coalition_endpoint_reading psl211_algebra)` in its statement; the docstring says "the all-decks model is input distinguishable at 1/660" with no reading. | same class as R12 | `(** psl211_alldecks_published_input_distinguishability — the program's reader`<br>`    gives the obstruction back, at every real field, and this is its second`<br>`    conjunct: at the coalition's own endpoint reading the all-decks model is`<br>`    input distinguishable at 1/660. The first conjunct is that 1/660 is above`<br>`    zero, which is what makes the second a comparison. The published value`<br>`    and this statement are one theorem. *)` |
| R15 | MUST | `instances/psl211/psl211_reading_constancy.v:730-732` | "so no program over this model publishes less" inside the `psl211_alldecks_no_small_eps_cert` docstring names no reading. The paragraph qualifies at its end, the sentence does not. | task rule: any "no program publishes less" without the reading | `    its certificate's own number publishes at least 1/660. The obligation of`<br>`    conclude bounds the published number below by cert_eps cert, so no`<br>`    program over this model whose certificate is at that reading publishes`<br>`    less.` |
| R16 | MUST | `instances/psl211/psl211_reading_constancy.v:36-38`, `:40-41`, `:53-55`, and the index entries `:137-138`, `:141-143`, `:145-147`, `:148-150` | The file header's quantitative paragraph and the index entries of the five narrowed statements state the exclusions with no reading: "no certificate carries a shuffle bound epsilon strictly below 1/1320", "every program over this model that publishes its certificate's own number publishes at least 1/660", "1/660 bounds from below what any input-indistinguishability program over this model publishes", "no certificate over the all-decks model carries a shuffle bound epsilon under 1/1320", "in particular none carries an epsilon of zero", "the all-decks model is input distinguishable at 1/660", "every number an input-indistinguishability program over that model states is at least 1/660". | S4; a ruling applied to a header's prose is applied to its index | insert `at the coalition's own endpoint reading` once at the head of the quantitative paragraph (`A certificate over the all-decks model at the coalition's own endpoint reading states its distance against the group-uniform law, ...`), and in each of the four index entries replace `over the all-decks model` / `over that model` by `over the all-decks model at the coalition's own endpoint reading` and `the all-decks model is input distinguishable at 1/660` by `at the coalition's own endpoint reading the all-decks model is input distinguishable at 1/660`, reflowed to the 80-column box |
| R17 | MUST | `manifest/pgg_tableau.v:130`; `instances/psl211/psl211_colour_reading.v:58` | Two header index lines are 79 bytes, not 80. Both were written by this landing. | TEMPLATE: box lines are exactly 80 columns with a space before the closer | `(*   CoalitionReading       == what a coalition is granted to read off its    *)` and `(*   psl211_colour_of_reading_obsE                                            *)` |
| R18 | MUST | `manifest/pgg_tableau_syntax.v:125-128` | "The surface has two kinds of word. A preposition carries one meaning throughout: ... and of what is read." False in the tree and contradicted by this same header at `:107-113`, which says `of` is also the bind's word. Instance files write the bind form directly: `;;; sample_step of pgl27_word_family`, `;;; certify_indistinguishability of pgl27_word_cert`, `;;; publish BaselineClassicalOnly of IdealFinite` at `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v:505-508`. | task F: a preposition used outside its one meaning | `(* The surface has two kinds of word. A preposition carries one meaning       *)` / `(* throughout: at a number, a size or a real field with its index, by a proof *)` / `(* or a piece of evidence, assuming an assumption status, and of what is      *)` / `(* read. The bind of manifest/pgg_tableau.v writes of before a payload and is *)` / `(* not one of these rules. A slot name names a thing, and the term that is    *)` / `(* that thing follows it, at once or after a preposition: dealt, encoded and  *)` (continue the existing list unchanged) |
| R19 | MUST | `manifest/pgg_tableau.v:985, 993, 1016, 1024, 1045, 1052` | Naming audit N5 was not carried out. `ExactPayloadOfReading`, `IndistinguishabilityPayloadOfReading`, `IdealProximityPayloadOfReading`, `exact_of_reading`, `indistinguishability_of_reading`, `idealproximity_of_reading` keep `_of_reading`, which already carries two other senses in the tree (`psl211_colour_of_reading`, a map applied to what a reading grants; `exact_independence_of_witness`, a statement derived from a form). RULINGS design ruling 3 cites N5 but answers only the statement names. | N5, class MUST, not overturned by any ruling | `ReadingExactPayload`, `ReadingIndistinguishabilityPayload`, `ReadingIdealProximityPayload`, `reading_exact_payload`, `reading_indistinguishability_payload`, `reading_idealproximity_payload`. Reading first as a qualifier, matching `ReadingIndistinguishabilityPropAt`, `ReadingExactIndependence`, `reading_factors`, `reading_indistinguishability_postprocessing`. `git grep -w` over the tracked tree: zero hits for each of the six. |
| R20 | SHOULD | `manifest/pgg_tableau_reading.v:14-15` | "Along such a map the two distance properties travel in opposite directions, and saying which is the whole content of the file." False: the file also holds `ReadingExactIndependence` and `exact_independence_of_witness`, which the header itself describes two paragraphs later. | the file's own declaration list | `(* One reading factors through another when a coalitionwise map sends what    *)` / `(* the finer grants to what the coarser grants. Along such a map the two      *)` / `(* distance properties travel in opposite directions, and saying which is     *)` / `(* most of what the file holds.                                               *)` |
| R21 | SHOULD | `manifest/pgg_tableau_reading.v:57` and `:202-203` | `indistinguishability_number_ge_across_readings` is glossed "an obstruction at a reading and a certificate at a finer one bound each other" and "bound each other". The conclusion is the single inequality `c <= c'`. Not type-honest. | the statement at `:205-213` | index: `(*                              == an obstruction at a reading bounds from    *)` / `(*                                 below the number of a certificate at a     *)` / `(*                                 finer one                                  *)`; docstring: `(* An obstruction at one reading bounds from below the number of a`<br>`   certificate at a reading the first factors through. The number bound of`<br>`   the framework is this one at a single reading. A bound across two`<br>`   readings needs the factorisation and is this lemma, not that one. *)` |
| R22 | SHOULD | `manifest/pgg_tableau.v:1462-1464` | `reading_of`: "It is the fourth coordinate of the manifest's description of a capability, the observer". The observer is the third of the four the same header lists at `:133` and that `manifest/pgg_analysis_manifest.v:134-136` fixes as `(theorem, distribution, observer, notion)`. | `manifest/pgg_analysis_manifest.v:134` | `   one index of its family. It is the observer coordinate of the manifest's`<br>`   description of a capability, which the path itself does not carry: two` |
| R23 | SHOULD | `manifest/pgg_tableau.v:414-416` and `:1741-1743` | Two mid-paragraph short lines left by an unfinished reflow: `   program's` (12 bytes) in the `IdealProximityCert` comment, and `   is free,` (11 bytes) in the `ObstructionKind` comment. The landing agent flagged the first. | comment layout | reflow both paragraphs to fill to column 78. For `IdealProximityCert` the first two lines become `(* The proximity certificate at a reading: a second sample adapter over the`<br>`   program's own execution, standing for the ideal run; an exact witness for`<br>`   that ideal at the same reading, which is what makes the ideal a model` and so on; for `ObstructionKind` join `   requires the number positive. At a number at or below zero the inequality`<br>`   is free, var_dist being non-negative, and the empty coalition is below` |
| R24 | SHOULD | `manifest/pgg_tableau.v:1745-1746` | "Positivity is what makes a published member a comparison of two readings of the model." Since this landing "reading" is the record, and here it means the two values read at two run arguments. One word, two concepts, inside one comment that the landing rewrote. | one word per concept, file-wide | `   model whose run-argument type is inhabited. Positivity is what makes a`<br>`   published member a comparison of what the reading grants at two run`<br>`   arguments. *)` |
| R25 | SHOULD | `manifest/pgg_tableau.v:1737-1740` | `ObstructionKind`: "so the two readers of a published program answer the same question of a negative result and of a positive one". "reader" already means a human reader two sentences earlier in the same comment, and elsewhere in the tree it means a coalition's view function. Three senses. | one word per concept | `   distinguishable at and the number it is distinguishable at, the reading`<br>`   standing to an obstruction as it stands to security evidence, so`<br>`   security_property_of and reading_of answer the same question of a`<br>`   negative result and of a positive one.` |
| R26 | SHOULD | `manifest/pgg_tableau.v:1979, 1988, 1998` | The three `certify_*_readingE` are proved `Proof. by []. Qed.` They are **not** in the measured dangerous shape: the goal is one `tableau_at` of a `certify`, with no `conclude`, no `publish` and no `published_at`, the same shape as the adjacent `_propertyE` lemmas, and the file measured 14.0 s against a 13.8 s baseline. The tree's stated rule for a new equation is nonetheless `exact: erefl`, and every new equation in `psl211_tableau_dealt.v` obeys it. | TEMPLATE, "Every new equation closes by `exact: erefl` and never `by []` or `done`"; memory `tableau-done-hang-and-axiom-scan` | `Proof. exact: erefl. Qed.` on all three |
| R27 | SHOULD | `instances/psl211/psl211_models.v:361` | New `psl211_dealt_endpoints` docstring: "so one equation serves both modes and neither costs a reduction of its own". `cost` is on the barred economic list. | banned vocabulary | `    content readout, so one equation serves both modes and neither needs a`<br>`    reduction of its own. *)` |
| R28 | SHOULD | `instances/psl211/tableau/psl211_tableau_dealt.v:35-37` | "The obstruction's path differs from the manifest's twelfth in its observed execution, that one being the all-decks run." It differs in two coordinates: `psl211_dealt_observed` against `PSL211Analysis.observed`, and `psl211_dealt_family` against `PSL211Analysis.exact_family`. | `manifest/pgg_analysis_manifest.v:1203-1205` against `psl211_tableau_dealt.v:318-322` | `(* among the manifest's twelve. The obstruction's path differs from the      *)` / `(* manifest's twelfth in two coordinates, its observed execution and its     *)` / `(* model family, the twelfth being over the all-decks run; a manifest path   *)` / `(* for the colour program needs its raw theorem stated below the manifest    *)` / `(* and is not written here.                                                  *)` |
| R29 | SHOULD | `instances/psl211/tableau/psl211_tableau_dealt.v:303` against `instances/psl211/tableau/psl211_tableau_analysis_bridged.v:552-556` | Two obstruction programs of one instance at one and the same reading, one writing `of (coalition_endpoint_reading psl211_algebra)` and one writing no `of` clause. A reader comparing the two lines is invited to think they differ in the reading. The parenthesis is also the only place in the surface where a slot is filled by a parenthesised application, where the colour program next to it fills the same slot with the name `psl211_colour_reading`. | task F; LANDING departure 2 keeps both rules so that both have a caller | add beside the notation block of `psl211_tableau_dealt.v`: `Definition psl211_endpoint_reading : CoalitionReading psl211_algebra := coalition_endpoint_reading psl211_algebra.` (`git grep -w psl211_endpoint_reading`: zero hits) and write `of psl211_endpoint_reading` in both obstruction programs, with an index entry and the docstring `(** psl211_endpoint_reading — the identity reading at this algebra, the finest: a coalition is granted the whole of its own endpoints. Both obstruction programs of the instance name it, so their one line says which reading the 1/660 is read at. *)` |
| R30 | SHOULD | `instances/s5/tableau/s5_tableau_analysis_bridged.v:154`; `instances/kim2025/tableau/five_card_tableau_analysis_bridged.v:499, 627, 644, 898, 991`; `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v:285, 350, 775, 845`; `instances/psl211/tableau/psl211_tableau_analysis_bridged.v:182, 293`; `manifest/pgg_tableau_security_property_relations.v:326, 362, 400, 479, 606` | Roughly twenty witness and certificate docstrings whose types gained a reading were not restated. They say "that coalition's reading", "a coalition's reading of the ideal cut", "the coalition's executed reading", which under the new vocabulary reads as the record and is now the endpoint reading in every one of them. `COMMENTS.md` records no paragraph for any of them. | RULINGS N40/N41 as accepted, and the brief's Comments section, which scopes the pass to every object that gained a reading | one sentence pattern, instantiated per site: for `pgl27_exact_witness`, `    exact sample space, and, at every coalition of fewer than four seats, the`<br>`    independence from it of the whole of that coalition's endpoints, which is`<br>`    the reading this witness is indexed by.` The same edit at the other sites, naming the reading once in the opening sentence. |
| R31 | SHOULD | `manifest/pgg_tableau.v:130-152` | The nine new index entries (`CoalitionReading`, `coalition_endpoint_reading`, `evidence_reading`, `ab_reading`, `reading_of`, `ReadingIndistinguishabilityPropAt`, the three `certify_reading_*`) are grouped at the head of the Definitions block, while their declarations sit at `:336, :354, :461, :605, :1468, :724, :1196, :1240, :1281`. The block was otherwise in file order, and the three `certify_reading_*` are now separated from the `certify_*` they twin. | header index in file order | move `evidence_reading` after `SecurityEvidence`, `ab_reading` after `StackAt`, `ReadingIndistinguishabilityPropAt` before `IndistinguishabilityPropAt`'s neighbours, `reading_of` after `security_property_of`, and each `certify_reading_X` immediately after its `certify_X`; keep `CoalitionReading` and `coalition_endpoint_reading` where they are, which is file order |
| R32 | SHOULD | `instances/psl211/psl211_colour_reading.v:39-41, 56-60` | Index out of file order in two places: `psl211_colour_reading` is listed before `psl211_colour_of_reading` where the file has `:198` then `:211`, and `psl211_colour_of_reading_obsE` is listed after `psl211_colour_reading_funE` where the file has `:222` then `:251` then `:266`. The landing agent flagged this block. | header index in file order | swap the two Definitions entries; move the `psl211_colour_of_reading_obsE` entry above the `psl211_colour_readingE` entry |
| R33 | SHOULD | `instances/psl211/tableau/psl211_tableau_dealt.v:56-70` | Key results lists `psl211_dealt_input_distinguishable` (file `:213`) before the three colour `_readingE`/`_propertyE`/`_pathE` (file `:169, :176, :188`). | header index in file order | move the `psl211_dealt_input_distinguishable` entry below `psl211_colour_exact_published_pathE` |
| R34 | SHOULD | `manifest/pgg_tableau.v:52-54, 282-287` | Three header lines describe the narrowed statements with no reading: "At some models these three fields leave no certificate whose marginal bound's epsilon, taken twice, is below a positive number", "a distinguishable model bounds from below the number an input-indistinguishability program over it publishes", "and leaves no certificate whose ideal cut is that close to its own cut law". The prose at `:107-110` does name the reading, the index does not. | a ruling applied to a header's prose is applied to its index | index: `(*                          == a model distinguishable at a reading bounds    *)` / `(*                             from below the number an                       *)` / `(*                             input-indistinguishability program at that     *)` / `(*                             reading publishes                              *)`, and `(*                          == and leaves no certificate at that reading whose *)` / `(*                             ideal cut is that close to its own cut law     *)` |
| R35 | SHOULD | `instances/psl211/psl211_colour_reading.v:307-310` | `psl211_colour_of_reading_collides`: "the identity of psl211_colour_of_reading_obsE is a factorisation in one direction only". That lemma is the seat reconciliation, not the factorisation; see R9. | the proof at `:296-302` | `    colour map is not injective and the factorisation of`<br>`    psl211_colour_indistinguishability_of_coalition_reading runs in one`<br>`    direction only.` |
| R36 | SHOULD | `instances/psl211/tableau/psl211_tableau_dealt.v:290-292` | `psl211_dealt_obstruction_published`: "it denies what its one member names". A published value does not deny anything, and what follows the colon is two consequences drawn from it, not a negation of what the member names. | type-honest phrasing; no narrative verb for a mathematical relation | `(** psl211_dealt_obstruction_published — the obstruction program. It`<br>`    certifies no security property, its data carrying no SecurityEvidence,`<br>`    and two exclusions follow from its one member: every number at which an`<br>`    input-indistinguishability program over this model AT THIS READING states` |
| R37 | NOTE | `instances/psl211/psl211_colour_reading.v:198` | `psl211_colour_of_reading` now takes an endpoint map, not a reading, so the `_of_reading` fragment names its argument wrongly. `psl211_colour_of_endpoints` would say what it is, but the name is cited at five sites in the file and once in the index, so the churn may not be worth it. No change proposed. | N5's own reading of the two other senses | — |
| R38 | NOTE | `instances/psl211/tableau/psl211_tableau_dealt.v:202-203` | "the two run arguments are the two chiralities of one deal". At the dealer-dealt parameters the run argument is the chirality alone and no deal is held fixed; the phrasing is inherited from the all-decks statement, where `(true, psl211_perdeck_deal)` and `(false, psl211_perdeck_deal)` genuinely share a deal. | `psl211_dealt_input_distinguishable` exhibits `true` and `false` | suggested: `    two run arguments are the two chiralities, the dealer laying the cards`<br>`    from each` |
| R39 | NOTE | `manifest/pgg_tableau.v:453-454`; `manifest/pgg_tableau_reading.v:220-222` | "the product of the two marginals the ideal model has, its own reading and its own secret" and "the coalition's reading through r of the model's own run argument and cut" use "reading" for the value read rather than for the record. Two senses, both in comments the landing rewrote. | one word per concept | suggested: "what the reading grants the ideal model" and "what r grants the coalition of the model's own run argument and cut" |
| R40 | NOTE | `manifest/pgg_tableau.v:140`; `:717` | "the input-indistinguishability proposition at a reading, at no certificate" and "It is stated at a reading and at no certificate". `at` is the surface's word for a number, a size or a real field, and here it is stretched to a negative existential. | one preposition per meaning | suggested: "the input-indistinguishability proposition of a reading, with no certificate in it" |

### Answers to the questions the table does not settle

**A. Statements.** (1) `ExactProp` puts `cr_read r C` on all four conjuncts, the fourth quantifying `h : cr_readT r C -> W`, so post-processing ranges over functions of the reading and not of the endpoints. `IdealProximityPropAt` reads both models through the one `r`, and `ipc_witness : ExactWitness ipc_ideal r` puts the ideal's witness at the same reading. Both correct. (2) `indistinguishability_number_ge_of_input_distinguishability` and `no_indistinguishability_cert_ideal_close_of_input_distinguishability` bind one `r` for the obstruction and for the certificate. Correct. (3) All four factorisation facts run the right way: `reading_factors r r' f` makes `r'` computed from `r`; `reading_factors_coalition_endpoint_reading` puts the endpoint reading first, so every reading factors through it; post-processing runs finer to coarser; `input_distinguishability_prop_finer` and its corollary run coarser to finer; `indistinguishability_number_ge_across_readings` takes the obstruction at the coarser side and the certificate at the finer. Correct. (4) The three `certify_*_readingE` are quantified over the real field and the index, as ruling 5(c) asks, and are proved `by []`; see R26 for the shape verdict. (5) See R3 and R4. (6) The new file's header states the pair as two different properties at two different readings, the chirality as the run argument with the non-generalisation, threshold six, three named positions, 1/660 and 1/1320, and that neither path is among the manifest's twelve. The numbers hold: `profile_k_psl211_algebra` fixes six, `psl211_perdeck_coalition` is the three seats 0, 1 and 2, `psl211_card` fixes the shuffle group at 660, and 1/1320 is half of 1/660.

**B. Narrowed statements.** All five in `psl211_reading_constancy.v` name the reading and say what stays open. The sixth statement to gain one, `psl211_alldecks_input_distinguishability`, names it and states the direction backwards (R2). `pgl27_word_input_distinguishability_false` names the reading and cites `input_distinguishability_prop_coalition_endpoint_reading` correctly, and its "covers every reading" claim is sound.

**C. Names.** The record, its two fields, the default, the three `certify_reading_*` and `ReadingIndistinguishabilityPropAt` all follow the rulings. `evidence_reading`, `ab_reading`, `reading_of`, `reading_factors`, `psl211_dealt_*`, `psl211_colour_exact_*`, `psl211_dealt_number`, `psl211_colour_of_reading_obsE`, `coalition_reading_cst_unit`, `exact_witness_cst_reading` and `input_distinguishability_obstruction` all say what the thing is. N5 is outstanding (R19). No identifier from the retired vocabulary survives anywhere in the tracked tree outside `notes/`: `git grep -n -E "StaticReading|sr_read|sr_readT|static_coalition_reading|EndpointReading|er_of_endpoints|efr_read"` returns nothing. In prose, three sites still name the retired notion (R6, R7, R8). The other tree-wide hits for "static endpoint reading" are `static_coalition_obs`, which is a live object and not the retired record, so N18's collision is moot.

**D. Comments.** Every code block in `landing2/COMMENTS.md` appears in its named file, the sixteen that did not match verbatim being partial quotes marked with an ellipsis. The record is faithful. What it does not cover is R12, R13, R14 and R30, none of which appears in it. No status, history, plan id, probe token, date or timing survives in the changed comments. No barred word appears in them except R27.

**E. Headers and layout.** Two box lines are 79 bytes (R17). No line exceeds 80 bytes except the five notation strings of `pgg_tableau_syntax.v`, which are unbreakable. No `**)` at 80. Index completeness: `psl211_tableau_dealt.v` omits three (R10), `pgg_tableau_syntax.v`, `pgg_tableau_reading.v`, `pgg_tableau_security_property_relations.v` and `psl211_colour_reading.v` are complete, ordering defects at R31, R32, R33. `pgg_tableau_reading.v`'s header describes the file as it now is and carries no trace of the retired record, one false universal aside (R20). The syntax header describes `of` as what is read, where it sits, that it reserves nothing and that the count stays twenty, says the five-clause rule is at the default reading, and describes both obstruction terminals; its arithmetic checks out, four of eight certify rules putting `by` after the property's name, one being the five-clause rule and three naming a reading. `psl211_tableau_dealt.v` follows the template and its siblings in licence lines, banner widths, coqdoc form, `Local Open Scope` block and the two index tables, the omissions of R10 aside.

**F. The surface.** The colour program reads cleanly: `certify ExactIndependence of psl211_colour_reading by psl211_colour_exact_witness |> publish StaticExecutedOnly assuming BaselineClassicalOnly`, with `of` the reading, `by` the evidence and `assuming` the assumption status. The dealt obstruction program reads `publish Obstruction InputDistinguishability of (coalition_endpoint_reading psl211_algebra) at psl211_dealt_number by psl211_dealt_obstruction_pf assuming BaselineClassicalOnly`, each preposition in its one meaning and no clause reading as an application. The `of (...)` is readable but is the only parenthesised slot in the surface (R29). The all-decks obstruction program names no reading, which is permitted but is a defect of the pair, both being at the same reading (R29). The one place a preposition is used outside its meaning is the header's own claim about `of` (R18).

**G. Carried-out check.** N1 DONE (`CoalitionReading`). N2 DONE (`cr_read`). N3 DONE (`ReadingIndistinguishabilityPropAt`). N4 DONE (one free-reading proposition, the other two bodies inlined). **N5 NOT DONE** (R19). N7 DONE (`psl211_dealt_sampled`). N18 MOOT (the record is not named `EndpointReading`). N19 DONE (the header, `CoalitionReading`, `evidence_reading`, `ab_reading` and `reading_of` all say the observer coordinate holds a reading, and no new text uses "observer" for the party). N20 DONE at commit 1 (`at R idx by b`). N21 MOOT by the owner's inline obstruction. N23 MOOT. N27 DONE (twenty, and `Obstruction` stays an identifier after the literal `publish`). N28 DONE (the header gives both reserved positions). N29 DONE (three slot positions and one literal position for `assuming`). N40 DONE for the nine sites in `pgg_tableau.v`, **NOT DONE** for the instance sites (R12, R14, R30). S1 DONE (the relations file migrated with the constant-reading record). S2 DONE in production: `indistinguishability_number_ge_across_readings` exists and the framework comment points at it, and nothing in the changed files repeats "holds by typing". S3 DONE (`input_distinguishability_prop_finer`, `reading_factors_coalition_endpoint_reading`, the corollary). S4 DONE. S5 DONE. S6 DONE in form (three framework `_readingE` and `psl211_colour_exact_published_readingE`, all quantified over the field and the index) and **NOT DONE** in the one sentence ruling 5 asked for on the vacuity record (R4).

**H. Other.** Nothing false, vacuous or stronger in English than in Rocq beyond the rows above. One thing I could not check as a read-only auditor: the `Print Assumptions` results and the 48.6 s fidelity run reported in `LANDING.md`. I verified statically that `landing_fidelity.v` contains the 38 `Check`s, the K5 verbatim copies, the four default-reading equations, `fidelity_number_bound_nondegenerate` and the seven `Timeout 300 Print Assumptions`, so the evidence S9 and S20 asked for is present in the file. I did not run it.
