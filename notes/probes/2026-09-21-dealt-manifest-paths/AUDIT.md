GO-WITH-CHANGES

Probe audited read-only at HEAD 108d869. Nothing compiled, nothing edited, no Rocq process started. Independent mechanical checks I ran: a per-file `diff` of all nine staged files against production, a scripted declaration-by-declaration text comparison of all 25 moved declarations (21 byte-identical statement+proof+comment, 4 differing exactly as the ledger discloses plus one it does not), a repo-wide `git grep -w` on every new name, an 80-column box scan of all seven touched files, a barred-word scan, and an independent run of `scripts/comment_pass/closure.py` over the six changed production files.

**Soundness core holds.** (Q1) Neither raw theorem smuggles anything: the new file has no `Section`, no `Variable`, no `Hypothesis`, no `Axiom`, no `Admitted`, no `Abort`. `psl211_dealt_colour_indep` is the literal unfolding of `ReadingExactIndependence` (`manifest/pgg_tableau_reading.v:230-237`) with `psl211_colour_of_reading C` where `cr_read psl211_colour_reading C` stands, and the reading record's read field IS `psl211_colour_of_reading`, so the Tableau lemma closes by `exact:` alone. `psl211_dealt_perdeck_reading_ge` is stated at `sa_cut_dist (psl211_dealt_sample secretP)`, exactly the distribution `InputDistinguishabilityPropAt` pushes the reading forward along (`manifest/pgg_tableau.v:1646-1656`), and it absorbs the `psl211_dealt_sample_cut_distE` rewrite that used to sit in the Tableau proof. The single `Local Opaque psl211_dealt_raw_count` is placed after `psl211_dealt_raw_countE`, which publishes both values, so it hides no number a reader needs. (Q6) Departure 1 is correct: every other `_pathE` in the repo already equates the published path with the NAMED typed manifest path (`instances/psl211/tableau/psl211_tableau_analysis_bridged.v:568-571`, `:221-223`, `:424-426`, and four in `five_card_tableau_analysis_bridged.v`); the spelled `MkAnalysisPath` right-hand side was the outlier. (Q2) Move is faithful. M8 reproduced independently: `touched 6, closure 34`, `frozen files met: none`. Axioms: the classical trio and nothing else, four times, in `logs/fidelity.log`.

**What must change is the manifest prose and three stale sentences.** The largest defect is D1/D2: Path 13's capability line and its level justification both name the CUT law as the distribution of `dealt_colour_indep`, but that theorem is stated at the adapter's joint sample law. An independence cannot be read at the cut law, because `dealt_secret` is not a function of the cut. Path 9 gets this right for the twin line (`prior R, the same distribution`).

| id | class | where (staged tree) | finding | evidence | exact replacement |
|---|---|---|---|---|---|
| D1 | MUST | `manifest/pgg_analysis_manifest.v:985-986`, same text in `LEDGER.md:204-206` | Path 13's capability line gives `dealt_colour_indep` the distribution "the cut law of dealt_sample". False. The theorem is at `sa_sampleP (psl211_dealt_sample secretP)`, the joint law of the chirality prior and the cut. `dealt_secret` is a random variable of that joint law and is constant on no cut fibre, so the statement does not exist at the cut law. Path 9's twin line names the joint law (`prior R`). | `staged/instances/psl211/psl211_dealt_model.v:463-471` (the theorem's first line is `sa_sampleP (psl211_dealt_sample secretP)`), `:159-164` (the adapter's law field is `psl211P secretP`), `:167-169` (`psl211_dealt_sample_lawE`), `instances/psl211/psl211_secrecy.v:230` (`psl211P := secretP \`x (\`U psl211_G_pos)`), production `manifest/pgg_analysis_manifest.v:686-689` (Path 9's line) | distribution cell becomes: `the sample law of dealt_sample, which is dealt_prior secretP, the chirality drawn from secretP and the cut drawn uniformly over the 660 elements of the shuffle group, the two independent` (reflow into the 80-column box, continuation lines indented as the neighbouring capability lines are) |
| D2 | MUST | `manifest/pgg_analysis_manifest.v:997-999` | Path 13's level justification reads "dealt_colour_indep is a privacy theorem stated at that cut distribution and at the colour observer read off that model's own coalition reader, giving AnalysisBridged". Same false antecedent as D1. The preceding clause about `dealt_cut_distE` and Sampled stays true and stays. | same as D1 | replace that clause with: `dealt_colour_indep is a privacy theorem stated at sa_sampleP (dealt_sample secretP), the model's own sample law, and at the colour observer read off that model's own coalition reader, so the theorem, the distribution and the observer are this path's own, giving AnalysisBridged.` |
| D3 | MUST | `manifest/pgg_analysis_manifest.v:1006-1010` | "at the card-identity observer dealt_static_view the same independence is false over this model" is a false universal. `psl211_dealt_reading_indep_false` carries two premises, `secretP true != 0` and `secretP false != 0`. At a prior supported on one chirality the secret is almost surely constant and the independence HOLDS at that observer, and the path's model is a family indexed by the prior. | `staged/instances/psl211/psl211_colour_reading.v:291-296`; the same premises are pinned in `fidelity.v:161-166` | replace the clause with: `at the card-identity observer dealt_static_view the same independence fails at every prior giving mass to both chiralities, by psl211_dealt_reading_indep_false of instances/psl211/psl211_colour_reading.v, and that is the theorem Path 14 below records in its distributional form.` |
| D4 | MUST | `manifest/pgg_analysis_manifest.v:1011-1014` | "at the six seats of the mirror representative independence at the colour observer already fails, by psl211_colour_reading_dep_k6" drops the same two premises. This is the sentence spec invariant 3.4 requires, so it must be the one that is exactly true. | `staged/instances/psl211/psl211_colour_reading.v:228-232` (`secretP true != 0 -> secretP false != 0 -> ...`), `fidelity.v:135-145` | replace with: `at the six seats of the mirror representative independence at the colour observer already fails at every prior giving mass to both chiralities, by psl211_colour_reading_dep_k6 of the same file, and six is what the derived profile declares.` |
| D5 | MUST | `manifest/pgg_analysis_manifest.v:1082-1086` | Two faults in one sentence. (a) "the limitation is about this model's privacy" unqualified contradicts Path 13, which proves exact privacy over the same model at the colour observer. (b) "at Path 12, whose run argument is a deck description, the same shape of theorem says nothing about a secret" is false: Path 12's theorem compares `(true, psl211_perdeck_deal)` and `(false, psl211_perdeck_deal)`, which differ exactly in `psl211_alldecks_secret`, that being `u.1.1`. | `instances/psl211/psl211_models.v:209-210` (`psl211_alldecks_secret := fun u => u.1.1`), `instances/psl211/psl211_alldecks_input_distinguishability.v:157-165` (the two run arguments) | replace from "so the limitation is" to the end of that sentence with: `so the limitation is about this model's privacy at the card-identity observer and not about two inputs alone. At Path 12 the two run arguments differ in the secret coordinate of a deck description and agree in its other three, so the limitation there is at one fixed deal.` |
| D6 | MUST | `instances/psl211/psl211_reading_constancy.v:73-74` and `:906-908` | Both sites still say the dealer-dealt sample adapter `psl211_dealt_sample` is "of instances/psl211/psl211_colour_reading.v". After the move it is in `psl211_dealt_model.v`. Only the file name is wrong; the rest of both sentences stays true. | the staged diff moves `psl211_dealt_sample` out of `psl211_colour_reading.v` (removed at production `:130-136`), into `psl211_dealt_model.v:159` | at both sites, `instances/psl211/psl211_colour_reading.v` becomes `instances/psl211/psl211_dealt_model.v` and the paragraph is rewrapped to its own comment style and width. Nothing else in either sentence changes. |
| D7 | MUST | `instances/psl211/psl211_colour_reading.v:11-16` | The opening paragraph still says "this file supplies both over that law: the adapter psl211_dealt_sample ... and the colour reading". After the move the file supplies only the reading, and the fourth paragraph of the same header (`:36-43`) says so, so the header contradicts itself. | `staged/instances/psl211/psl211_colour_reading.v:36-43` against `:11-16`; the adapter is at `psl211_dealt_model.v:159` | replace from "A proposition of the Tableau" to the end of that paragraph with: `A proposition of the Tableau is stated at a sample adapter and at a reading. The adapter over that law is psl211_dealt_sample of instances/psl211/psl211_dealt_model.v, over the dealer-dealt run parameters, whose run argument carrier is the chirality itself. What this file supplies is the reading: the colour reading, which gives each position of a coalition the colour of the card the encoder deck of that chirality puts at the cut image of that position.` (rewrap to the 80-column box) |
| D8 | MUST | `instances/psl211/psl211_analysis.v:419-422` and `manifest/pgg_analysis_manifest.v:2399` | `dealt_observed_recovers` is the correctness theorem BOTH new paths name, and it is pinned by no spelled-type Check in either checker, while the all-decks twin is pinned in both (`psl211_analysis.v` section 5 and manifest `--- 5 Correctness ---`). The introducing comment "one per section that has one" is therefore false, and it is doubly false because sections 3, 4 and 6 get more than one. The alias is also the one alias whose body is a record-field projection, so without the pin a reader cannot read the proposition at this instance's carriers at all. | production `instances/psl211/psl211_analysis.v:295-302` and `manifest/pgg_analysis_manifest.v:2145-2151` pin `observed_recovers`; the staged blocks pin `dealt_exec_plug`, `dealt_observed`, `dealt_static_view`, `dealt_colour_of_reading`, `dealt_secret`, `dealt_sample`, `dealt_family`, `dealt_cut_distE`, `dealt_colour_indep`, `dealt_perdeck_reading_ge` and nothing for section 5 | comment becomes: `The dealer-dealt aliases at their spelled types. The execution and the model keep their dependent indices, recovery keeps its group-membership hypothesis, and the two security aliases are pinned at the observers they are stated at, the colour map for the first and the framework's static reader for the second.` Add the Check in the block below the table, in both files. |
| D9 | SHOULD | `instances/psl211/psl211_dealt_model.v:27` | The Definitions index lists `psl211_colour_of_reading` third, between `psl211_dealt_family` and `psl211_dealt_decktbl`, but it is the LAST definition in the file. Every index in the instance cone is in file order, including the one this file's donor keeps (`psl211_colour_reading.v:44-49` after the removals). | definitions in file order are at `:159`, `:207`, `:224`, `:251`, `:257`, `:283`, `:326`, `:379` | move the line `(*   psl211_colour_of_reading  == the colour map on a coalition's endpoints   *)` to sit immediately after the `psl211_dealt_fiber` entry, as the last Definitions row. |
| D10 | SHOULD | `instances/psl211/psl211_dealt_model.v:34-50` | Key results omits `psl211_dealt_colour_viewE`, which is the file's one export to the layer above: both `psl211_colour_readingE` and `psl211_colour_reading_funE` are now proved from it by `exact:`. | `:436-447` defines it, `staged/instances/psl211/psl211_colour_reading.v:140` and `:153` consume it | insert after the `psl211_dealt_massE` entry, in file order: `(*   psl211_dealt_colour_viewE == the model's colour view of a coalition is   *)` / `(*                                the colour map of that coalition's          *)` / `(*                                endpoints                                   *)` |
| D11 | SHOULD | `instances/psl211/psl211_dealt_model.v:4-6` | The title calls the file's subject "the fixed-dealer colour model", but half the file, the two encoder decks, the fibre counts and `psl211_dealt_perdeck_reading_ge`, is at the card-identity observer and not the colour one. The file is named for the model and the title names it for one observer. (Leave the phrase where it is inside `psl211_dealt_sample`'s own docstring, which the move copied verbatim.) | `:214-365` is card-identity material, `:368-447` is the colour map, `:491-500` is at `static_coalition_obs` | title lines become: `(* psl211_dealt_model: the dealer-dealt model of the twelve-card chirality    *)` / `(*                     instance, and the two theorems the analysis manifest's *)` / `(*                     paths over it name                                     *)` |
| D12 | SHOULD | `instances/psl211/psl211_analysis.v:27` | New header box line is 79 characters where every other line of every box in the file is 80, so the right rule breaks at that one line. | the scan flagged exactly this one box line in the seven touched files | `(* Two dealers are aliased here and their aliases are kept apart by name.     *)` (one more space before the closing marker) |
| D13 | SHOULD | `instances/psl211/psl211_colour_reading.v:140` | New proof line is 81 characters, the only line over 80 in the seven touched files. | the scan flagged exactly this one line | split onto three lines, in the block below the table. |
| D14 | SHOULD | `manifest/pgg_analysis_manifest.v:975-978` and `:1112` | Path 13's observers cell omits `dealt_secret`, the random variable the independence is OF, and `dealt_secret` is instead filed under "Aliases carrying no capability yet". Path 9 names `secret` in its observers cell and does NOT file it there. A reader auditing Path 13 by its table therefore cannot see what the colour observer is independent of. `dealt_prior` may stay filed there, matching how `prior` is filed. | production `manifest/pgg_analysis_manifest.v:659-661` (Path 9 observers) against `:1108-1111` (PSL211 no-capability row, `secret` absent) | add to Path 13's observers cell, after the `dealt_static_view` entry: `PSL211Analysis.dealt_secret : bool, the chirality the dealer was given, a random variable on dealt_prior at the path's index` and delete `dealt_secret` from the PSL211Analysis row of the no-capability table |
| D15 | NOTE | `instances/psl211/psl211_analysis.v:111` | `dealt_secret` is the only new name with a collision: `dealt_secret` already exists at the framework level and `psl211_analysis.v` Imports the file that defines it, so inside `Module PSL211Analysis` the alias shadows the framework constant. It compiles and the alias IS that framework constant at this instance's arguments (`psl211_secrecy.v:234`), so the shadowing is harmless, but no other facade alias shadows a framework name. Every other new name is collision-free by `git grep -w`. | `reconstruct/design_privacy.v`, `reconstruct/transitivity_privacy.v`, `reconstruct/coalition_view_transport.v` define or use `dealt_secret`; `psl211_analysis.v` Imports `design_privacy` | — |
| D16 | NOTE | `LEDGER.md:36-38` | The edit list is one item short. `psl211_dealt_raw_countE` also LOST a three-line source comment about `vm_compute` reading the closure table through a seal. The deletion is CORRECT, because the new file issues no `Local Opaque psl211_elem_table` before that lemma, so the condition the comment asserts no longer exists. The ledger should say so rather than claim only the notation rename and two sentences changed. | production `instances/psl211/psl211_reading_constancy.v:452` and `:975-978` against `staged/instances/psl211/psl211_dealt_model.v:287-291` | — |
| D17 | NOTE | `instances/psl211/psl211_dealt_model.v:139`, `:167`, `:173`, `:179`, `:418` | Five moved declarations have no user anywhere in the repo or the staged tree: `psl211_dealt_inputTE`, `psl211_dealt_sample_lawE`, `psl211_dealt_sample_argE`, `psl211_dealt_sample_cutE`, `psl211_colour_of_reading_collides`. All five were already unused in production, so the move changes nothing, but under the keep-only-what-is-claimed rule the landing is the moment to decide. The first four are the adapter's coordinate equations that spec M1 names and that make the adapter legible. The fifth is the file's recorded reason why the post-processing law runs in one direction only, so it reads as a premise of a statement above. My reading is that all five stay. | `git grep -w` on each returns only the declaration and its index entry | — |
| D18 | NOTE | `fidelity.v` | M7 coverage is narrower than the move. The 35 statement checks cover `psl211_colour_reading.v` and `psl211_tableau_dealt.v` only. The thirteen declarations moved out of `psl211_reading_constancy.v` are pinned by no Check. I closed that gap by text comparison instead: all thirteen are byte-identical in statement, proof and comment. | my scripted comparison: 21 of 25 declarations byte-identical, the other four differing only in the `seats`/`cards` to `seatT`/`cardT` rename and the two disclosed comment rewrites plus D16 | — |
| D19 | NOTE | `manifest/pgg_analysis_manifest.v:2399` | The dealer-dealt Check block is a flat list appended under the `--- 7 Transfer ---` heading, where the four facade blocks are each filed under `--- 1 Program ---` through `--- 7 Transfer ---`. A reader scanning by heading finds execution, observer and model pins filed under Transfer. | production `:1390-1638`, `:1663-1928`, `:2035-2221`, `:2262-2389` | — |
| D20 | NOTE | `LEDGER.md:28` | M8's evidence is not in the probe tree: `closure.py` lives in `scripts/comment_pass/` and no closure log was saved. I reran it and it reproduces: `touched 6, closure 34`, `frozen files met: none`. The ledger's `touched 7` counts the new file, which the script does not. | my run of `python3 scripts/comment_pass/closure.py` over the six changed production files | — |
| D21 | NOTE | `instances/psl211/psl211_analysis.v:145` | `dealt_observed_recovers := OE.oe_run_recovers psl211_dealt_observed` is a record-field projection. Every other facade correctness alias in the repo names an instance-level theorem, and the all-decks twin `psl211_alldecks_observed_recovers` exists precisely so the facade can alias one. Adding a `psl211_dealt_observed_recovers` twin to `psl211_models.v` beside it would make the two halves of this facade the same shape. This is an alternative to D8, not a substitute: the pin is required either way. | `instances/psl211/psl211_models.v:381-386`, and `s5_analysis.v:233`, `pgl27_analysis.v:243`, `five_card_analysis.v:300` | — |

Replacement blocks the table points at.

D8, to be added under the corrected introducing comment in `instances/psl211/psl211_analysis.v` and again in the manifest's dealer-dealt block:

```
(* 5 Correctness: recovery keeps its group-membership hypothesis and returns
   the chirality the dealer was given. *)
Timeout 60 Check (PSL211Analysis.dealt_observed_recovers :
  forall (x : bool) (w0 : pgg_gT (mp_M PSL211Analysis.profile)),
    w0 \in pgg_G (mp_M PSL211Analysis.profile) ->
    exec_decode PSL211Analysis.dealt_exec_plug
      (OE.oe_endpoints_size PSL211Analysis.dealt_observed x w0) = x).
```

I wrote that right-hand side as `x` by reading `OE.oe_run_recovers` (`protocol/pgg_observed_execution.v:158-165`), whose conclusion is `= oe_expected oe x`, against the all-decks twin, which spells the same field as `x.1` because its run argument is a pair (`instances/psl211/psl211_models.v:381-385`). At the dealer-dealt parameters the run argument is the bare chirality, so `oe_expected` should reduce to `x`. I could not compile, so the lander should let the Timeout-guarded Check arbitrate: if the right-hand side is not `x`, write what `oe_expected psl211_dealt_observed` does reduce to rather than drop the pin.

D13, replacing the 81-character line at `instances/psl211/psl211_colour_reading.v:140`:

```
Proof.
exact: (congr1 (fun f => f u) (psl211_dealt_colour_viewE secretP C)).
Qed.
```

Answers to the remaining questions. (Q3) Every other Path 13 and Path 14 cell checks out against the statements. Both "missing premise" cells are filled as the manifest's own duty section requires for StaticExecutedOnly and NegativeTransfer (`:1118-1126`). Path 14's addition of "at this observer" to the Path 12 wording is a genuine narrowing and is right, since `InputDistinguishabilityPropAt` is monotone from coarser readings to finer and the colour reading is the coarser one. NegativeTransfer is quoted verbatim from `manifest/pgg_analysis_status.v:68-69`. The notion labels are both in the closed list at `manifest/pgg_analysis_manifest.v:69-71` and both are the narrowest the theorems support. "exact privacy" is attached to `dealt_colour_of_reading` and to nothing else, and Path 13 says in prose that the same independence fails at the card-identity observer, so the hazard the spec names is handled, subject to D3 and D4 restoring the premise. Assumption status confirmed by the four `Print Assumptions` blocks in `logs/fidelity.log`. (Q4) The two transfer-status aliases are not a new shape: `s5_analysis.v` carries three and `five_card_analysis.v` three. No `dealt_` alias can be mistaken for an all-decks one, no name is shared, and the facade header says which prefix means which dealer. Exporting `variation_dist` follows the facade's own stated contract, that it exports the type vocabulary an alias type is written in, and `dealt_perdeck_reading_ge`'s spelled type is written in `var_dist`. Path 12's alias was checked the same way through the same export in the production file. (Q5) All names clear except D15. `psl211_dealt_model.v` beside `psl211_word_model.v` holds. `psl211_dealt_perdeck_reading_ge` is the exact twin of `psl211_alldecks_perdeck_reading_ge` in shape and in wording. `psl211_dealt_colour_path` and `psl211_dealt_obstruction_path` inherit the plain-versus-obstruction asymmetry of the existing `psl211_alldecks_path` and `psl211_alldecks_obstruction_path` pair, which I take as precedent rather than drift. Nothing abbreviates indistinguishability. (Q7) The barred-word scan is clean: the only hits are pre-existing uses of "row" for an interpreter trace row and for a Steiner system row, neither of which is a program or a path, and there are no status or history words in any new comment. `psl211_reading_constancy.v`'s own index never listed the thirteen declarations it loses, so the removals leave no stale index entry there, and `psl211_colour_reading.v`'s index after the removals is complete and in file order. The new file's index needs D9 and D10. (Q8) Worth landing: all of it, with the changes above. The two theorems the manifest wants are provable below it with their existing proofs, the chain has no cycle, and the dependency added is one file below the manifest with nothing above it gaining a Require. (Q9) The four things stronger in English than in Rocq are D1, D3, D4 and D5.
