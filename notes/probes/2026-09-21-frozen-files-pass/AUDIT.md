GO-WITH-CHANGES

Audit done entirely by me (Opus, this session). No subagent was used, so nothing here rests on unaudited output. Read-only: nothing was edited, nothing compiled, no Rocq process started.

## Summary of the substantive audit (what I could NOT fault)

- **Current text**: all 38 Part A "current paragraph" quotations are byte-for-byte what the files hold at the stated line ranges. Zero mismatches.
- **Completeness**: my own whole-word, case-insensitive scan over the comments of the 35 files of `FROZEN_MODULES` (via `common.split`) for 46 economic words plus arm/port/row/gate*/posit*/apex/indist reproduces the sheet exactly: arm 1, budget 16, buys 2, cash 1, cheaper 2, cost 3, costly 1, costs 2, overhead 1, owed 2, owes 6, owing 2, paid 2, price 7, spends 2, spent 4, worth 1, row+rows 72. Every one maps to a Part A row or a Part A0 entry. A separate scan for the norm abbreviation over comments AND code of the 35 returns exactly A30, A31, B1, B2, B3 and nothing else. **Part A is complete.**
- **Measured-time precedent VERIFIED** in `cca5e24`: every surviving "cost"/"costs" in a non-frozen `.v` after that commit carries a measured number or duration (`five_card_tableau_analysis_bridged.v:884` "costs 147 s here", `pgl27_tableau_checks.v:131,134,135`, `s5_mixing.v:211` "costs minutes to typecheck", `psl211_alldecks_input_distinguishability.v:175` "costs 228 s"). A2's kept "cost 568 s" is correct under it.
- **Machine check of every replacement**: no `"`, no `(*`, no `*)` in any of the 77 fenced blocks of the sheet; the only barred token inside any replacement is A2's kept "cost". So the real lexer risk (odd `"` inside a Rocq comment) is absent.
- Declarations read and replacements checked true: A6 (`pgl27_fuel := 220` at `pgl27_run.v:50` ✓), A10 (witness is `psl211_exact_witness`, carried by the two published programs of `psl211_tableau_analysis_bridged.v:215,242`; no manifest record holds it ✓), A11 (`psl211_word_mixing` at `psl211_mixing.v:545` is `var_dist … <= 2^-40`, a bound, so "the one psl211_word_mixing bounds by 2^-40" is type-honest ✓), A12 (word-for-word twin of `pgl27_profile.v:99` ✓), A13 (`psl211_point_uniform` is proved from `psl211_2transitive`; threshold from `psl211_count_okT` ✓), A16, A17, A21 (`instance_exec = params_exec (ex_commits E) (ex_content E) (ex_fuel E)` → readout / process list / fuel ✓), A25, A31 (`sw_bound : var_dist … <= sw_bound_eps` ✓), A33/A34 (`ts_T - ts_k <= 2 * cd_genus` ✓; both also remove "cap" as a noun for a bound), A35/A36 (`var_dist_uniform_supp` is an equality `= 2k/|A|`; the proof of `collusion_bound_conditional` at `pgg_collusion_bound.v:478` reads `2T'/N` off it after `var_dist_triangle`, so both labels are right ✓), A37 (`solve_L_aux`'s own argument is named `fuel` ✓).
- **Writer's item 1 confirmed**: `pgl27_profile.v:78-79` is a two-line `pgl27_se_exact` docstring with no such sentence and `profile_k_pgl27` at 120-121 says nothing about transitivity. A11 and A13 are genuinely fresh, and both read true.
- **B tables complete**: my own `grep -E "\bL1\b|\bL2\b|\bl1\b|\bl2\b"` over the six named files returns exactly the sheet's line sets. `git grep -n -E "\(L1\s*:=|\(L2\s*:=|\(l1\s*:=|\(l2\s*:=" -- '*.v'` returns **zero**, so no call site passes those implicits by name; the `Arguments` line of B1 and its four positional use sites (874, 910, 1025, 1031) are the whole story.

## Findings

| id | class | sheet row | finding | evidence | exact replacement / name |
|---|---|---|---|---|---|
| F1 | MUST | B8 | The sheet misses a mention site. The header index entry names the family and would name three lemmas that do not exist after the rename. `git grep -w s5_nt_L1` cannot see it. | `instances/s5/pgg_raag_s5.v:24` = `(*   s5_nt_L* == vm_compute trace counts …*)`, its own `box-index` paragraph, ent=3 eq=17 | hand-lay at the line's own column, 80 bytes: `(*   s5_n_traces_natB* == vm_compute trace counts                             *)` |
| F2 | MUST | A32 | The paragraph is **192-208**, not 192-195. Re-laying it out merges the three-item citation list and the quoted `"any T - k missing shares can be tolerated"` clause into flowing text; I ran the relayout and it destroys 17 lines. | `common.paragraphs("reconstruct/algebraic_rigidity.v")` → one `plain` paragraph 192-208 | do NOT reflow. Replace line 194 in place, leaving 192, 193, 195 byte-identical: `   a dropout-tolerance allowance. Reconstruction in every concrete` (63 → 66 bytes) |
| F3 | MUST | A30 | Lines 124 and 125 are **two separate one-line paragraphs**, so no tool can move words between them; and re-rendering either pads it to 80 bytes off the block's 77/78 grid. C8 names the width but not the two-paragraph obstacle. | `common.paragraphs` → `[124-124 box-prose]`, `[125-125 box-prose]`; simulated render gives three 80-byte lines | hand-lay three lines at 78 bytes (line 124 unchanged): `(* and the gap to fdist_uniform is the constant sa_eps_inf (1 in infotheo's *)` / `(* var_dist, the un-halved sum of absolute differences; 1/2 in the total    *)` / `(* variation distance of the literature).                                   *)` |
| F4 | MUST | B4 (59, 60, 321, 376) | Three of B4's five comment sites sit inside multi-line structured paragraphs. Lines 59-60 are inside **one 10-line paragraph 51-60** (the whole record index); reflowing it collapses the index into a wall of text (simulated). Line 321 is inside paragraph 319-323 (the `Mathematical source:` listing); line 376 inside 375-377 (a displayed inequality). Line 94 is a `table` paragraph, which reflow skips, and line 394 is its own paragraph and is safe. | `common.paragraphs("security/pgg_schreier.v")` | hand-edit these lines only, at their current 79-byte width: 59 `(*   schreier_epsilon_decreasing == eps(L') <= eps(L) when L <= L'           *)`; 60 `(*   security_monotone == var_dist at L' bounded by eps(L) when L <= L'      *)`; 94 `(*     sum-of-squares to total variation conversion for reversible chains    *)`; 321 `       Saloff-Coste (1997), Theorem 2.6 (sum-of-squares to TV conversion)`; 376 `   sqrt(N) * r^L' <= sqrt(N) * r^L when 0 <= r < 1, L <= L'.` |
| F5 | MUST | B8 | The sheet calls `rigidity_s5_instance.v:31-33` three "header index" entries. They are not: all three plus the `vm_compute demonstrations:` line form **one `box-prose` paragraph 30-33**, and reflowing it runs the three equations together (simulated). | `common.paragraphs("instances/s5/rigidity_s5_instance.v")` | hand-edit three lines only, at their current 79-byte width: `(*   s5_n_traces_natB1 : n_traces_natB 4 1 path_comm_nat = 4                 *)` / `(*   s5_n_traces_natB2 : n_traces_natB 4 2 path_comm_nat = 13                *)` / `(*   s5_n_traces_natB3 : n_traces_natB 4 3 path_comm_nat = 40                *)` |
| F6 | MUST | A5 | `psl211_exec.v`'s header box has two `==` columns (23 in Definitions, 27/28 in Key results); the splitter picks 23, so re-laying out this entry moves the `==` four columns left and turns two lines into three. Simulated. | `render_para` on paragraph 43-44 | hand-edit line 44 only: `(*                              inside the fuel                               *)` (80 bytes) |
| F7 | MUST | procedure | `reflow.py` joins words with single spaces, so **every paragraph it re-lays-out loses the two-space sentence spacing** the frozen files use throughout and the sheet's own replacement text preserves. I confirmed on A2: `own file.  Compiling` becomes `own file. Compiling`. Separately, `reflow.py --all` is not idempotent on this tree (15 changed lines in `psl211_endpoints.v`, 197 in `pgg_instance.v`, 178 in `algebraic_rigidity.v`, 177 in `pgg_schreier.v` with no edit at all). | ran `reflow.reflow_text(..., everything=True)` | run reflow only with `--base 124f950`, never `--all`; and restore the two-space sentence spacing by hand in every rewritten paragraph, or lay those paragraphs out by hand |
| F8 | SHOULD | A27 | Same relation as A8 and A26 (a declaration running no reduction) gets a different verb; the sheet's own reason says "same substitution as A26" and then writes a different word. | A8 "No reduction is used", A26 "uses no reduction of its own", A27 "needs no reduction" | `…so instantiating it at the supplied layout uses no reduction of the run's own, and the two modes of the sharing family share the profile's one decision.` |
| F9 | SHOULD | A18 | Five words changed where one suffices, and "faster" then matches "makes it faster" two sentences later. | `protocol/pgg_instance.v:19` | `…and a second is replaced by a faster equivalent. generic_static_recon…` (rest of A18 unchanged) |
| F10 | SHOULD | A30, A31 | The tree's one phrase for the factor of two is "twice the total variation distance of the literature" / "the total variation distance of the literature" (`lib/var_dist_supp.v:55,182`, `manifest/pgg_analysis_manifest.v:851`, `pgl27_proximity.v:21-22`, `five_card_proximity.v:22-23`, `psl211_word_proximity.v:16-17`). "the standard total variation of the literature" and "with no factor one half" are two further spellings of it. | as cited | A30 as in F3. A31: `sw_bound_eps is the stated upper bound on one endpoint marginal, in the sum of absolute differences, which is twice the total variation distance of the literature. It is a per-position quantity, not a coalition-view distance.` |
| F11 | SHOULD | A3 | "obligation" is the word this file and its twin use (A7 keeps the owner's "leaves termination as the instance's only obligation"; A8, A23, A25 all say "obligation"). A3 introduces a second phrasing for the same thing, and moves the appositive off "termination". | `psl211_exec.v:16`; twin `pgl27_exec.v:351-353` | `…and the interpreter fuel is psl211_fuel.  Of the three run facts termination is its only obligation, decided by reduction at fuel 220 in 0.49 s of vm_compute and 0.47 s of Qed (measured 2026-09-15 with Time in place, then removed); reconstruction follows from the coordinate law and uses no reduction, and the endpoint equation is stated once at the profile in psl211_endpoints.v.` |
| F12 | SHOULD | A33 | "not just a classification, and twice it is…" breaks the `not just X but Y` construction and leaves "it" to be resolved. | `algebraic_rigidity.v:391-392` | `…This is the numeric form of the threshold leg of algebraic rigidity: genus is not just a classification, and twice the genus is a literal upper bound on the gap.` |
| F13 | SHOULD | B8 | `len` is a project-local abbreviation with no MathComp precedent (MathComp says `size`) and none in this tree; and `s5_n_traces_len*` names the wrong function — the lemmas evaluate `n_traces_natB` (`groups/pgg_raag.v:123`), not `n_traces` (`:1033`). The real sibling family is `star3_ntB0/1/2` in `groups/pgg_raag_clique.v:252,255,258`, the same function at the same word lengths, which the sheet does not mention. | as cited; `git grep -w` for `s5_n_traces_natB1`, `s5_n_traces_natB2`, `s5_n_traces_natB3`, `s5_ntB1/2/3`, `s5_n_traces_len1`, `n_traces_len` returns **zero** across all tracked `.v` | **one recommendation: `s5_n_traces_natB1`, `s5_n_traces_natB2`, `s5_n_traces_natB3`** — collision-free, spells the declaration the lemma evaluates, carries no abbreviation the owner has not himself published as a name, and puts the word length last as a digit exactly as `star3_ntB0/1/2` does. To hold one word per concept, rename `star3_ntB0/1/2` to `star3_n_traces_natB0/1/2` in the same commit (`pgg_raag_clique.v` is not frozen and compiles in seconds); if the owner declines that, `s5_ntB1/2/3` is the alternative that matches the existing family instead. Do not use `len` either way. |
| F14 | NOTE | A4, A5 | The "Current" quotations span a neighbouring paragraph: A4's line 34 belongs to the previous entry (paragraph 33-34) and A5's lines 42 and 45 are separate entries. The applier matches on the paragraph, which is 35-36 and 43-44. | `common.paragraphs("instances/psl211/psl211_exec.v")` | — |
| F15 | NOTE | A0-N | Says "13 lines" in its heading and "12 lines" in the Counts section, and lists 12 line numbers. The GL(2,q) and PGL(2,q) names actually occupy 37 lines of `reconstruct/pgl_bound.v` (12 13 14 17 18 19 20 21 47 53 56 59 78 92 94 96 97 102 110 113 128 137 155 158 164 167 169 170 173 183 196 197 207 211 212 213 232). The verdict "innocent" is right; only the inventory is short. | as cited | — |
| F16 | NOTE | A18 | The brief asks me to verify "rows A2 and A18's kept cost". A18's replacement keeps **no** economic word: all four hits (cheaper, owes, cheaper, costly) change. The precedent question applies to A2 alone, and it holds (see Summary). | `pgg_instance.v:19,23,25,28` | — |
| F17 | NOTE | — | `manifest/pgg_tableau_security_property_relations.v:470` still reads "the distance below is what that omission costs", an abstract use that `cca5e24` left standing. It is a residue in a non-frozen file, not a counter-precedent, and it is outside this sheet. | as cited | — |
| F18 | NOTE | A2 | The replacement keeps "as probe P1b2". If the owner reads probe identifiers as barred, this is a site; I judge it measurement provenance, like the dates and the timings beside it, and recommend keeping. | `psl211_endpoints.v:9` | — |
| F19 | NOTE | judgement call 2 | **"variation distance" is what the non-frozen tree calls `var_dist` today**, with "total variation distance of the literature" reserved for the halved quantity (32 sites; see F10's list plus `psl211_mixing.v:540`, `pgl27_mixing.v:1043`, `five_card_kim.v:460`). So A35's and A36's in-row correction of "TV distance" is consistent and should be accepted. I would **not** extend it to the rest of `pgg_collusion_bound.v` in this pass: lines 115 and 881 are a plain vocabulary residue, but lines 237 and 263 ("at total variation distance at least 2 (1 - k/n.+1)") assert a number that is a `var_dist`, so they need a truth check and not a word swap. Record 115, 237, 263, 881 as residue. | as cited | — |
| F20 | NOTE | C8 | The line range is wrong. The 77/78-byte sub-block is **119-125**, not 112-125; lines 110-118 of the same run sit at 81 characters (line 115 measures 83 bytes because of an em-dash). Substance of C8 stands. | measured | — |
| F21 | NOTE | B2 | "each consumed on the same line" is inexact at 605 and 615: the goal carries the two names to the following line (`apply: pinj`, `apply: rinj`), though never by name. Harmless. Scope check otherwise confirmed: no `i` or `j` in context at 401, 605 or 615, and ssreflect would raise a hard error rather than capture if there were. | `reconstruct/transitivity_privacy.v:401,605-606,615-616` | — |
| F22 | NOTE | B3 | **I agree: do not rename.** The vendoring header is verbatim as quoted (`smc/smc_interpreter.v:1-3`, infotheo fork @ dumas2017dual, commit b5a899f7), and `git grep -w rstep_disjoint -- '*.v'` returns the declaration alone. One risk the sheet omits: `smc_interpreter.v` sits at the bottom of the tree (every `psl211_*`, `pgg_*` file requires it), so C3's "verified by a real compile of that one file" understates it — a single-file compile of it invalidates the whole tree, including the 900 s file. Another reason to defer. | as cited | — |
| F23 | NOTE | C9 | The order is **sound**, but its stated reason for `psl211_alldecks.v` is false: `psl211_alldecks.v` does **not** require `psl211_endpoints` (`grep "^From" instances/psl211/psl211_alldecks.v`; and `psl211_endpoints.v:35` requires `psl211_exec`, i.e. the arrow runs the other way). The correct reason is that `psl211_alldecks.v` sits above the frozen `psl211_orbit.v`, `psl211_exec.v` and `psl211_profile.v`, so it must be compiled while **their** `.vo` files are valid; the first of those to be rewritten is B1's `psl211_orbit.v`, whose rebuild then invalidates `psl211_endpoints.vo`. Same rule puts `pgg_schreier.v` / `pgg_schreier_weighted.v` before the `algebraic_rigidity.v` rows (A30-A33) and the two S5 files before A1's `groups/pgg_raag.v`, which the sheet's step 2 already does in the right order. | as cited | — |
| F24 | NOTE | Part B "innocent" list | Checked and agreed: `gl2`/`pgl2`/`scalar_gl2`/`card_gl2`/`card_scalar_gl2`/`card_pgl2` are the 2 of GL(2,q) and PGL(2,q); `card_bool2` is `#\|bool\| = 2`; `pgl27_gen_val1`/`gen_val2` carry no L; the MathComp `*1*`/`*2*` names are library names. Add two more that a scan will flag and that are also innocent: the `H1`/`H2` intro names sitting on the same lines as B1's and B6's renames, and `psl1`/`psl2`/`Hpsl1`/`Hpsl2` in B3 (they inherit the pattern from `l1 l2` rather than spelling it, exactly as the sheet says). | measured | — |

## Sheet rows I ACCEPT unchanged

Part A: **A1, A2, A4, A6, A7, A8, A9, A10, A11, A12, A13, A14, A15, A16, A17, A19, A20, A21, A22, A23, A24, A25, A26, A28, A29, A34, A35, A36, A37, A38.**

A38 ("overhead" → "no extra term") I recommend **taking**: the statement is `var_dist adversary_marginal target_uniform <= epsilon`, so "no extra term" says exactly what the bound has, and it is more type-honest than the word it replaces. A0-W ("worth" at `pgg_collusion_bound.v:12`) I recommend **leaving**: the sense is how much a hypothesis yields, ordinary English, and the offered alternative is no plainer. "cash it in" folded into A35 is correctly removed, and "use it" is right ( `var_dist_uniform_supp` is read at `pgg_collusion_bound.v:478` and named at 702 and 721, so the plural "the file's coalition bounds" is fair).

Part A0: **A0-R, A0-M, A0-S, A0-E** accepted (A0-R's 70 + A10's 2 = 72 reproduced independently); **A0-N** accepted in verdict, corrected in count (F15); **A0-W** accepted.

Part B: **B1, B2, B4 (with F4's hand-edit instruction), B5, B6, B7** accepted; **B3** accepted as "do not rename" (F22); **B8** accepted only with F1 and F5 added and the name changed per F13.

Part C: **C1, C2, C4, C5, C6, C7, C9** accepted (C9 with F23's corrected reason); **C3** accepted and strengthened (F22); **C8** accepted with its range corrected and its reason widened (F3, F20). Risks missing from Part C that F2-F7 add: paragraph boundaries wider than the sheet's line ranges (A32, B4, B8-rigidity), the two-paragraph obstacle at A30, the `==` re-columning at A5, the two-space sentence spacing lost by `reflow.py`, and the fact that `reflow.py --all` rewrites hundreds of untouched lines per file. No replacement sits inside a coqdoc bracket, none contains a quote or a comment delimiter, and `scripts/strip_comments.py` exists, so C4's check is runnable.
