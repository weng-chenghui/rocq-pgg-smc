# What the `cp` retires at PSL(2,11), and every reference to it

Checked at HEAD `cdda915` on branch `feat/tableau-extensions-probe`.
`grep -rn psl211_rows` and `grep -rn psl211_word_proximity` over the whole
working tree, with the agent worktrees under `.claude/` and this probe
directory excluded. Reverse closures computed from the `Require` lines of
every file in a `-R` or `-Q` directory of `_CoqProject`, transitively.

## Files to `git rm`

| Path | Lines | Contents |
|---|---|---|
| `instances/psl211/psl211_rows.v` | 324 | 12 declarations, two of them recorded `Fail`s, all of which land in `instances/psl211/tableau/` |

## Files replaced in place

| Path | Lines before | Lines after | What leaves |
|---|---|---|---|
| `instances/psl211/psl211_word_proximity.v` | 387 | 140 | 12 of its 17 declarations, and its `Require` of `psl211_rows` |

The staged reduced copy is `staged/instances/psl211/psl211_word_proximity.v`.
It keeps `psl211_word_proximity_close`, `psl211_pow2_40_ge1`,
`psl211_pow2_40_gt0`, `psl211_word_law_le2` and the recorded `Fail`
`psl211_word_law_by_var_dist_le2`, each token-identical to production's, and
its innermost `Local Open Scope` is unchanged.

## References that must change, in code

| File:line | Text at that line | What it becomes |
|---|---|---|
| `_CoqProject:235` | `instances/psl211/psl211_rows.v` | removed |
| `_CoqProject:236` | `instances/psl211/psl211_word_proximity.v` | unchanged, and the six `instances/psl211/tableau/…` lines are inserted after it, in phase order; see `../STATUS.md` |
| `instances/psl211/psl211_word_proximity.v:91` | `From pgg_smc Require Import psl211_rows psl211_word_model.` | removed; the staged reduced copy's import list carries the change and nothing else |

`instances/psl211/psl211_word_proximity.v` is the only file in the tree that
`Require`s `psl211_rows`. No file `Require`s `psl211_word_proximity`. No
source anywhere writes a fully qualified `pgg_smc.<module>.<ident>` for a name
this batch moves, so no cited name changes.

The reduced proximity file must stay ahead of the phase files in
`_CoqProject`, because `psl211_tableau_analysis_bridged.v` requires it for
`psl211_word_proximity_close` and the two `pow2` facts.

## References that must change, in a comment

Three production files cite a file whose contents move. Each staged copy is
generated by `../stage_edits.py` from production by anchored replacement, so
it cannot freeze against a base that moves; each is verified comment-only by
comparing the comment-stripped text of the two copies, and no line exceeds 80
bytes.

| File:line | Text at that line | What it should cite after the move |
|---|---|---|
| `instances/psl211/psl211_models.v:40` | `(* psl211_rows.v, beside the Tableau surface whose ExactWitness record gives  *)` | `instances/psl211/tableau/psl211_tableau_analysis_bridged.v`, which is where `psl211_exact_witness` lands. **Deferred**, see below |
| `instances/psl211/psl211_word_proximity.v:173` | `    row is measured against is the model psl211_rows.v publishes and not a` | already carried: the line is inside the docstring of `psl211_word_proximity_cert_idealE`, that declaration moves, and its docstring is the one intended difference `verify.py` prints |
| `instances/psl211/psl211_reading_constancy.v:15` | `(* the proximity arm, in instances/psl211/psl211_word_proximity.v, and this   *)` | the same file: the word row is published there, not in the proximity file |
| `instances/psl211/psl211_reading_constancy.v:44` | `(* field, and the row of instances/psl211/psl211_word_proximity.v publishes   *)` | the same file |
| `instances/psl211/psl211_reading_constancy.v:74` | `(* instances/psl211/psl211_word_proximity.v carries a proximity certificate,  *)` | the same file |
| `instances/psl211/psl211_reading_constancy.v:780` | `    instances/psl211/psl211_word_proximity.v carries a proximity certificate,` | the same file, inside the docstring of `psl211_alldecks_constancy_false_word584` |
| `manifest/pgg_analysis_manifest.v:801` | `(*                          instances/psl211/psl211_word_proximity.v, whose   *)` | Row 11's certificate is `psl211_word_proximity_cert` of `psl211_tableau_analysis_bridged.v`; its distance field `psl211_word_proximity_close` stays in `instances/psl211/psl211_word_proximity.v`, so the row needs both paths. **Deferred**, see below |
| `manifest/pgg_analysis_manifest.v:805` | `(*                          instances/psl211/psl211_word_proximity.v |        *)` | `psl211_tableau_analysis_bridged.v` in `instances/psl211/tableau/`, where `psl211_word_view_proximity` lands. **Deferred**, see below |

### What goes with this move, and what is deferred

Only `instances/psl211/psl211_reading_constancy.v` is repointed with the move.
Its reverse closure is empty, so the edit costs one compile of that file.

`instances/psl211/psl211_models.v` and `manifest/pgg_analysis_manifest.v` are
**deferred to one pass after all four instances**, because each will need the
same treatment again at PGL(2,7) and at five-card and each costs twenty and
seventeen module recompiles for a comment. Their staged copies stay in
`staged-comments/` and are not part of this `cp`. Until that pass runs, the
two files cite a file whose name still exists in the tree at the other three
instances and, at PSL(2,11), a file that still exists but no longer holds the
cited declaration.

`.Makefile.rocq.d:383-384` also names `psl211_rows`. It is generated by the
build from `_CoqProject` and is regenerated on the next build, so it is not
edited.

`manifest/pgg_analysis_manifest.v:794` cites
`instances/psl211/psl211_word_model.v` and `:811` cites
`psl211_word_proximity_close` by name alone. Both stay correct and are not
edited.

The staged copies are under `staged-comments/`, mirroring the production tree,
and deliberately **not** under `staged/`: `staged/instances/psl211` is an `-R`
root of this probe's `_CoqProject`, so a `.v` placed there would shadow the
production module it copies.

```
staged-comments/instances/psl211/psl211_models.v
staged-comments/instances/psl211/psl211_reading_constancy.v
staged-comments/manifest/pgg_analysis_manifest.v
```

## What each comment edit costs to recompile

Reverse closures at this HEAD. None of the three reaches
`instances/psl211/psl211_endpoints.v`, which sits below all of them, so no
comment edit here can cause its `.vo` to be rebuilt by a single-file compile.

| Edited file | Modules to recompile | Which |
|---|---|---|
| `instances/psl211/psl211_reading_constancy.v` (with the move) | 0 | nothing imports it |
| `manifest/pgg_analysis_manifest.v` | 17 | `pgg_tableau`, `pgg_tableau_syntax`, `pgg_tableau_arm_relations`, `pgg_analysis_client`, `pgl27_rows`, `pgl27_proximity`, `five_card_rows`, `five_card_proximity`, `psl211_rows`, `psl211_word_proximity`, `psl211_reading_constancy`, and the six `s5_tableau_*` |
| `instances/psl211/psl211_models.v` | 20 | those 17, plus `pgg_analysis_manifest`, `psl211_analysis`, `psl211_word_model` |

After this batch lands, the six `psl211_tableau_*` modules join both lists and
`psl211_rows` leaves them. The two large edits are comment-only and could be
taken in a separate commit from the move; the `psl211_reading_constancy.v`
edit costs nothing and should go with the move.

## References outside the code, for the owner's note. Not edited here

No file under `paper/`, `paper-wadt2026/` or
`paper-wadt2026-baseline-application/` mentions `psl211_rows` or
`psl211_word_proximity`, and neither does `README.md`. Every remaining
occurrence is in a dated plan under `docs/`, which records what a past batch
did and is not a description of the tree at HEAD.

| File | Kind |
|---|---|
| `docs/superpowers/plans/2026-09-15-psl211-plan-b-alldecks.md` (`:137`, `:174`, `:1922`, `:2244`, `:2247`, `:2361`, `:2366`, `:2372`, `:2394`, `:2423`, `:2461`, `:2527`, `:2626`, `:2803`, `:2811`, `:2844`) | the landing plan of the all-decks row |
| `docs/superpowers/plans/2026-09-19-psl211-spectral-constancy-landing.md` (`:20`, `:30`, `:35`, `:53`) | file list and staging instructions of a landed batch |
| `docs/superpowers/plans/2026-09-19-kim-spectral-landing.md` (`:33`, `:62`, `:85`) | file list of a landed batch |
| `docs/superpowers/plans/2026-09-19-general-dealer-law-landing.md` (`:103`, `:157`) | file list and a count of declarations per rows file |

Dated notes under `notes/` also cite the two names; they are the same kind of
record and are left alone for the same reason.
