# Economic-metaphor comment pass: every changed passage

Total changed passages: **119**.  Per row: R1=8, R2=9, R3=2, R4=3, R5=13, R6=72, RA=1, RB=11.
Line numbers are the **HEAD** (6a20f7d) line of the first changed
line.  `Row` is the row of the owner's vocabulary table:

- **R1** - hop / loss of a hybrid argument
- **R2** - loss (ideal object replaced by a finite or biased one)
- **R3** - perfect against statistical security
- **R4** - tight / slack
- **R5** - literal (reductions, or a measured time)
- **R6** - the plain relation
- **RA** - residue (a): the three analyses part by FAMILY
- **RB** - residue (b): a table outside the development

Code tokens after comment stripping are identical to HEAD in
every file listed (`token_check.py`).

## 1. `instances/kim2025/tableau/five_card_tableau_analysis_bridged.v:11`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* instance owes an arm is here, every row it publishes is here, and every    *)
```

After:

```
(* instance gives an arm is here, every row it publishes is here, and every   *)
```

## 2. `instances/kim2025/tableau/five_card_tableau_analysis_bridged.v:480`

Declaration checked: `Definition five_card_exact_witness`  -  Row **R6** (the plain relation)

Before:

```
    field, so the witness is the whole of what this instance owes the exact
    arm. *)
```

After:

```
    field, so the witness is all the exact arm requires of this instance. *)
```

## 3. `instances/kim2025/tableau/five_card_tableau_analysis_bridged.v:570`

Declaration checked: `Lemma kim_biased_epsE`  -  Row **R1** (hop / loss of a hybrid argument)

Before:

```
    it publishes twice that, because the comparison through the ideal cut
    spends the number once for each of the two committed pairs. *)
```

After:

```
    it publishes twice that, because the comparison through the ideal cut is a
    two-hop hybrid, one hop for each of the two committed pairs, and each hop
    loses that number. *)
```

## 4. `instances/kim2025/tableau/five_card_tableau_analysis_bridged.v:579`

Declaration checked: `Lemma kim_biased_exact_le_eps`  -  Row **R4** (tight / slack)

Before:

```
    built on it publishes. The certificate therefore overstates the distance
    it certifies by about two fifths, and the gap is the price of quoting the
    bundle's number rather than the exact one. *)
```

After:

```
    built on it publishes. The certificate's number is therefore not tight: it
    exceeds the distance it certifies by about two fifths, the slack left by
    quoting the bundle's number rather than the exact one. *)
```

## 5. `instances/kim2025/tableau/five_card_tableau_analysis_bridged.v:850`

Declaration checked: `Lemma five_card_row_repeated39_atE`  -  Row **R5** (literal (reductions, or a measured time))

Before:

```
   erefl takes spent 147 s here against reflexivity's 0.07 s. Measured on
```

After:

```
   erefl takes costs 147 s here against reflexivity's 0.07 s. Measured on
```

## 6. `instances/kim2025/tableau/five_card_tableau_analysis_bridged.v:891`

Declaration checked: `Definition five_card_row_biased_inv25`  -  Row **R6** (the plain relation)

Before:

```
    changes and the status it earns does not. It continues from the exact
```

After:

```
    changes and its transfer status does not. It continues from the exact
```

## 7. `instances/kim2025/tableau/five_card_tableau_analysis_bridged.v:997`

Declaration checked: `Lemma kim_biased_proximity_eps_halfE`  -  Row **R1** (hop / loss of a hybrid argument)

Before:

```
    input-indistinguishability arm spends it once for each of the two
    committed pairs it compares and the proximity arm compares one law with
    one law. The relation is between these two certificates and not between
    the two arms: the same model also carries kim_biased_cert, whose marginal
    bound is sqrt 5 over eighty and which therefore publishes sqrt 5 over
    forty, so neither arm determines the number of the other. *)
```

After:

```
    input-indistinguishability arm loses that distance at one hop for each
    of the two committed pairs it compares, and the proximity arm compares
    one law with one law. The relation is between these two certificates and
    not between the two arms: the same model also carries kim_biased_cert,
    whose marginal bound is sqrt 5 over eighty and which therefore publishes
    sqrt 5 over forty, so neither arm determines the number of the other. *)
```

## 8. `instances/kim2025/tableau/five_card_tableau_analysis_bridged.v:1035`

Declaration checked: `Lemma five_card_row_biased_branch_indistinguishability_atE`  -  Row **R6** (the plain relation)

Before:

```
    AnalysisBridged coordinate, so naming the Sampled value costs the
    input-indistinguishability row nothing. *)
```

After:

```
    AnalysisBridged coordinate, so naming the Sampled value leaves the
    input-indistinguishability row's coordinate unchanged. *)
```

## 9. `instances/kim2025/tableau/five_card_tableau_analysis_bridged.v:1055`

Declaration checked: `Definition five_card_row_biased_proximity`  -  Row **R1** (hop / loss of a hybrid argument)

Before:

```
    and the conjunction are independent outright. The number is spent once,
    against the input-indistinguishability row's twice. Its transfer status is
```

After:

```
    and the conjunction are independent outright. The certificate crosses to
    the ideal once and so loses that number once, where the
    input-indistinguishability row crosses twice. Its transfer status is
```

## 10. `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v:11`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* instance owes an arm is here, every row it publishes is here, and every    *)
```

After:

```
(* instance gives an arm is here, every row it publishes is here, and every   *)
```

## 11. `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v:256`

Declaration checked: `Definition pgl27_exact_witness`  -  Row **R6** (the plain relation)

Before:

```
    field, so the witness is the whole of what this instance owes the exact
    arm. *)
```

After:

```
    field, so the witness is all the exact arm requires of this instance. *)
```

## 12. `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v:296`

Declaration checked: `Lemma pgl27_word_view_const`  -  Row **R6** (the plain relation)

Before:

```
    spends no mixing bound. *)
```

After:

```
    appeals to no mixing bound. *)
```

## 13. `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v:319`

Declaration checked: `Definition pgl27_word_cert`  -  Row **R3** (perfect against statistical security)

Before:

```
    pgl27_word_view_const and is exact. The two currencies are visible in the
    fields: everything about the ideal cut is exact and three-transitive, and
    the only inexact quantity anywhere in this row is the walk's 2^-40. *)
```

After:

```
    pgl27_word_view_const and is exact. Perfect and statistical security are
    both visible in the fields: everything about the ideal cut is exact and
    three-transitive, and the only statistical quantity anywhere in this row
    is the walk's 2^-40. *)
```

## 14. `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v:371`

Declaration checked: `Definition pgl27_row_word_tableau`  -  Row **R1** (hop / loss of a hybrid argument)

Before:

```
    and each crossing spends the same mixing bound once. *)
```

After:

```
    and each of the two hops loses the same mixing bound. *)
```

## 15. `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v:844`

Declaration checked: `Lemma pgl27_word_proximity_eps_halfE`  -  Row **R1** (hop / loss of a hybrid argument)

Before:

```
    arm spends it once for each of the two dealt secrets it compares and the
```

After:

```
    arm loses that bound at each of two hops, one per dealt secret, and the
```

## 16. `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v:897`

Declaration checked: `Definition pgl27_row_word_proximity`  -  Row **R1** (hop / loss of a hybrid argument)

Before:

```
    its two marginals. The number is spent once, against the
    input-indistinguishability certificate's twice. Its transfer status is
```

After:

```
    its two marginals. The certificate crosses to the ideal once and so loses
    that number once, where the input-indistinguishability certificate
    crosses twice. Its transfer status is
```

## 17. `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v:951`

Declaration checked: `Theorem pgl27_word_view_proximity`  -  Row **R5** (literal (reductions, or a measured time))

Before:

```
   0.01 s. The row-against-row form is the expensive one, 96.0 s by
```

After:

```
   0.01 s. The row-against-row form is the slow one, 96.0 s by
```

## 18. `instances/psl211/psl211_reading_constancy.v:17`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* is what the input-indistinguishability arm would cost it.                  *)
```

After:

```
(* is what the input-indistinguishability arm would require of it.            *)
```

## 19. `instances/psl211/psl211_reading_constancy.v:250`

Declaration checked: `Lemma psl211_perdeck_coalition_below_k`  -  Row **R6** (the plain relation)

Before:

```
    to discharge this premise, and it is the only nontrivial premise the
    counterexample owes. *)
```

After:

```
    to discharge this premise, and it is the counterexample's only nontrivial
    premise. *)
```

## 20. `instances/psl211/psl211_reading_constancy.v:524`

Declaration checked: `Lemma fdistmap_point_condE`  -  Row **R6** (the plain relation)

Before:

```
    condition, which is the form a support hypothesis is spent in. *)
```

After:

```
    condition, which is the form a support hypothesis is used in. *)
```

## 21. `instances/psl211/psl211_reading_constancy.v:754`

Declaration checked: `Lemma psl211_alldecks_constancy_false_word`  -  Row **R2** (loss (ideal object replaced by a finite or biased one))

Before:

```
    below the reciprocal of the group order. The first distance is the price
    of replacing the exact shuffle by one a dealer can perform, the second is
    a certificate's own distance field, and a row over any cut law but the
    group-uniform one pays both. *)
```

After:

```
    below the reciprocal of the group order. The first distance bounds the
    loss of replacing the exact shuffle by one a dealer can perform, the
    second is a certificate's own distance field, and a row over any cut law
    but the group-uniform one loses both. *)
```

## 22. `instances/psl211/psl211_reading_constancy.v:776`

Declaration checked: `Lemma psl211_alldecks_constancy_false_word584`  -  Row **R2** (loss (ideal object replaced by a finite or biased one))

Before:

```
    draw, and the 2^-40 is the whole information-theoretic price of that
    replacement, while the eps is a certificate's own distance field. It is
```

After:

```
    draw, and 2^-40 bounds the whole information-theoretic loss of that
    replacement, while the eps is a certificate's own distance field. It is
```

## 23. `instances/psl211/psl211_reading_constancy.v:1020`

Declaration checked: `Lemma psl211_alldecks_constancy_set0`  -  Row **R6** (the plain relation)

Before:

```
    parameters therefore has to spend a nonempty coalition: the field is not
```

After:

```
    parameters therefore has to exhibit a nonempty coalition: the field is not
```

## 24. `instances/kim2025/five_card_exec.v:17`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R5** (literal (reductions, or a measured time))

Before:

```
(* record assembled here. The instance pays two reductions for them:          *)
```

After:

```
(* record assembled here. The instance needs two reductions for them:         *)
```

## 25. `instances/kim2025/five_card_exec.v:464`

Declaration checked: `Definition five_card_params`  -  Row **R6** (the plain relation)

Before:

```
    the run recovers is the conjunction, and the interpreter budget is 100.
```

After:

```
    the run recovers is the conjunction, and the interpreter fuel is 100.
```

## 26. `instances/kim2025/five_card_exec.v:481`

Declaration checked: `Lemma five_card_terminates`  -  Row **R6** (the plain relation)

Before:

```
    within the budget. The one run fact that has no route through the
    algebra: it depends on the interpreter and on the budget, and is decided
```

After:

```
    within the fuel. The one run fact that has no route through the
    algebra: it depends on the interpreter and on the fuel, and is decided
```

## 27. `instances/kim2025/five_card_exec.v:510`

Declaration checked: `Definition five_card_endpoints`  -  Row **R5** (literal (reductions, or a measured time))

Before:

```
    and spends the reduction once at the profile. *)
```

After:

```
    and uses one reduction, at the profile. *)
```

## 28. `instances/kim2025/five_card_exec.v:519`

Declaration checked: `Definition five_card_recon`  -  Row **R6** (the plain relation)

Before:

```
    reconstruction correctness of this instance costs no further proof. *)
```

After:

```
    reconstruction correctness of this instance needs no further proof. *)
```

## 29. `instances/pgl27/pgl27_exec.v:19`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* owes no proof at all for the third: reconstruction follows from the        *)
```

After:

```
(* needs no proof at all for the third: reconstruction follows from the       *)
```

## 30. `instances/pgl27/pgl27_exec.v:351`

Declaration checked: `Definition pgl27_dealt_params`  -  Row **R6** (the plain relation)

Before:

```
    commits an input, and the interpreter budget is pgl27_fuel.  The
```

After:

```
    commits an input, and the fuel is pgl27_fuel.  The
```

## 31. `instances/pgl27/pgl27_exec.v:372`

Declaration checked: `Lemma pgl27_dealt_terminates`  -  Row **R6** (the plain relation)

Before:

```
    algebra: it depends on the interpreter and on the budget, and is decided
```

After:

```
    algebra: it depends on the interpreter and on the fuel, and is decided
```

## 32. `instances/pgl27/pgl27_exec.v:374`

Declaration checked: `Lemma pgl27_dealt_terminates`  -  Row **R5** (literal (reductions, or a measured time))

Before:

```
    which states the same reduction at the hand-written plug; both spend the
```

After:

```
    which states the same reduction at the hand-written plug; both use the
```

## 33. `instances/pgl27/pgl27_exec.v:392`

Declaration checked: `Definition pgl27_dealt_endpoints`  -  Row **R5** (literal (reductions, or a measured time))

Before:

```
    abstract-readout equation at the dealt readout, so the instance pays no
    reduction of its own for it. *)
```

After:

```
    abstract-readout equation at the dealt readout, so the instance uses no
    reduction of its own for it. *)
```

## 34. `instances/pgl27/pgl27_exec.v:402`

Declaration checked: `Definition pgl27_dealt_recon`  -  Row **R6** (the plain relation)

Before:

```
    consequence of that law and costs no further proof. *)
```

After:

```
    consequence of that law and needs no further proof. *)
```

## 35. `instances/s5/s5_exec.v:23`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R5** (literal (reductions, or a measured time))

Before:

```
(* instance spends three reductions in all, one termination per mode and the  *)
```

After:

```
(* instance needs three reductions in all, one termination per mode and the   *)
```

## 36. `instances/s5/s5_exec.v:25`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R5** (literal (reductions, or a measured time))

Before:

```
(* obligations instantiate; the two reconstructions cost no reduction at all, *)
```

After:

```
(* obligations instantiate; the two reconstructions use no reduction at all,  *)
```

## 37. `instances/s5/s5_exec.v:380`

Declaration checked: `Lemma s5_dealt_terminates`  -  Row **R5** (literal (reductions, or a measured time))

Before:

```
    states the same reduction at the hand-written plug; both spend the
```

After:

```
    states the same reduction at the hand-written plug; both use the
```

## 38. `instances/s5/s5_exec.v:397`

Declaration checked: `Definition s5_dealt_endpoints`  -  Row **R5** (literal (reductions, or a measured time))

Before:

```
    equation at the dealt readout, so the run pays no reduction of its own
    for it. *)
```

After:

```
    equation at the dealt readout, so the run uses no reduction of its own
    for it. *)
```

## 39. `instances/s5/s5_exec.v:407`

Declaration checked: `Definition s5_dealt_recon`  -  Row **R6** (the plain relation)

Before:

```
    consequence of that law and costs no further proof. *)
```

After:

```
    consequence of that law and needs no further proof. *)
```

## 40. `instances/s5/s5_exec.v:936`

Declaration checked: `Definition s5_supplied_endpoints`  -  Row **R5** (literal (reductions, or a measured time))

Before:

```
    content readout, so instantiating it at the additive layout costs this
    run no reduction of its own, and the two modes share one decision. *)
```

After:

```
    content readout, so instantiating it at the additive layout uses no
    reduction of this run's own, and the two modes share one decision. *)
```

## 41. `instances/psl211/psl211_alldecks.v:36`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* labellings, and a stolen leading parameter is the most expensive mistake   *)
(* to diagnose in that position.                                              *)
```

After:

```
(* labellings, and a stolen leading parameter is the mistake hardest to       *)
(* diagnose in that position.                                                 *)
```

## 42. `instances/psl211/psl211_alldecks.v:70`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(*   psl211_alldecks_terminates == every process finishes inside the budget   *)
```

After:

```
(*   psl211_alldecks_terminates == every process finishes inside the fuel     *)
```

## 43. `instances/psl211/psl211_alldecks.v:566`

Declaration checked: `Lemma psl211_alldecks_terminates`  -  Row **R5** (literal (reductions, or a measured time))

Before:

```
   carried through at no cost and the all-decks mode costs no more to
   terminate than the dealer-dealt one. *)
```

After:

```
   carried through unchanged and the all-decks mode needs no more reduction
   to terminate than the dealer-dealt one. *)
```

## 44. `instances/psl211/psl211_alldecks.v:569`

Declaration checked: `Lemma psl211_alldecks_terminates`  -  Row **R6** (the plain relation)

Before:

```
    all-decks run finishes inside the budget, at every deck description and
```

After:

```
    all-decks run finishes inside the fuel, at every deck description and
```

## 45. `instances/psl211/psl211_alldecks.v:577`

Declaration checked: `Definition psl211_alldecks_recon`  -  Row **R6** (the plain relation)

Before:

```
    derives it from the coordinate law, so the whole of what this instance
    owes reconstruction is distinctness, the heart set and the chirality
    readout. *)
```

After:

```
    derives it from the coordinate law, so all reconstruction requires of this
    instance is distinctness, the heart set and the chirality readout. *)
```

## 46. `instances/psl211/tableau/psl211_tableau_observed.v:8`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* run correctness: the interpreter finishes within the budget of 220 steps,  *)
```

After:

```
(* run correctness: the interpreter finishes within the fuel of 220 steps,    *)
```

## 47. `instances/psl211/tableau/psl211_tableau_observed.v:31`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(*                           == the prefix with the budget written as a       *)
```

After:

```
(*                           == the prefix with the fuel written as a         *)
```

## 48. `instances/psl211/tableau/psl211_tableau_observed.v:41`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(*                           == and so does the literal budget                *)
```

After:

```
(*                           == and so does the literal fuel                  *)
```

## 49. `instances/psl211/tableau/psl211_tableau_observed.v:73`

Declaration checked: `Definition psl211_alldecks_prefix`  -  Row **R6** (the plain relation)

Before:

```
    budget, and the three run facts. What has been proved at this point is run
```

After:

```
    fuel, and the three run facts. What has been proved at this point is run
```

## 50. `instances/psl211/tableau/psl211_tableau_observed.v:130`

Declaration checked: `Definition psl211_alldecks_prefix_lit`  -  Row **R6** (the plain relation)

Before:

```
    budget written as the literal 220 rather than named. *)
```

After:

```
    fuel written as the literal 220 rather than named. *)
```

## 51. `instances/psl211/tableau/psl211_tableau_observed.v:143`

Declaration checked: `Lemma psl211_alldecks_prefix_lit_paramsE`  -  Row **R6** (the plain relation)

Before:

```
    run. The row names the budget, so that it is stated once. *)
```

After:

```
    run. The row names the fuel, so that it is stated once. *)
```

## 52. `instances/s5/s5_mixing.v:396`

Declaration checked: `Lemma s5_cert_bound_col_dominant`  -  Row **R4** (tight / slack)

Before:

```
    dominance, the second half of the budget psd_of_dominant spends. *)
```

After:

```
    dominance, the second of the two slack conditions psd_of_dominant needs. *)
```

## 53. `instances/s5/s5_mixing.v:480`

Declaration checked: `Definition s5_gap_R`  -  Row **R6** (the plain relation)

Before:

```
    of the word buys a factor 1 - gap. *)
```

After:

```
    of the word gives a factor 1 - gap. *)
```

## 54. `instances/s5/s5_mixing.v:510`

Declaration checked: `Lemma s5_spectral_convergence_gap`  -  Row **R6** (the plain relation)

Before:

```
    bound are stated in, which is how one letter of a dealer's word acquires
    a price. *)
```

After:

```
    bound are stated in, which is where each letter of a dealer's word
    contributes its factor 1 - gap. *)
```

## 55. `manifest/pgg_tableau.v:170`

Declaration checked: `Record ExactWitness`  -  Row **R6** (the plain relation)

Before:

```
   producing this record owes nothing further about mutual information. *)
```

After:

```
   producing this record has nothing further to supply about mutual
   information. *)
```

## 56. `manifest/pgg_tableau.v:400`

Declaration checked: `Definition observed_correct`  -  Row **R6** (the plain relation)

Before:

```
   three run facts reaches run correctness at no further cost. *)
```

After:

```
   three run facts reaches run correctness with no further proof. *)
```

## 57. `manifest/pgg_tableau.v:705`

Declaration checked: `Lemma exact_tail`  -  Row **R6** (the plain relation)

Before:

```
   exact arm, and what makes an ExactWitness the whole of what an instance
   owes on it. *)
```

After:

```
   exact arm, and what makes an ExactWitness the whole of what an instance
   supplies on it. *)
```

## 58. `manifest/pgg_tableau.v:842`

Declaration checked: `Definition ConcludePayload`  -  Row **R6** (the plain relation)

Before:

```
   not monotone in the number it carries owes a different obligation here. *)
```

After:

```
   not monotone in the number it carries needs a different obligation here. *)
```

## 59. `manifest/pgg_tableau.v:858`

Declaration checked: `Lemma port_conclude`  -  Row **R6** (the plain relation)

Before:

```
   number owes as well; it is what lets a row state the constant a paper
   cites while asserting about the coalition no more than the certificate
   proved. *)
```

After:

```
   number must satisfy as well; it is what lets a row state the constant a
   paper cites while asserting about the coalition no more than the
   certificate proved. *)
```

## 60. `security/pgg_mixing.v:270`

Declaration checked: `Lemma symm_ds_power_norm_sq_bound`  -  Row **R6** (the plain relation)

Before:

```
    L walk steps costs one factor of alpha^2, unconditional on any
```

After:

```
    L walk steps contributes one factor of alpha^2, unconditional on any
```

## 61. `security/pgg_mixing.v:886`

Declaration checked: `Lemma big_offdiag_exchange`  -  Row **R6** (the plain relation)

Before:

```
    two indices are exchanged.  It turns a row-indexed dominance budget into
    a column-indexed one, which is why a dominance certificate has to supply
    both the row sums and the column sums. *)
```

After:

```
    two indices are exchanged.  It turns a row-indexed dominance condition
    into a column-indexed one, which is why a dominance certificate has to
    supply both the row sums and the column sums. *)
```

## 62. `security/pgg_mixing.v:902`

Declaration checked: `Lemma abs_prod_le_sqr`  -  Row **R6** (the plain relation)

Before:

```
    off-diagonal entry pay for itself out of the two diagonal entries it
    sits between. *)
```

After:

```
    off-diagonal entry be absorbed by the two diagonal entries it sits
    between. *)
```

## 63. `security/pgg_mixing.v:915`

Declaration checked: `Lemma pointwise_dom`  -  Row **R6** (the plain relation)

Before:

```
    per-entry step of the diagonal-dominance argument: the budget al is
    spent half on each of the two coordinates the entry couples. *)
```

After:

```
    per-entry step of the diagonal-dominance argument: al is split half onto
    each of the two coordinates the entry couples. *)
```

## 64. `instances/kim2025/five_card_mixing.v:44`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(*   fc_kim_rho_supp_pow == every cut a Kim word shuffle charges is a         *)
(*                          rotation                                          *)
```

After:

```
(*   fc_kim_rho_supp_pow == every cut in the support of a Kim word shuffle    *)
(*                          is a rotation                                     *)
```

## 65. `instances/kim2025/five_card_mixing.v:301`

Declaration checked: `Lemma five_card_static_obs_const`  -  Row **R6** (the plain relation)

Before:

```
    certificate, and it is exact. It spends no mixing bound. *)
```

After:

```
    certificate, and it is exact. It appeals to no mixing bound. *)
```

## 66. `instances/kim2025/five_card_mixing.v:406`

Declaration checked: `Lemma kim_centi_static_obs_indistinguishability`  -  Row **R1** (hop / loss of a hybrid argument)

Before:

```
    spectral one spent once for each pair, so the only inexact quantity is that
    mixing distance; the constancy of the ideal reading is exact and costs
    nothing. *)
```

After:

```
    spectral one lost at one hop for each pair, so the only inexact quantity is
    that mixing distance; the constancy of the ideal reading is exact and adds
    nothing. *)
```

## 67. `instances/pgl27/pgl27_spectral.v:106`

Declaration checked: `Definition pgl27_gap_R`  -  Row **R6** (the plain relation)

Before:

```
    one letter of the word buys a factor 1 - gap. *)
```

After:

```
    one letter of the word gives a factor 1 - gap. *)
```

## 68. `instances/pgl27/pgl27_spectral.v:517`

Declaration checked: `Lemma pgl27_cert_bound_col_dominant`  -  Row **R4** (tight / slack)

Before:

```
    dominance, the second half of the budget psd_of_dominant spends. *)
```

After:

```
    dominance, the second of the two slack conditions psd_of_dominant needs. *)
```

## 69. `instances/pgl27/pgl27_spectral.v:586`

Declaration checked: `Lemma pgl27_spectral_convergence_gap`  -  Row **R6** (the plain relation)

Before:

```
    sc_convergence field of SchreierCertificate is stated in, which is how
    one letter of the word acquires a price. *)
```

After:

```
    sc_convergence field of SchreierCertificate is stated in, which is where
    each letter of the word contributes its factor 1 - gap. *)
```

## 70. `instances/pgl27/tableau/pgl27_tableau_checks.v:115`

Declaration checked: `Lemma pgl27_row_word_arm_neq` (the comment sits inside its proof; corrected after the plain-rows audit, finding B9)  -  Row **R5** (literal (reductions, or a measured time))

Before:

```
(* Three costs are each why one line reads as it does. Binding the prior as
```

After:

```
(* Three timings are each why one line reads as it does. Binding the prior as
```

## 71. `instances/psl211/psl211_models.v:347`

Declaration checked: `Definition psl211_alldecks_observed`  -  Row **R6** (the plain relation)

Before:

```
    the run finishes inside its budget, the verifier collects one endpoint per
```

After:

```
    the run finishes inside its fuel, the verifier collects one endpoint per
```

## 72. `instances/psl211/psl211_models.v:386`

Declaration checked: `Lemma psl211_alldecks_fuelE`  -  Row **R6** (the plain relation)

Before:

```
(** psl211_alldecks_fuelE — the derived plug runs at the budget the
```

After:

```
(** psl211_alldecks_fuelE — the derived plug runs at the fuel the
```

## 73. `instances/psl211/psl211_models.v:457`

Declaration checked: `Lemma psl211_alldecks_exec_viewE`  -  Row **R6** (the plain relation)

Before:

```
    group action, and it is what the endpoint equation buys. *)
```

After:

```
    group action, and it is what the endpoint equation gives. *)
```

## 74. `instances/psl211/psl211_models.v:620`

Declaration checked: `Lemma psl211_dealerPE`  -  Row **R6** (the plain relation)

Before:

```
    identification costs nothing probabilistic and fixes which of the dealer
```

After:

```
    identification adds nothing probabilistic and fixes which of the dealer
```

## 75. `manifest/pgg_tableau_syntax.v:64`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* The surface spends nineteen identifiers as global keywords in every file   *)
```

After:

```
(* The surface reserves nineteen identifiers as global keywords in every file *)
```

## 76. `manifest/pgg_tableau_syntax.v:79`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* because ssreflect already spends it, measured on 2026-09-19 by binding it  *)
```

After:

```
(* because ssreflect already reserves it, measured on 2026-09-19 by binding it*)
```

## 77. `manifest/pgg_tableau_syntax.v:153`

Declaration checked: `Definition mk_indistinguishability`  -  Row **R3** (perfect against statistical security)

Before:

```
   what makes the two currencies of the port visible where it is written: the
   fourth component is the only inexact one, and the fifth is exact. *)
```

After:

```
   what makes the port's perfect and statistical halves visible where it is
   written: the fourth component is the only statistical one, and the fifth
   is exact. *)
```

## 78. `manifest/pgg_tableau_syntax.v:283`

Declaration checked: `Definition exact_leaks`  -  Row **R6** (the plain relation)

Before:

```
   then dropped. The result is the witness itself, so the annotation costs
   nothing in the term a row builds and everything proved about an annotated
```

After:

```
   then dropped. The result is the witness itself, so the annotation leaves
   the term a row builds unchanged and everything proved about an annotated
```

## 79. `instances/psl211/psl211_mixing.v:544`

Declaration checked: `Lemma psl211_word_mixing`  -  Row **R2** (loss (ideal object replaced by a finite or biased one))

Before:

```
    actually perform at a cost of 2^-40. *)
```

After:

```
    actually perform, losing at most 2^-40. *)
```

## 80. `instances/psl211/psl211_mixing.v:562`

Declaration checked: `Lemma psl211_endpoint_mixing`  -  Row **R2** (loss (ideal object replaced by a finite or biased one))

Before:

```
    exactly uniform, so the 2^-40 is the whole price of replacing the exact
```

After:

```
    exactly uniform, so 2^-40 bounds the whole loss of replacing the exact
```

## 81. `instances/psl211/psl211_mixing.v:592`

Declaration checked: `Lemma psl211_joint_mixing`  -  Row **R2** (loss (ideal object replaced by a finite or biased one))

Before:

```
    an independent binary secret prior costs nothing, so every observable of
```

After:

```
    an independent binary secret prior loses nothing, so every observable of
```

## 82. `instances/psl211/tableau/psl211_tableau_analysis_bridged.v:26`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* arm, and the instance owes it one witness. Over the 584-letter word cut,   *)
```

After:

```
(* arm, and the instance gives it one witness. Over the 584-letter word cut,  *)
```

## 83. `instances/psl211/tableau/psl211_tableau_analysis_bridged.v:30`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* the instance owes it a certificate whose ideal is the first row's own      *)
```

After:

```
(* the instance gives it a certificate whose ideal is the first row's own     *)
```

## 84. `instances/psl211/tableau/psl211_tableau_analysis_bridged.v:149`

Declaration checked: `Definition psl211_exact_witness`  -  Row **R6** (the plain relation)

Before:

```
    field, so the witness is the whole of what this instance owes the exact
    arm. *)
```

After:

```
    field, so the witness is all the exact arm requires of this instance. *)
```

## 85. `lib/mutual_info_recoding.v:43`

Declaration checked: `Lemma eq_in_fdistmap`  -  Row **R6** (the plain relation)

Before:

```
(** Two maps that agree wherever a law charges push that law forward to the
```

After:

```
(** Two maps that agree wherever a law gives mass push that law forward to the
```

## 86. `lib/mutual_info_recoding.v:80`

Declaration checked: `Lemma fdistmap_prod_uniform_Ml`  -  Row **R6** (the plain relation)

Before:

```
    monodromy group charges every sample exactly as it charges the
```

After:

```
    monodromy group gives every sample the same mass it gives the
```

## 87. `lib/var_dist_supp.v:15`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* pushforward charges only the image, and the distance between the point     *)
```

After:

```
(* pushforward is supported in the image, and the distance between the point  *)
```

## 88. `lib/var_dist_supp.v:25`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(*   fdistmap_neq0_codom        == a pushforward charges only the image       *)
```

After:

```
(*   fdistmap_neq0_codom        == a pushforward is supported in the image    *)
```

## 89. `lib/var_dist_supp.v:80`

Declaration checked: `Lemma var_dist_fdistmap_supp_inj`  -  Row **R6** (the plain relation)

Before:

```
    reading and owes it on the group. *)
```

After:

```
    reading and must establish it on the group. *)
```

## 90. `manifest/pgg_tableau_arm_relations.v:15`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* over one model, so the ideal cut and the constancy field are spent inside  *)
```

After:

```
(* over one model, so the ideal cut and the constancy field are used inside   *)
```

## 91. `manifest/pgg_tableau_arm_relations.v:33`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* The arm's mathematics is spent on the ideal witness's independence: it     *)
```

After:

```
(* The arm's mathematics rests on the ideal witness's independence: it        *)
```

## 92. `manifest/pgg_tableau_arm_relations.v:192`

Declaration checked: `Fail Definition idealproximity_tail_without_independence`  -  Row **R6** (the plain relation)

Before:

```
    the product of its marginals is where the witness is spent. Without it the
```

After:

```
    the product of its marginals is where the witness is used. Without it the
```

## 93. `instances/pgl27/pgl27_mixing.v:1047`

Declaration checked: `Lemma pgl27_word_mixing`  -  Row **R2** (loss (ideal object replaced by a finite or biased one))

Before:

```
    actually perform at a cost of 2^-40. *)
```

After:

```
    actually perform, losing at most 2^-40. *)
```

## 94. `instances/pgl27/pgl27_mixing.v:1062`

Declaration checked: `Lemma pgl27_endpoint_mixing`  -  Row **R2** (loss (ideal object replaced by a finite or biased one))

Before:

```
    shuffle makes that marginal exactly uniform, and this is the price of
    replacing it by a finite word. *)
```

After:

```
    shuffle makes that marginal exactly uniform, and 2^-40 bounds the loss of
    replacing it by a finite word. *)
```

## 95. `instances/pgl27/pgl27_profile.v:99`

Declaration checked: `Definition pgl27_marginal_bound`  -  Row **R2** (loss (ideal object replaced by a finite or biased one))

Before:

```
    from the group itself, and the price of that idealisation is paid by
    pgl27_word_mixing, not here. *)
```

After:

```
    from the group itself, and the loss of that idealisation is bounded by
    pgl27_word_mixing, not here. *)
```

## 96. `instances/pgl27/pgl27_word_privacy.v:88`

Declaration checked: `Definition pgl27_word_marginal_bound`  -  Row **R2** (loss (ideal object replaced by a finite or biased one))

Before:

```
    group itself; the 2^-40 here is what replacing that draw by a finite word
    costs, and it is the number a word row's spectral arm spends. *)
```

After:

```
    group itself; 2^-40 here bounds the loss of replacing that draw by a
    finite word, and it is the number a word row's spectral arm carries. *)
```

## 97. `reconstruct/invariant_profiler.v:22`

Declaration checked: `Section invariant_profiler.`  -  Row **R6** (the plain relation)

Before:

```
(* closed under complementation, i.e. the cheap subset-sum reasoning is valid;  *)
```

After:

```
(* closed under complementation, i.e. the simple subset-sum reasoning is valid; *)
```

## 98. `reconstruct/invariant_profiler.v:81`

Declaration checked: `Lemma maschke_ss`  -  Row **R6** (the plain relation)

Before:

```
    invariant dimensions are closed under taking complements, so the cheap
    subset-sum reasoning over the dimension profile is sound. The
```

After:

```
    invariant dimensions are closed under taking complements, so the simple
    subset-sum reasoning over the dimension profile is sound. The
```

## 99. `security/var_dist_joint_law.v:151`

Declaration checked: `(inside) Lemma var_dist_own_marginals`  -  Row **R1** (hop / loss of a hybrid argument)

Before:

```
   data processing, and replacing the two factors one at a time costs d each,
   so the number is spent three times. *)
```

After:

```
   data processing, and replacing the two factors one at a time loses d at
   each hop, so the three hops together give 3 * d. *)
```

## 100. `groups/pgg_raag_clique.v:1112`

Declaration checked: `Lemma table_T4_star3`  -  Row **R6** (the plain relation)

Before:

```
   For the search-space reading this means the deck designer buys nothing by
   choosing between two graphs with the same clique polynomial. *)
```

After:

```
   For the search-space reading this means the choice between two graphs with
   the same clique polynomial leaves the deck designer's trace counts equal. *)
```

## 101. `instances/denboer1989/den_boer_run.v:195`

Declaration checked: `Lemma den_boer_run_recovers`  -  Row **R6** (the plain relation)

Before:

```
    in the group: the random cut buys privacy, not correctness. This is the
```

After:

```
    in the group: the random cut gives privacy, not correctness. This is the
```

## 102. `instances/kim2025/five_card_analysis.v:126`

Declaration checked: `Definition den_boer_profile`  -  Row **R6** (the plain relation)

Before:

```
    twice costs nothing at the value level, per the retention check below. *)
```

After:

```
    twice leaves the value unchanged, per the retention check below. *)
```

## 103. `instances/pgl27/pgl27_profile_privacy.v:117`

Declaration checked: `Lemma pgl27_view_indep_via_profile`  -  Row **R6** (the plain relation)

Before:

```
    theorem costs the instance nothing beyond its two premises. *)
```

After:

```
    theorem needs nothing of the instance beyond its two premises. *)
```

## 104. `instances/psl211/tableau/psl211_tableau_executable.v:71`

Declaration checked: `Definition psl211_alldecks_executable`  -  Row **R6** (the plain relation)

Before:

```
    argument, and the interpreter is given the instance's budget of 220
```

After:

```
    argument, and the interpreter is given the instance's fuel of 220
```

## 105. `instances/s5/tableau/s5_tableau_analysis_bridged.v:147`

Declaration checked: `Definition s5_rand_exact_witness`  -  Row **R6** (the plain relation)

Before:

```
    from this one field, so the witness is the whole of what this instance
    owes the exact arm. *)
```

After:

```
    from this one field, so the witness is all the exact arm requires of this
    instance. *)
```

## 106. `protocol/pgg_functionality.v:140`

Declaration checked: `Definition realises_expected`  -  Row **R6** (the plain relation)

Before:

```
   discharge it by reflexivity. That case still closes through funext, at the
   price of functional_extensionality_dep and propositional_extensionality in
   the row's assumption list, so the difference between the two specifications
```

After:

```
   discharge it by reflexivity. That case still closes through funext, which
   puts functional_extensionality_dep and propositional_extensionality into
   the row's assumption list, so the difference between the two specifications
```

## 107. `security/pgg_schreier.v:119`

Declaration checked: `file header (no enclosing declaration)`  -  Row **R6** (the plain relation)

Before:

```
(* (2019) proved a single shuffle suffices at the cost of more cards.         *)
```

After:

```
(* (2019) proved a single shuffle suffices if more cards are used.            *)
```

## 108. `instances/pgl27/tableau/pgl27_tableau_algebraic.v:57`

Declaration checked: `Definition pgl27_algebraic_start`  -  Row **RA** (residue (a): the three analyses part by FAMILY)

Before:

```
    that mode. The three probability models the instance analyses part three
    levels above, so what this file fixes is shared by every row the
    instance publishes. *)
```

After:

```
    that mode. The three analyses part three levels above, at Sampled, where
    each names its own family, so what this file fixes is shared by every
    row the instance publishes. *)
```

## 109. `manifest/pgg_tableau.v:1045`

Declaration checked: `Lemma publish_armE`  -  Row **RB** (residue (b): a table outside the development)

Before:

```
   out to a published row, where a paper's table reads them. *)
```

After:

```
   out to a published row. *)
```

## 110. `instances/s5/tableau/s5_tableau_analysis_bridged.v:184`

Declaration checked: `Lemma s5_row_rand_armE`  -  Row **RB** (residue (b): a table outside the development)

Before:

```
    readings. This is the value a paper's table prints in the arm column for
    this row, settled by the certify statement the program wrote. *)
```

After:

```
    readings. The certify statement the program wrote settles which arm that
    is. *)
```

## 111. `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v:420`

Declaration checked: `Lemma pgl27_row_exact_armE`  -  Row **RB** (residue (b): a table outside the development)

Before:

```
    between two readings. This is the value a paper's table prints in the arm
    column for this row; certify_exact_armE and publish_armE are why the
    value is settled by the row's certify statement. *)
```

After:

```
    between two readings. The row's certify statement settles which arm that
    is, through certify_exact_armE and publish_armE. *)
```

## 112. `instances/kim2025/tableau/five_card_tableau_analysis_bridged.v:527`

Declaration checked: `Lemma five_card_row_uniform_armE`  -  Row **RB** (residue (b): a table outside the development)

Before:

```
    distance between two readings. This is the value a paper's table prints in
    the arm column for this row, settled by the certify statement the program
    wrote. *)
```

After:

```
    distance between two readings. The certify statement the program wrote
    settles which arm that is. *)
```

## 113. `instances/kim2025/tableau/five_card_tableau_analysis_bridged.v:693`

Declaration checked: `Lemma five_card_row_repeated_indistinguishability_armE`  -  Row **RB** (residue (b): a table outside the development)

Before:

```
    the committed bits. This is the value a paper's table prints in the arm
    column for this row, settled by the certify statement the program
    wrote. *)
```

After:

```
    the committed bits. The certify statement the program wrote settles which
    arm that is. *)
```

## 114. `instances/psl211/tableau/psl211_tableau_analysis_bridged.v:190`

Declaration checked: `Lemma psl211_row_alldecks_armE`  -  Row **RB** (residue (b): a table outside the development)

Before:

```
    readings. This is the value a paper's table prints in the arm column for
    this row, settled by the certify statement the program wrote. *)
```

After:

```
    readings. The certify statement the program wrote settles which arm that
    is. *)
```

## 115. `manifest/pgg_tableau.v:983`

Declaration checked: `Definition security_arm_of`  -  Row **RB** (residue (b): a table outside the development)

Before:

```
   family. It reads ab_arm past the publish statement, so it is the reader a
   paper's arm column is taken from, and publish_armE is why the publish
   statement does not change the answer. *)
```

After:

```
   family. It reads ab_arm past the publish statement, and publish_armE is
   why the publish statement does not change the answer. *)
```

## 116. `instances/kim2025/tableau/five_card_tableau_analysis_bridged.v:860`

Declaration checked: `Lemma five_card_row_repeated39_armE`  -  Row **RB** (residue (b): a table outside the development)

Before:

```
    put it, so the row at 2^-39 and the row at the bundle's spectral number
    print one arm column. *)
```

After:

```
    put it, so the row at 2^-39 and the row at the bundle's spectral number
    carry the same arm. *)
```

## 117. `instances/pgl27/tableau/pgl27_tableau_checks.v:101`

Declaration checked: `Fail Definition pgl27_row_word_arm_neq`  -  Row **RB** (residue (b): a table outside the development)

Before:

```
    table column holds the arm the row's certify statement wrote and no
    other. *)
```

After:

```
    row carries the arm its certify statement wrote and no other. *)
```

## 118. `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v:476`

Declaration checked: `Lemma pgl27_row_word39_armE`  -  Row **RB** (residue (b): a table outside the development)

Before:

```
    port untouched, so the row at 2^-39 carries the arm the row at its own sum
    carries, and the table column is the same for both. *)
```

After:

```
    port untouched, so the row at 2^-39 carries the arm the row at its own
    sum carries. *)
```

## 119. `instances/pgl27/tableau/pgl27_tableau_analysis_bridged.v:500`

Declaration checked: `Lemma pgl27_row_word_branch39_armE`  -  Row **RB** (residue (b): a table outside the development)

Before:

```
    certify statement leaves the port where that statement put it, so the
    branch row's table column is the one pgl27_row_word39 has. *)
```

After:

```
    certify statement leaves the port where that statement put it, so the
    branch row carries the arm pgl27_row_word39 carries. *)
```

