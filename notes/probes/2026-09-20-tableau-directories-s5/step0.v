(* Step 0: which logical name does a file in a tableau/ subdirectory of an
   -R <instance dir> pgg_smc root receive, and does a short Require resolve
   it from another file? *)
From pgg_smc Require Import s5_tableau_algebraic.
Check s5_loadpath_probe.
Print Libraries.
