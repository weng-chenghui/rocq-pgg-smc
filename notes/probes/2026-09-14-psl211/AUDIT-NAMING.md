# Naming / style / precedent audit, PSL(2,11) chirality instance spec

Date: 2026-09-14. Adversarial read of
`notes/2026-09-14-003000-psl211-chirality-instance-proposal.md` sections 5, 7, 8
and of `notes/probes/2026-09-14-psl211/probe_*.v`, `psl211_tables.v`.

Repo state at audit time. `git log -3 --oneline` moved during the audit, from
`b7f39a4` to `dabc61d refactor(manifest): rename the row-program carrier from
ViewLift to Tableau`, via `3aa45e3 docs(notes): PSL(2,11) twelve-card chirality
instance spec with claim ledger`. `git status --short instances/pgl27` is now
clean. The concurrent `pgl27_sym_sigmas` to `pgl27_moves` rename has LANDED in
the live tree: `instances/pgl27/pgl27_mixing.v:61` defines
`pgl27_moves : 5.-tuple {perm 'I_8}` and `:69` keeps
`Notation pgl27_sym_sigmas := pgl27_moves (only parsing).` as a shim.
`pgl27_spectral.v` uses `pgl27_moves` 17 times, `pgl27_word_privacy.v` twice,
`manifest/pgg_analysis_manifest.v:963` once.
`.claude/worktrees/agent-aa400558bc3410a7b` is a second worktree pinned at
`b7f39a4`, one commit behind. Every citation below is to the live tree.

## Findings

| # | Claim (spec location) | Verdict | Evidence (file:line / grep / compiled snippet) | Fix |
|---|---|---|---|---|
| A1 | `Gen_PGGTypes 2 6` pattern, carrier `@Gen_PGGTypes 1 10 psl211_gens` with `pgg_N' = 11` (spec 7 L1) | TRUE PRECEDENT | `instances/pgl27/pgl27_group.v:97` `Notation pgl27_M := (@Gen_PGGTypes 2 6 pgl27_gens).` Template at `protocol/pgg_interface.v:430-458`: `Let T := m.+1. Let N := n.+2.` so `1 10` gives 2 generators on `'I_12`, `pgg_N' = 11`. `probe_group.vo` exists, so `psl211_N'` and both `Fail` mutations compiled | none |
| A2 | stabp subgroup pattern, `pgl27_orbit.v` lines 243-289 (probe_orbit.v:160-164) | TRUE PRECEDENT, line range slightly off | `instances/pgl27/pgl27_orbit.v:243` `Local Definition stabp`, `:247 stabpP`, `:254 group_set_stabp`, `:267 gens_sub_stabp`, `:278 Local Canonical stabp_group`, `:280 G_sub_stabp`. Block is 242-281, not 243-289 | cite 242-281 |
| A3 | `subset_class_orbitE` shape (spec 7 L11) | TRUE PRECEDENT, different RHS shape | `instances/pgl27/pgl27_orbit.v:720` `Lemma subset_class_orbitE (S : {set 'I_8}) : #\|S\| = 4 -> orbit 'P^* (pgg_G pgl27_M) S = [set T \| (#\|T\| == 4) && (subset_class T == subset_class S)].` The probe's `setsA_orbitE` (probe_decomposition.v:91) drops the cardinality premise and names an enumerated set as RHS | keep, note the shape change in the plan |
| A4 | `pgl27_orbit.v` `code_bfs` (spec 7 L11) | TRUE PRECEDENT | `instances/pgl27/pgl27_orbit.v:601` `Local Fixpoint code_bfs`, `:623` `code_bfs 12 [:: (rep_list b, [::])]` | none |
| A5 | `pgl27_mixing.v` `walkN` / `elem_bfs` shape (spec 5, 7 L14/L15) | TRUE PRECEDENT | `instances/pgl27/pgl27_mixing.v:137 Local Fixpoint elem_bfs`, `:152 Local Definition elem_table`, `:174 pred_table`, `:208 Local Fixpoint walkN`. Both are `Local` in the landed file and `Local` in `probe_mixing.v:27,41,89` | none |
| A6 | "two letters, L = 406 if the non-symmetric alphabet is acceptable to the walk lemma" (spec 5, Word model) | FALSE. The landed walk machinery requires an inverse-closed alphabet with an involutive letter pairing | `instances/pgl27/pgl27_spectral.v:575` `apply: (symm_ds_TV_bound_inv_closed (f := pgl27_inv_letter_ord)).` discharged by `:577 pgl27_inv_letter_ordK : involutive pgl27_inv_letter_ord` and `:578 pgl27_moves_inv_closed`. A two-letter `{r4, m6}` alphabet has no such pairing | delete the two-letter option, or plan new machinery. The three-letter `{r4, m6, m6i}` alphabet does admit the pairing `[0;2;1]` |
| A7 | `ttrans_view_indep_gen` template (probe_bridge.v:106) | TRUE PRECEDENT | `reconstruct/transitivity_privacy.v:674` | none |
| A8 | `inde_prod_fst` (spec 8, probe_bridge.v:103) | TRUE PRECEDENT, arity correct | `reconstruct/transitivity_privacy.v:88`. Real uses at `:515` and `:689` pass the same eight `@`-arguments the probe passes | none |
| A9 | `profile_view_indep` sibling (spec 7 note, probe_decomposition.v:134-136) | TRUE PRECEDENT | `reconstruct/transitivity_privacy.v:959` | none |
| A10 | "The all-decks dealer of `transitivity_privacy.v` is the existing point of contact: there the code view already factors through H cap C" (spec 5, Deck model) | FALSE PRECEDENT | `grep -c ":&:" reconstruct/transitivity_privacy.v` returns `0`. `coalition_view` (`:493`) has carrier `{ffun 'I_N'.+1 -> 'I_N'.+1}`, a padded card-value reader, not an intersection. The only sub-coalition relation in the file is `C' \subset C` at `:719`, routed through a local `restrict` ffun, not a set intersection | strike the sentence. The H-cap-C factoring is new work, not a point of contact |
| A11 | "Not `ttrans_private`" and the implied novelty of a transitivity-free bridge (spec 5, Privacy bridge) | TRUE, and section 6 does not duplicate it | `reconstruct/transitivity_privacy.v:746` `Hypothesis rhoG_ntrans : ntransitive t (rho @* G) [set: 'I_N'.+1] 'P.` is in scope for lines 762-925, so `ttrans_view_indep_alldecks` (`:840`) and `ttrans_view_indep_deck` (`:900`) both carry it as a discharged explicit argument. `alldecks_shuffle_absorb` (`:860`) is the one transitivity-free result there, and it is a law-preservation fact, not an independence statement | none. Note that the docstring at `:893-899` saying the shuffle "was never load-bearing" is about the shuffle coordinate, not about transitivity |
| A12 | `MkReconPlug` with a non-identity `rp_content` (spec 7 L13) | FALSE PRECEDENT. Every `MkReconPlug` in the live tree passes the identity | `instances/pgl27/pgl27_scheme.v:96` `id`, `instances/pgl27/pgl27_profile_privacy.v:204` `id`, `instances/s5/rigidity_s5_instance.v:252` `id`, `instances/kim2025/five_card_family.v:148` `five_card_program.fc_content`, and `instances/denboer1989/five_card_program.v:185` `Definition fc_content (c : 'I_5) : 'I_5 := c.` The repo's one colour observer sits in the models layer, `instances/kim2025/five_card_models.v:277 five_card_exec_colour_view`, not in the plug | see E3. Keep `rp_content := id` and put the colour collapse in the observer |
| A13 | `ReconPlug` four fields, `MonodromyProfile` four fields, `ThresholdScheme` with `ts_k'` (spec 8) | TRUE | `reconstruct/covering_scheme.v:122-130` four fields. `protocol/pgg_monodromy_profile.v:54-70` four fields. `reconstruct/pgg_sharing_framework.v:47-64` eight fields, `ts_private` premise is `#\|C\| < ts_k'.+1`, matching `probe_decomposition.v:74` | none |
| A14 | `pgl27_recovery.v` shapes (spec 5, 7 L16) | TRUE PRECEDENT, but it consumes 2-transitivity | `instances/pgl27/pgl27_recovery.v:68 pgl27_seven_reveal_class`, `:93 pgl27_six_reveal_ambiguous`, `:131 pgl27_reveal_ambiguous`. The ambiguity proof runs on `:108 have Htr := pgl27_2transitive.` and `:110 atransP2` | see A15 |
| A15 | "Out of scope, not probed: 2-transitivity (not used)" (spec 7, closing paragraph) contradicts "transported by 2-transitivity as in `pgl27_recovery.v`" (spec 5, Recovery) | CONTRADICTION | The two sentences are in the same document. `pgl27_six_reveal_ambiguous` cannot be transported without it, per A14 | add a ledger row for 2-transitivity of `<r4, m6>`, or drop the recovery claim from scope |
| A16 | `pgl27_card` (spec 7 L17) | TRUE PRECEDENT | `instances/pgl27/pgl27_mixing.v:545` `Lemma pgl27_card : #\|pgg_G pgl27_M\| = 336.` | none |
| A17 | Chirality, "the non-square scaling exchanges them, a PGL(2,11) shuffle would destroy the secret" (spec 2, lines 73-76, the paper's headline) | NOT PROBED, NOT IN THE LEDGER | `probe_orbit.v:71-77` substitutes "a cheaper witness", the full-pile reversal, and states the intended witness only in a comment. No ledger row L1-L17 covers the swap | add a row: the non-square scaling maps `tblA` rowwise onto `tblB` |
| B1 | `orbit_class`, `deck_ok`, `heart_set`, `is_heart`, `subset_class`, `orbit_encode`, `orbit_valid`, `orbit_scheme`, `orbit_recon_invariant` reused unprefixed | COLLISION, nine names, same `pgg_smc` namespace, different meaning | All nine are bare non-`Local` top-level declarations in files with no `Section` and no `Module`: `pgl27_orbit.v:90,94,98,104,112,349` and `pgl27_scheme.v:46,79,86`. Compiled proof in `audit-naming/collide.v`: `Locate deck_ok` prints `Constant pgg_smc.pgl27_orbit.deck_ok` before the redefinition and `Constant collide.deck_ok` after, with the old one demoted to `pgl27_orbit.deck_ok`. `Print orbit_valid` then renders as `pgl27_orbit.deck_ok sh /\ orbit_class sh = s`, two spellings of one word in one goal | prefix all nine `psl211_` |
| B2 | The reuse is not merely lexical: the two `orbit_valid`s are different propositions | SEMANTIC COLLISION | `pgl27_scheme.v:46` is a two-conjunct `deck_ok sh /\ orbit_class sh = s`, destructed as `[_ <-]` at `:87`. `probe_decomposition.v:52` is a three-conjunct `deck_ok sh /\ subset_valid (heart_set sh) /\ orbit_class sh = s`, destructed as `[_ [_ <-]]` at `:111`. Likewise `subset_class` is a cross-ratio verdict on 4-subsets at `pgl27_orbit.v:104` and a table membership on 6-subsets at `probe_decomposition.v:47`, and `is_heart` is `val c < 4` on `'I_8` against `val c < 6` on `'I_12` | as B1 |
| B3 | Is unprefixed reuse acceptable under the repo's conventions? | NO for these nine | Cross-instance imports are a live pattern: `instances/kim2025/five_card_analysis.v:81-84` `Require Import`s `five_card_group five_card_program five_card_scheme_I5` from `instances/denboer1989` alongside its own files, and `manifest/pgg_analysis_manifest.v:74` `Require Export`s all three facades. The facades avoid their own collisions by module wrapping: `Module PGL27Analysis` (`pgl27_analysis.v:86-302`), `Module FiveCardAnalysis` (`five_card_analysis.v:98-370`), `Module S5Analysis` (`s5_analysis.v:74-349`), which is why `profile`, `observed`, `exec_plug` can repeat across three files. The cone files have no such wrapper. The one existing cone-level triple collision, `content_of`, is a verbatim copy of the same body in `s5_trace.v:53`, `pgl27_trace.v:76` and `denboer_trace.v:40`, three instance directories in one namespace. It survives only because no file imports two of them, and it is also a quality-catalog R9 duplication | prefix |
| B4 | `colour_view` | NO GLOBAL COLLISION, but a near miss | `instances/kim2025/five_card_analysis.v:173 Definition colour_view` sits inside `Module FiveCardAnalysis`, so its full name is `FiveCardAnalysis.colour_view`. `Locate colour_view` under full mathcomp plus infotheo reports `No object of basename colour_view` | safe, but `psl211_colour_view` reads better next to `psl211_colour_view_indep` |
| B5 | `colour_view` is defined TWICE inside the probe set, with two different bodies | INTERNAL COLLISION | `probe_bridge.v:89` `fun u => [ffun i => if i \in C then colour (tnth (encode u.1) (rho u.2 i)) else false]` against `probe_decomposition.v:142` `... is_heart (tnth (orbit_encode u.1) (@pgg_rho psl211_M u.2 i)) ...`. `probe_decomposition.v:148` then restates `colour_view_indep_of_fibres` as a fresh `Admitted` rather than instantiating `probe_bridge.v:111` | make `probe_decomposition`'s `colour_view` the instance of `probe_bridge`'s, and record the instantiation as a ledger row |
| B6 | `walkN`, `elem_table` | NO COLLISION | Both are `Local` at `pgl27_mixing.v:208,152` and `Local` at `probe_mixing.v:89,41` | keep them `Local` |
| B7 | `total` (probe_mixing.v:98) | LIBRARY COLLISION | `Locate total` under `ssreflect ... order` reports `Constant Corelib.ssr.ssrbool.total`, shadowed by the probe's `Local Definition total (d : seq N) : N` | rename `walk_total` |
| B8 | `map_row` (probe_orbit.v:60) | LIBRARY COLLISION | `Locate map_row` under `all_ssreflect all_algebra` reports `Constant mathcomp.algebra.matrix.map_row`. `matrix` reaches the instance namespace through `instances/s5/s5_analysis.v:45 Require Export ... matrix` | rename `row_image` or `map_block` |
| B9 | `r4_tbl`, `m6_tbl`, `m6i_tbl`, `tblA`, `tblB`, `setsA`, `setsB`, `psl211_gens`, `r4_inj`, `m6_inj`, `r4_perm`, `m6_perm` as bare globals | NO CURRENT COLLISION, but against the pgl27 table precedent | `grep psl211 .` outside `notes/` returns nothing, so no `psl211_*` name is taken. But every pgl27 table is `Local` or prefixed: `pgl27_group.v:51,52,53,56,57` `Local Definition tr_tbl sc_tbl inv_tbl tr_inv_tbl sc_inv_tbl`, `pgl27_mixing.v:72,156,314` `Local mtbl tbl_index ptbl`, and `pgl27_spectral.v:212,316,330` `pgl27_letter_tbl pgl27_shift_tbl pgl27_cert_lower_tbl`. `psl211_tables.v` declares all five tables bare and non-`Local` | make the tables `Local`, or prefix them `psl211_` |
| B10 | No planned name collides with mathcomp or infotheo, beyond B7 and B8 | CLEAN | `Locate` run under `all_ssreflect all_algebra all_fingroup boolp reals realType_ext fdist proba` returns `No object of basename` for `pcount colour_view orbit_class subset_class orbit_scheme orbit_encode deck_ok heart_set is_heart walkN elem_table count_ok stable_ok design5_ok setsA tblA colour_content colour_law orbit_valid subset_valid orbit_fibre_card inter` | none |
| C1 | `tblA` / `tblB`, `setsA` / `setsB` | POOR | reference.md 13 makes Definitions `snake_case`, and 10 "Avoid Overly Generic Names" rejects labels with no content. `A`/`B` carry no meaning and `psl211_tables.v:5-6` already says what they are: `tblB` is the M12 hexad system, `tblA` its mirror | `psl211_hexad_tbl` / `psl211_mirror_tbl`, and `psl211_hexads` / `psl211_mirrors` for the finsets |
| C2 | `m6i_tbl` | POOR | `i` for inverse is an invented one-letter abbreviation. quality-catalog R8 bans "opaque project abbreviations a mathcomp reader cannot parse". mathcomp spells inversion `V` (reference.md 11, arithmetic table) or `inv`, and the repo uses `inv`: `pgl27_group.v:53,56,57 inv_tbl tr_inv_tbl sc_inv_tbl`, `pgl27_mixing.v:167 pgl27_inv_letter` | `m6_inv_tbl`, or the physical name `milk6_tbl` since spec 3 calls it the milk shuffle |
| C3 | `pcount` | POOR, actively misleading | reference.md 11 "Numeric Condition Prefixes (from ssrnum)": prefix `p` means positive. `pcount` reads as "positive count". Head symbol should come first (reference.md 10) | `count_pattern` |
| C4 | `count_ok`, `stable_ok`, `design5_ok` | ACCEPTABLE SUFFIX, WRONG ORDER, and `5` is baked in | `_ok` is the repo's established suffix for a `vm_compute`d boolean certificate: `deck_ok`, `elem_table_ok`, `code_table_ok`, `word_table_ok`, `mixing_bound_ok`, `gen_ok`. All of them put the thing first, `elem_table_ok`, not `ok_elem_table`. The three probe names put the property first and the table nowhere, and `design5_ok` hard-codes the strength | `tbl_count_ok`, `tbl_stable_ok`, `tbl_design_ok t tbl` with `t` an argument |
| C5 | `colour_content` | GOOD name, bad placement | `snake_case` per reference.md 13, and the repo spells colour with a u (`five_card_exec_colour_view`), so no synonym drift. The problem is A12 and E3, not the name | keep the name, move the function |
| C6 | `psl211_fibres_eq` | POOR | The repo spells the concept `fiber`: `reconstruct/transitivity_privacy.v:311 rho_tuple_fiber_card`, `:13,31,305,307` in prose, and `pgl27_orbit.v:716-718` "classifier fiber". `grep` for `fibre` in any identifier across the live tree returns nothing. The user's one-word-per-concept rule applies. Also `_eq` is not a mathcomp suffix, the equational suffix is `E` (reference.md 11) | `psl211_colour_fiber_cardE` |
| C7 | `uniform_fdistmap_of_fibres` | POOR | Three problems. `_of_` is Lean's connective, not mathcomp's (reference.md 10 gives `condition_mainSymbol_suffixes`). The head symbol of the LHS is `fdistmap`, and reference.md 10 "Head Symbol Goes First" puts it first. `fibres` again | `eq_fdistmap_uniform`, following mathcomp's `eq_card` / `eq_bigr` / `eq_imset` family, with the fibre-card hypothesis |
| C8 | `orbit_fibre_card` | POOR, and redundant, see D1 | mathcomp puts `card` first in exactly this neighbourhood: `card_orbit`, `card_orbit_in`, `card_orbit_stab`, `card_rcoset`. Spelling again `fibre`. And the set it names already has a mathcomp name, `amove` | delete, or `card_amove` |
| C9 | `colour_view_indep_of_laws`, `colour_view_indep_of_fibres` | POOR | `_of_` again. The repo's sibling family suffixes the variant without a connective: `ttrans_view_indep_gen`, `ttrans_view_indep_alldecks`, `ttrans_view_indep_deck` (`transitivity_privacy.v:674,840,900`) | `colour_view_indep_law`, `colour_view_indep_fiber` |
| C10 | `r4_perm`, `m6_perm`, `r4_tbl`, `m6_tbl` | BORDERLINE | Carrier qualifier last is right. `r4` and `m6` are opaque to a reader who has not read spec section 3. The pgl27 counterparts spell the operation: `pgl27_moves`, `pgl27_letter_tbl`, `pgl27_inv_letter` | acceptable if the file header glosses them, better `psl211_rev4_perm` / `psl211_monge6_perm` |
| C11 | `psl211_M`, `psl211_gens`, `psl211_plug`, `psl211_PI`, `psl211_profile`, `psl211_private`, `psl211_card` | GOOD | Exact mirrors of `pgl27_M` (`pgl27_group.v:97`), `pgl27_gens` (`:91`), `pgl27_plug` (`pgl27_scheme.v:95`), `pgl27_PI` (`pgl27_profile.v:54`), `pgl27_profile` (`:117`), `pgl27_private` (`pgl27_scheme.v:57`), `pgl27_card` (`pgl27_mixing.v:545`) | none |
| C12 | `psl211_G`, `psl211_rho` (probe_group.v:51-52) | NO PRECEDENT, wrappers that anchor nothing | pgl27 defines neither. It has only `pgl27_G_pos : (0 < #\|pgg_G pgl27_M\|)%N` at `pgl27_profile.v:61`, and writes `pgg_G pgl27_M` and `@pgg_rho pgl27_M` inline everywhere else. reference.md 24.1 rejects wrappers that only restate an inferable type | probe-only. Do not land them. Land `psl211_G_pos` instead |
| C13 | `subset_valid` | WEAK | `valid` is the framework's word for `ts_valid` and `orbit_valid`, both predicates on decks. Here it is a predicate on a 6-subset | `is_hexad_or_mirror`, or `psl211_block` |
| C14 | Banned vocabulary: apex, gate, posit | CLEAN | `grep -niE "\bapex\b\|\bgate[sd]?\b\|\bgating\b\|\bposit(s\|ed\|ing)?\b"` over the spec and all six probe files returns nothing | none |
| D1 | `orbit_fibre_card` is new work (spec 7 L10, "library object (mathcomp action)") | REDUNDANT. mathcomp already names the set and the coset identity | `mathcomp/fingroup/action.v:198` `Definition amove to A x y := [set a in A \| to x a == y].`, which is the probe's set-builder after delta. `action.v:737` `Lemma amove_act a : a \in G -> amove to G x (to x a) = 'C_G[x \| to] :* a.` With `fingroup.v:745 card_rcoset` the lemma is one tactic line. `grep transporter` over all of mathcomp returns zero hits, so `amove` is the name to use. My own from-scratch version without `amove` took eight lines (`audit-naming/fibre.v`, compiled) | delete L10, or restate as `card_amove` over a general `rT : finType` |
| D2 | `orbit_fibre_card` takes `S : {set {set 'I_12}}` | DEAD ARGUMENT | `audit-naming/fibre.v` compiled: `Lemma orbit_fibre_card_with_dead_S (S : ...) ... Proof. exact: orbit_fibre_card. Qed.` The printed type in `audit-naming/scope.v` is `{set {set 'I_12}} -> forall (to : action G {set 'I_12}) ...`, with `S` absent from the body. Coq issues no warning | drop `S`, and generalise `'I_12` to `rT : finType` |
| D3 | `uniform_fdistmap_of_fibres` (spec 7 L8) is new work | NEW, but its PROVER hint names a lemma that does not exist | `grep -rn fdist_uniform_suppE` over `/Users/cheng-huiweng/Projects/coq/_opam/lib/coq/user-contrib/infotheo` and over `/Users/cheng-huiweng/Projects/coq/infotheo` returns nothing. `probe_bridge.v:40` names it. What exists is the pair `fdist.v:516 fdist_uniform_supp_in` and `fdist.v:520 fdist_uniform_supp_notin`, plus `fdist.v:377 fdistmapE : fdistmap b = \sum_(a in A \| a \in g @^-1 b) p a`. No infotheo lemma states a fibre ratio: `grep` for `fdistmap.*uniform` and `uniform.*fdistmap` across infotheo returns nothing | correct the hint. Split off the missing ratio lemma `fdistmap f (`U HA) v = #\|fibre\|%:R / #\|A\|%:R`, about ten lines by `bigID`, `big1`, `eq_bigr`, `sumr_const`, `eq_card`, then the planned statement is one line |
| D4 | An equal-fibres-to-equal-fdistmap lemma already in the repo | NO | Every repo `fdistmap` plus uniform lemma is an injectivity result: `security/pgg_collusion_bound.v:506,572,725`, `reconstruct/transitivity_privacy.v:275 fdist_uniform_supp_bij` (bijective), `:141 bij_uniform` (bijective). `lib/perm_uniform.v` and `security/pgg_fdist_rV_indep.v` contain zero occurrences of `fdistmap`. `reconstruct/transitivity_privacy.v:591 ktuple_encode_uniform` runs the same `fdistmapE`/`bigID`/`big1`/`eq_bigr`/`sumr_const`/`eq_card` chain inline at `:648-670`, but only for the constant-fibre case and under `k <= t` with transitivity | keep L8, and reuse the chain from `ktuple_encode_uniform` rather than rediscovering it |
| D5 | `rho_tuple_fiber_card` (`transitivity_privacy.v:311`) reproves by hand what `amove_act` gives | PRE-EXISTING REDUNDANCY, noted for the record | `:311` uses the same `[set g in G \| ... == q]` spelling and proves it with `partition_big` plus a coset `card_imset` | out of scope for this instance, worth a separate ticket |
| D6 | Section 6 of `transitivity_privacy.v` already gives the all-decks statement without transitivity, so the new bridge duplicates it | NO. The new bridge is genuinely new | See A11. `rhoG_ntrans` at `:746` is a premise of both `:840` and `:900` | none |
| E1 | Headline `psl211_colour_view_indep` (probe_decomposition.v:164) and the supports are stated over the same objects | NO. Three disjoint object languages | `probe_orbit.v:87` counts ROWS: `pcount (tbl : seq (seq nat)) (C A : seq nat) : nat := count (fun R => inter R C == A) tbl`, over ascending `seq nat`. `probe_decomposition.v:156` counts GROUP ELEMENTS: `#\|[set g in pgg_G psl211_M \| colour_view C (true, g) == v]\|`, over `{set 'I_12}` and `{ffun 'I_12 -> bool}`. `probe_bridge.v:111` is a third language, a generic section over `(N', gT, G, rho, encode, colour)` | see E2 |
| E2 | The ledger covers the link from row counts to element counts (spec 7 rows L10, L11) | NO. Four steps are missing | The identity needed is `#\|{g in G \| view = v}\| = #\|Stab\| * #\|{S in orbit \| S :&: C = A_v}\|`. L10 gives ONE fibre's cardinality. L11 gives orbit equals table set. Neither of these is stated: (i) `#\|setsA\| = #\|setsB\| = 132`, hence by `card_orbit_stab` both stabilisers have order 5, which is what makes the two row counts comparable as element counts. I verified the arithmetic in `audit-naming/order.v`: `size tblA = 132`, `size tblB = 132`, both `uniq`, disjoint, and `132 * 5 = 660`. (ii) the partition step summing L10 over the orbit, mathcomp `partition_big` or `card_partition`. (iii) the dictionary `pcount tblA C A = #\|[set S in setsA \| S :&: C == A]\|`, translating ascending `seq nat` to `{set 'I_12}` through `list_to_set`. (iv) the view-to-pattern dictionary `colour_view C (b, g) == v` iff `(g^-1 @: K_b) :&: C == A_v`. Also unstated: fibres for `v` that are true outside `C` are empty on both sides | add four ledger rows. `card_orbit_stab` (`action.v:989`) supersedes L10's role in (i) |
| E3 | `psl211_plug` with `rp_content := colour_content` is compatible with the framework | NO. The content collapse contradicts the scheme's own validity predicate | `probe_decomposition.v:36` `deck_ok sh := uniq sh`, and `orbit_valid` requires it. `probe_decomposition.v:118` `colour_content` sends twelve codes to two values, so a twelve-entry content tuple is never `uniq`. The downstream consumer `reconstruct/algebraic_rigidity.v:409-425 ar_protocol_correct` demands `ts_valid ... [tuple rp_content ... ]`, an unsatisfiable hypothesis at this plug. The record itself will still build, so L13 would pass while the recovery path goes vacuous | keep `rp_content := id` as every landed instance does, and put the colour collapse in the observer, mirroring `instances/kim2025/five_card_models.v:277` |
| E4 | `probe_bridge.v` compiles | NO. Three defects | (1) `:37` `Error: Cannot infer the implicit parameter R of fdistmap whose type is "realType"` (I compiled a copy). Fix `:> R.-fdist T`. (2) `:63` `'C_G[x \| to]` does not elaborate: no file in the import chain opens group scope. `grep "Import GroupScope\|Open Scope group_scope"` over `transitivity_privacy.v pgg_interface.v pgg_sharing_framework.v covering_scheme.v pgg_monodromy_profile.v pgl27_orbit.v` returns nothing, and `audit-naming/scope.v` compiles a `Fail` on the verbatim statement and a `Check` on the same statement after `Import GroupScope.` (3) `:128-132` the tautology probe is inert, see E5 | fix all three before the prover reports L8, L9, L10 as passing |
| E5 | `Fail Lemma taut ... Proof. reflexivity. Qed.` (probe_bridge.v:128-132) checks that the law equality is not definitional | NO. The idiom cannot express that | `audit-naming/failshape.v` compiled: `Fail Lemma bogus : 1 = 2.` gives `Error: The command has not failed!`, because opening a goal succeeds for any well-typed statement. So the probe passes only when its statement is ill-typed, which is not the property claimed, and the trailing `Proof.` then runs with no goal | use `Goal <eq>. Fail reflexivity. Abort.` or `Fail Example taut : <eq> := erefl.` The `Fail Definition` mutations in `probe_group.v:58-59` are sound and unaffected |
| E6 | `Definition orbit_encode (b : bool) : 12.-tuple 'I_12. Admitted.` (probe_decomposition.v:68) | AXIOM, not a definition | An `Admitted` Definition introduces an opaque constant. Spec 8 says "No new axiom or assumed constant". The probe header licenses it, so this is not a defect of the probe, but L13's "records built from Admitted supports" certifies nothing about the encoder's existence. pgl27 supplies a literal tuple at `pgl27_orbit.v:349` | add a ledger row naming one literal deck per class |

## Blocking findings

1. **E4**, three compile defects in `probe_bridge.v`. Rows L8, L9, L10 cannot be
   reported as passing until the file elaborates. Two of the three are in the
   statements, not the proofs, so the prover filling `Admitted`s will hit them
   immediately.
2. **E3**, `rp_content := colour collapse` against `ts_valid = uniq`. This is a
   design decision, not a typo. Taken as written it makes the recovery path
   through the plug vacuous while L13 still reports green.
3. **E2**, the row-count to element-count link. The spec's headline
   `psl211_colour_view_indep` rests on `psl211_fibres_eq`, and nothing in the
   ledger derives `psl211_fibres_eq` from `count_ok`. Four lemmas are missing,
   including the unstated `#|setsA| = #|setsB| = 132`.
4. **B1 and B2**, nine unprefixed names already taken in `pgg_smc`, two of them
   at a different arity or a different carrier. Landing them as written makes
   two distinct propositions share a name in one namespace.
5. **A6**, the two-letter alphabet option is not available to the landed walk
   machinery.
6. **A10**, the "existing point of contact" in section 5 does not exist. The
   H-cap-C factoring is new work and needs its own rows.
7. **A15**, section 5 and section 7 contradict each other on whether
   2-transitivity is used.
8. **A17**, the chirality claim that carries the paper has no probe and no
   ledger row.

Non-blocking but should be fixed before the plan is written: D1 and D2
(use `amove`, drop the dead `S`), D3 (the PROVER hint names a lemma that does
not exist), C1 through C13, B7 through B9, E5, E6.

## What I compiled

All in `notes/probes/2026-09-14-psl211/audit-naming/`, via
`cd notes/probes/2026-09-14-psl211 && sh run.sh audit-naming/<file>`. Nothing
outside that directory was written.

| file | result | what it establishes |
|---|---|---|
| `collide.v` | green | `Locate` before and after redefinition. Redefining `deck_ok` demotes `pgg_smc.pgl27_orbit.deck_ok` to a qualified name and `Print orbit_valid` then shows both spellings in one body. Evidence for B1 |
| `fibre.v` | green | The probe's L10 statement fails to elaborate under its own preamble. The lemma proves in eight lines from `orbitP`, `card_rcoset`, `mem_rcoset`, `groupMr`, `astab1P`, `actM`, `actK`, `mulgKV`. The `S` argument is dead. `About card_orbit_stab` printed. Evidence for D1, D2, E4 |
| `scope.v` | green | Isolates the cause: `Fail` on the verbatim statement, a passing control with the stabiliser removed, `Fail` on the stabiliser alone, then a `Check` of the same statement after `Import GroupScope.` Evidence for E4(2) |
| `order.v` | green, 8.2 s | Independent `vm_compute` of the numbers the bridge needs and the ledger omits: the two-letter closure of `{r4, m6}` has 660 elements, `size tblA = size tblB = 132`, both `uniq`, `tblA` and `tblB` disjoint, `132 * 5 = 660`. Evidence for E2 |
| `failshape.v` | errors as predicted | `Fail Lemma bogus : 1 = 2.` reports `The command has not failed!`. Evidence for E5 |
| copy of `probe_bridge.v` | errors at line 37 | `Cannot infer the implicit parameter R of fdistmap whose type is "realType"`. Copy deleted after the run. Evidence for E4(1) |
| `t1.v`, `t2.v`, `final.v` | green | Written by the mathcomp/infotheo research agent. One-line `orbit_fibre_card` via `amove_act` plus `card_rcoset`, and the ten-line `uniform_fdistmap_fibre_ratio`. `probe_bridge_patched.v` is its uncompiled patch draft |

Library facts checked by `Locate` and by direct grep against the build's real
libraries at `/Users/cheng-huiweng/Projects/coq/_opam/lib/coq/user-contrib`,
not against the stale `~/.opam/infotheo` tree: `mathcomp/fingroup/action.v:198`
`amove`, `:737 amove_act`, `:989 card_orbit_stab`,
`mathcomp/fingroup/fingroup.v:745 card_rcoset`,
`infotheo/probability/fdist.v:377 fdistmapE`, `:509` the `` `U `` notation,
`:516 fdist_uniform_supp_in`, `:520 fdist_uniform_supp_notin`, and the absence
of `fdist_uniform_suppE`.

Audit of the two research subagents: both ran on Opus. I re-verified their
load-bearing claims myself before writing them down. `rhoG_ntrans` at
`transitivity_privacy.v:746` and the zero count of `:&:` in that file I
re-grepped. `amove`, `amove_act` and the absence of `fdist_uniform_suppE` I
re-grepped. The line-37 error in `probe_bridge.v` I reproduced by compiling a
copy. The claim that the `Fail Lemma` block errors at the trailing `Proof.` I
did not reproduce verbatim, and my own `failshape.v` shows a stronger and
simpler defect in the same idiom.

VERDICT: NO-GO

The spec is not ready for an implementation plan. Eight blocking findings, of
which three are substantive rather than cosmetic: the plug's content collapse
contradicts its own validity predicate (E3), the ledger has no path from the
table certificate to the fibre statement the headline rests on (E2), and the
paper's chirality claim is neither probed nor rowed (A17). Fix those three,
correct the two false precedents (A10, A12) and the internal contradiction
(A15), prefix the nine collided names (B1), and the spec becomes a GO.
