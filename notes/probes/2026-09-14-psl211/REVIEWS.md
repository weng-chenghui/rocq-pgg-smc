# PSL(2,11) instance: per-task review record (Plan A)

Plan: docs/superpowers/plans/2026-09-14-psl211-chirality-instance.md.
Every task ran as implementer (fresh agent) -> spec-compliance reviewer
(Opus, independent, compiles) -> code-quality reviewer (Opus, mathcomp-skills
reference) -> fixes -> re-review. Sonnet implementers (Tasks 2, 5, 10) were
audited by Opus reviewers before their output counted.

| task | file | landed commits | spec review | quality review | assumptions |
|---|---|---|---|---|---|
| 1 | _CoqProject | 93608c3, a997e63 (+ one line per landed file) | n/a | n/a (peer session found pre-registration broke coqdep; fixed) | n/a |
| 2 | instances/psl211/psl211_blocks.v | 4bc5173, f021d50, e96f55b, 0b7dd1e | compliant (tables byte-identical to the probe; python re-derivation of every certificate) | approved after fixes (comments, Local helpers, shape and inverse certificates, widths) | all closed under the global context |
| 3 | instances/psl211/psl211_group.v | bf793ff, 338cdff, deed6dd, 4e8d51f (closure layer) | compliant (20 lemmas closed; mutation checks fail for the right reason) | approved after fixes (prefixed word layer, wapply Local, m6_permVE, comments, imports); closure layer: approved with fixes deferred to Step 2c | all closed |
| 4 | instances/psl211/psl211_orbit.v | 89aa93f, f5916eb, 120b1b8 | compliant (uniq premise on pattern_countE verified as a correction by counterexample) | approved (deprecated enum identity removed; tbl_ok_asc6 bridge exported; Notation reverted after a scheme hang) | all closed |
| 5 | reconstruct/design_privacy.v | f8450b3, 7453c24, registered 1741d47 | compliant (token-identical to probe_bridge.v 25-130 with renames) | approved with three comment rewrites, applied | exactly the three boolp axioms (transitivity_privacy.v floor) |
| 6 | instances/psl211/psl211_scheme.v | a75d6a2, 7d2b18d (+ 0b7dd1e in blocks) | compliant (re-deal keeps codes; #C = 0 branch; no s1 != s2 assumption; perm_of_eq_card absent from mathcomp) | approved after fixes; tidy items R1-R6 on the plan | all closed |
| 7 | instances/psl211/psl211_secrecy.v | 534fab6, 224bf3d | compliant (the leak's non-degenerate-prior premises proved necessary and minimal; route consumes only psl211_count_okT, psl211_card, the 132 counts) | approved after fixes (%N on nat equations under ring_scope; header currency and scope) | boolp trio (section-variable floor); leak_coalition_card6 closed |
| 8 | instances/psl211/psl211_recovery.v | d2af766, 44466cf, c482ad1 | compliant (witness pair (0,2), mutation probes) | approved (set-form psl211_reveal_ambiguous added) | all closed |
| 9 | instances/psl211/psl211_mixing.v | 688e10c, 98612df | compliant (pgl27 walk convention proved; assumption set identical to pgl27_word_mixing) | approved with fixes (psl211_Wuni; header; dedupe via psl211_closure.v); the table-sealing recommendation was refuted by measurement | identical to pgl27_word_mixing (boolp trio) |
| 10 | instances/psl211/psl211_profile.v | 291712e, 6d8e6f5, 2c5bf01 | compliant | accepted after two comment edits (profile_k attribution; profile_eps_psl211) | psl211_profile closed; distributional lemmas boolp trio |

Mathematical corrections found by the process and folded into the plan:
`psl211_pattern_countE` needs `uniq tbl` (Task 4); `psl211_colour_view_dep_k6`
needs a non-degenerate prior (Task 7); the plan's inline milk6 proof body and
the "quarter" wording were wrong (Task 3); `Local Notation` for a BFS
certificate makes downstream `done` walk into the table (Task 4 fix, and the
same trap in Task 9, resolved by keeping certificates as constants and ending
steps with explicit `exact:`).

Step 2c refactor pass (4c08386, 98612df, d9406a2, 6fda271): psl211_closure.v
split, renames, enumeration-lemma consolidation, wording; nine-anchor
assumption table reproduced unchanged; a separate Opus review of the four
commits returned GO with comment-level findings, applied in c7f46f8 and
recompiled green in order. Full build at 6fda271: make -j8 nothing to be done,
exit 0. Axiom sweep: standard axioms only, all ten files.

Final Plan A code HEAD: c7f46f8 (chain green; make -n reports nothing to do).

## Plan B (branch feat/psl211-plan-b, 2026-09-15)

Review: notes/probes/2026-09-15-psl211-planb/REVIEW-planb-mechanical.md (Opus,
independent, read-only, Print Assumptions over the .vo chain). Probe evidence
for the executed cone: notes/probes/2026-09-15-psl211-planb/PROBE-REPORT.md.

| task | file | landed commits | spec review | quality review | assumptions |
|---|---|---|---|---|---|
| B1 | lib/perm_exchange.v | 8c68c7c, ccebf4f (cosmetic fixes) | compliant (landed statement is the pre-move Local one with the `n` binder and fuel premise deleted, conclusion character-identical; ubnP form with the base case written once; mathcomp-only imports, no HB/infotheo/pgg_smc; registered after lib/perm_uniform.v; no exported statement of psl211_scheme.v or psl211_secrecy.v changed, declaration multiset diff) | approved with fixes, all cosmetic (unused `seq` import, measured; redundant `by` before a closing `exact:`; implicit profile `[T] [U V] _` deliberate but undocumented; one `have`/`case/` pair foldable) | perm_onS, perm_of_eq_card and psl211_private all closed under the global context |
| B2 | instances/psl211/psl211_mixing.v | 5032d4a, ccebf4f (comment fix) | compliant (statements and proof scripts token-identical to pgl27_mixing.v 1055-1112 under 6 4 200 -> 10 2 584, 'I_8 -> 'I_12, Wuni -> psl211_Wuni; psl211_profile and pgg_collusion_bound imported, no cycle, every pairwise name intersection of the six imported modules empty; nothing existing changed) | approved with one blocking comment fix: psl211_endpoint_mixing's second sentence describes sigma^-1 s where the lemma is of sigma s, against transitivity_privacy.v:427/:495 (pgl27's original wording was the correct one and stays); var_dist_prodR duplicated Local in two files, hoist to pgg_collusion_bound.v, does not block; var_dist_fdistmap_prod_mix measured to subsume it in seven lines at the same axiom cost, but yields only the inequality | psl211_endpoint_mixing and psl211_joint_mixing exactly the boolp trio; psl211_word_mixing anchor unchanged |
| B3 | reconstruct/design_privacy.v | e7504cb, 943c021 (review fixes) | compliant (uniform_pair_indep_of_fibers and card_fiber_sum character-identical to the plan's C3 statements; 1631-token comment-stripped diff against probe_c3_bridge.v 20-197 empty, so the %N annotations and the four recorded spellings are preserved by construction; discharge confirmed by About: HX at finset.setTfor X not a proper subset, `U resolving to fdist_uniform_supp, one premise not two, Let P and Let c inlined, R HA HX explicit and X G T implicit; _CoqProject a pure reorder, identical 173-line multiset, no duplicate, nine psl211 lines above the manifest in their pre-move order with mixing before recovery, none of the six new paths pre-registered; psl211_secrecy.v the only Require and it still loads with psl211_colour_view_indep unchanged at the boolp trio; eighteen pre-existing declaration lines identical and shifted +3, nothing existing reworded) | approved with fixes, two blocking, both comment accuracy: card_fiber_sum's "every conclusion is a total, and this is the only step between them" is true of one of the four lemmas below it and is contradicted by uniform_pair_indep_of_class crossing at Htot and Hsplit by cardsID, and uniform_pair_indep_of_fibers' "which is false here" asserts a negative the file does not establish where the accurate claim is that the premise does not give it. Seven cosmetic: header Section 3 list omits pair_fibers_class_sizes, all-caps FUNCTION with no precedent in any tracked .v, the inde_prod_fst paragraph is rejected-alternatives narration naming another file, six lines over 80 columns inherited from the probe against zero before the commit, the _of_ connective against the file's colour_view_indep_fibers and uniform_fdistmap_fiberE (observed and declined, the name is plan-pinned), pr_countE's comment naming the discharged Let c, and design-note row C3 still calling the lemma uniform_prod_inde_fiber. Findings 3, 4 and 5 are defects in plan-supplied text at lines 393-395 and 455-475, fix the plan too. Probe framing, trap relocation and the plan's own 79-column header line were all handled correctly | card_fiber_sum and pair_fibers_class_sizes closed under the global context, pr_countE, uniform_pair_indep_of_class and uniform_pair_indep_of_fibers exactly the boolp trio |
| B4 | instances/psl211/psl211_exec.v | cbf46c7, 5b06e02, f231e2b, 30d54ee | compliant (algebra block, seat list and enumeration lemma character-identical to psl211_planb_defs.v:34-43 and :51-61, and a comment-stripped token diff of the whole file shows only the five sanctioned differences: the Step 1b trim, fuel 380 -> 220, psl211_fuel_small and psl211_dealt_params_small deleted, psl211_dealt_terminates added from probe_p1b2_endpoints_small.v:19-21 with the Time wrappers dropped, and the rename; psl211_profileE and profile_k_psl211_algebra both close by []; Print psl211_fuel = 220; recon and terminates character-identical to the plan's Step 1 block; every one of Step 1b's twenty drop candidates gone and pgg_monodromy_profile kept, verified independently (profile_k is a bare Definition at pgg_monodromy_profile.v:101, About resolves the head there, and no Require Export exists anywhere in protocol/ or instances/psl211/ so Import is not transitive), at zero closure cost; closure recomputed from coqdep's own graph is 33 already-landed files, and 35 with exec and endpoints, matching the header as 30d54ee reworded it; the _CoqProject line is at :195 after psl211_mixing.v, which is the Files header's placement, and a12c1d8 removed the plan's contradiction at :679 so the two readings now agree) | approved with fixes, none still blocking after 30d54ee. Three of REVIEW-T2's four file findings applied: the two leftover Local Open Scope lines (fdist_scope, proba_scope) deleted, the "That closure" antecedent reworded verbatim to "The closure psl211_endpoints.v freezes is 35 files, this one included", and the psl211_fuel comment's Used-by-shaped cross-reference replaced verbatim by "decided by reduction, not by counting rounds", which is true since psl211_dealt_terminates is Proof. by vm_compute. Qed. REVIEW-T2's blocking finding 1 is half-applied and downgraded to cosmetic: "at fuel 220" now names the right statement, removing the ambiguity with P1a's fuel-380 pair, but the numbers 0.49 s and 0.47 s still trace to no log (a grep of notes/ and docs/ finds only 0.49u/0.499u user-time components of other lemmas in failed probe runs), the provenance token is still the bare "(measured 2026-09-15)", the landed proof carries no Time so no compile of this file could have produced a per-transaction pair, and neither of REVIEW-T2's two routes (transcribe P1b2's 0.958/0.62, or land a re-measurement file and log and cite it) was completed. One new finding: the header's second cost paragraph at :22-23 quotes P1b2's memory figure as "a 17 GB peak (measured 2026-09-15)", which the T3 compile of the file it describes measured at 10.45 GB. Both defects are header provenance in a file inside the frozen 35-file closure, so both belong to one batched closure edit with psl211_endpoints.v:6-8; a re-measurement for the citation is free today through the probe directory's run.sh, which loads psl211_exec.vo rather than rebuilding it. Verified clean and recorded so a later pass does not undo them: zero lines over 80 after 30d54ee's two re-wraps; the Key-results == column shift is the s5_exec.v:64 precedent; the rename follows reference.md section 10 head-symbol-first after profile_k_s5_algebra at s5_exec.v:358; the cost paragraphs sit in the non-rendered (* *) header box, which is where cost narration belongs; all four extended statement comments accurate | nine declarations, nine Closed under the global context (psl211_players, psl211_players_enumE, psl211_algebra, psl211_profileE, psl211_fuel, psl211_dealt_params, psl211_dealt_recon, psl211_dealt_terminates, profile_k_psl211_algebra); no boolp trio, no axiom |
| B5 | instances/psl211/psl211_endpoints.v | 55933b6 | compliant on all six Task 3 checks. The statement is exactly profile_endpoints_stmt psl211_algebra psl211_fuel, read back from the kernel by About over the .vo as pgg_instance.profile_endpoints_stmt psl211_exec.psl211_algebra psl211_exec.psl211_fuel with no extra argument and no Local premise, and character-identical to the plan's :758-759; the proof is by vm_compute alone, character-identical to the plan's :760 including the two Time markers Step 2 requires, which make the file re-derive two of its three header numbers on every rebuild; a whole-body diff of :30-50 against the plan's :740-760 is empty; Print Assumptions over the .vo chain (rocq repl, not the rocq-mcp tool) returns Closed under the global context, the first reading of this statement at fuel 220 since P1d read it at 380 and probe_p1b2_endpoints_small.v carried none; the four Require lines are the plan's and the closure recomputed from rocq dep's own graph is 34 project prerequisites plus the file, 35 in all, set-equal to plan section 2's list with pgg_sample_adapter and pgg_weighted_words absent and perm_uniform present; the _CoqProject line is at :196 immediately after psl211_exec.v at :195, which is what Task 3's Files header says and Task 3 says it only once; make -n -f Makefile.rocq reports the target up to date, corroborated by mtimes (source 11:40:52, object 12:11:01, log 11:56:05 to 12:11:03) | approved with fixes, none blocking, one material. The header's cost sentence at :6-8 presents all three of its numbers as measurements of this file on 2026-09-15 under /usr/bin/time -l, and all three are probe P1b2's: 561 s and 331 s against this compile's 568.116 and 324.465, inside the plan's ten-percent tolerance, and "a true peak of 17.15 GB" against this compile's 10445357056 bytes, 64% high. Neither figure is a clean peak of this declaration: P1b2's is a true ru_maxrss but of a file carrying a second declaration that reduces in the same process after the 331 s Qed, and OCaml's major heap does not return memory to the OS so peak RSS is monotone; this compile's was taken one process layer further out, /usr/bin/time -l make -j1 against the probe's /usr/bin/time -l sh run.sh which execs rocq compile, and the log shows the attribution through make is partial (468,894,598 instructions retired and 120,076,714 cycles against P1b2's 11.3 trillion and 2.9 trillion for a run of the same length, while the CPU times did propagate), so 10.45 GB is a lower bound and not a 39% reduction. The honest wording is a dated range naming the measurement path for each number, with "true peak" dropped since that is PROBE-REPORT.md's term of art for distinguishing P1b2 from P1b's sampled figure. Classified as a batched closure edit at the end of Plan B rather than a freeze-budget edit now: the error over-states the machine requirement so it cannot cause an unsafe action, plan Step 2 already routes the corrected number to the free T8 as-built record, psl211_exec.v carries the same figure and the same 898 s price so one batch fixes three defects, and no later task rebuilds this .vo. Free now instead: add the as-built triple to design-note ledger row A3 and plan section 2's freeze box, and fix A3's 562 s, which is P1b's fuel-380 tactic number sitting in the fuel-220 row beside P1b2's 331 s Qed. One style nit, optional: the docstring at :43-44 names profile_endpointsE and supplied_endpointsE in the Used-by shape REVIEW-T2 finding 8 removed from psl211_fuel, inherited verbatim from the plan's :753-755, though the names are more load-bearing here since they are the two framework lemmas that carry the statement to the two modes. Verified clean: all 28 header-box lines exactly 80 and no line of the file over 80; the header box is the plan's block plus the date, five lines re-wrapped to absorb it; the cost paragraph is in the non-rendered (* *) box, not a (** *) docstring; the docstring is fact plus position with no status word, no effort estimate and no template slot; 12! is 479001600, fourteen processes is twelve seats plus dealer plus verifier, "562 s and 343 s at fuel 380" is P1b exactly, profile_endpointsE and supplied_endpointsE exist at pgg_instance.v:757 and :767 and carry the statement to dealt_secret_params and supplied_input_params as claimed, and "Definitions: none" is true | one declaration, one Closed under the global context (psl211_profile_endpoints); no boolp trio, no axiom |
| B6 | instances/psl211/psl211_alldecks.v | e4b8b99 | compliant on all six Task 4 checks. The two pinned counting statements are landed at the plan's types (psl211_alldecks_per_cut_count at :1303, psl211_alldecks_fiber_transfer at :1361, same premise #|C| <= 5, same index carriers, same argument order C g v), verified independently of the implementer's canary and then against it: t4_landed_canary.v restates both from the plan's text and closes each by exact:, its .vo (13:21:21) postdates psl211_alldecks.vo (13:20:48) so it was built against this object, and exact: pins the order because g and v have different types; the file's own transfer proof applies the per-cut lemma as (psl211_alldecks_per_cut_count C g v HC5). Comment-stripped token diffs against the probes are empty for all three validity rows (probe_b1_uniq.v:45-271 vs :180-430, probe_b2_heart.v:38-116 vs :432-518, probe_b3_class.v:42-82 vs :520-560, zero tokens each after dropping the mandated Local), and six tokens for C4 after the mandated psl211_ prefix and the C4 -> psl211_ext_count rename, that difference being one tactic line split in two inside psl211_perm_ext_count_ord with the same tactics in the same order. Print Assumptions over the .vo through rocq repl with PSL211_RFLAGS (not the rocq-mcp tool) returns Closed under the global context for all 27 exported declarations, ten Definitions and seventeen Lemmas, including both new counting theorems, which is Step 9's stated defect test passed since both were Admitted in probe_c56_decomposition.v:112 and :128; grep for Admitted, Axiom, Hypothesis and Parameter returns 0. The freeze rule holds exactly: git show --stat touches only _CoqProject (+1, the line at :197 immediately after psl211_endpoints.v at :196, the Files header's placement) and the new file, and the three Local re-proofs are necessary rather than duplicative, since list_to_setK, perm_list_to_set and list_to_set_inj are all Local in psl211_orbit.v at :809, :816 and :842 and no exported lemma of those statements exists anywhere in the tree. Every export carries psl211_ and every one of the sixty-odd unprefixed ad_* helpers is Local, with no collision: a tree-wide grep for the eight new exported names returns zero hits outside the file, and no Imod12 or psl211_fuel_small remains | approved with fixes, none blocking; the file is outside the endpoint closure and recompiles in about 8 s, so all of them are cheap. The two deviations the review brief names are mathematically right. ad_fiber_at_row's ad_ok premise is required because the audit's unconditional form is false in both ways the implementer gives: a reading nonzero off the coalition, or repeating a code at two read positions, is produced by no deal so the left side is 0 at every block line, while the closed form is nonzero at every line meeting the read positions in the pattern; the landed two-branch structure exhausts (ad_fiber_at_row at :1164 under ad_ok, ad_fiber_empty at :1194 under its negation deriving both conjuncts from any member, per_cut_count opening with case: (boolP ad_ok) at :1309), and the premise is class-free so the empty branch gives 0 = 0 on both sides. Step 11's cause is recorded at the site at :1315-1320 and the proof does what the comment says, reaching each side by transitivity through a one-table term and closing with symmetry; exact: Hf, honouring both of the plan's rules since no table-size hypothesis is ever in context and the last tactic is never by or done. Two recording gaps and one missing plan item, all fixable in one pass: plan Step 6 at :1128-1133 asks for the perm_uniform import cost in the file header and the header carries no timing at all; the step-11 comment at :1319 quotes "Killed at 40 s and 3.3 GB (measured 2026-09-15)" while the only 3.3 GB in the Plan B record is AUDIT-PLAN-B.md:37 and :134 and audit_t4_percut.v:121-137, attached to a different failure shape at 280 s and 420 s, and no log under notes/probes/2026-09-15-psl211-planb/ records a 40 s run, so either land and cite the measurement or attribute the memory figure to the audit; and the ad_ok departure from the audit's unconditional hypothesis is nowhere recorded as a departure. Three smaller recording items ride the same compile: the per-cut statement is pinned by closed type and not by source text because the section generalizes C, g and v, which a later text diff against plan:1195-1207 will read as drift; the empty-coalition case moved from the plan's route step 4 into ad_pattern_classE at :1262-1279, an improvement since it never needs the view to be constant, but unrecorded; and "census" at :679, :680, :1002 and :1260 drifts from the file-wide "count". Outside the tracked tree, t4_landed_canary.v:50-52's mutation comment describes a same-class mutant while the code writes a .+1 mutant. Verified clean: zero lines over 80 in 1373 lines; Key results and Definitions both in declaration order; every statement comment fact plus position with no status word, no effort estimate, no template slot and no proof sketch, thirteen pieces of encoding and tactic rationale correctly moved into non-rendered (* *) comments at :91, :154, :161, :224, :260, :282, :368, :451, :475, :557, :652, :1315 and :1330; the three extended statement comments all accurate, including the supplied-layout-mode sentence the plan asks for verbatim and the correct removal of the plan's psl211_models.v cross-reference; no banned word and no status jargon anywhere; no Local helper duplicating an exported lemma anywhere in the repository, the only near-duplicate being ad_inordK, a plan-mandated verbatim carry of a thin restatement of mathcomp's inordK. One naming observation for a later pass, out of scope here because plan:1078-1084 and ledger row C4 pin it: psl211_prescribed_of_set uses an _of_ connective, which is Mathlib grammar rather than the mathcomp mainSymbol_suffixes grammar | twenty-seven declarations, twenty-seven Closed under the global context; no boolp trio, no axiom |
| B7 | instances/psl211/psl211_models.v | bfc8e39 | compliant on all eight Task 5 checks. psl211_alldecks_endpoints read back from the kernel by About over the .vo as pgg_instance.instance_endpoints_stmt psl211_alldecks.psl211_alldecks_params with no extra argument and Arguments x w0, its body the plan's :1658-1661 character for character, and Closed, so the 900 s endpoint equation is named and not re-derived; psl211_alldecks_observed is instance_observed of exactly the three run facts in the framework's own order, pgg_instance.v:366-370 taking three and no fourth, with terminates and recon landed at psl211_alldecks.v:571 and :580; the witness is the pgl27 MkExactWitness ... eq_ind_r shape and About reads it back at ExactWitness (E:=psl211_alldecks_params) (amf_sample psl211_exact_family R idx), so the #|C| < profile_k to #|C| <= 5 coercion went through by conversion, the let carrying no proof step; psl211_alldecks_secret_expectedE ties the secret RV to ex_expected through the dot form of sa_arg and its comment states the vacuity boundary C7a exists for; the conversion-bomb rule holds with no Check, no Timeout, no Fail and no fuel literal anywhere, psl211_exec_rowE and psl211_content_trace both reaching the run through exec_run and psl211_alldecks_fuelE landed separately and instant; the freeze rule holds exactly, git show --stat being two files with _CoqProject at :198 immediately after psl211_alldecks.v at :197. Assumption sweep run over the .vo through rocq repl with PSL211_RFLAGS, not the rocq-mcp tool, one Print Assumptions per declaration, all 32 rather than the plan's 18: every one of the plan's eighteen predictions confirmed, including the two-axioms-and-not-three result for psl211_alldecks_static_obs_funE and psl211_alldecks_exact_viewE against the pgl27 analogy, and audit fold finding 8 confirmed, psl211_alldecks_secret_expectedE carrying the trio despite closing by []. Six of the eight reported deviations are right and recorded; the congr/funext change is right and unrecorded, and the s5 cite is wrong | approved with fixes, none blocking Task 6, ten findings. One correction to the record: the reported 13 Closed / 2 / 17 trio is 12 / 2 / 18 as measured, the file being compliant and the count being what is wrong. One material: the source comment at :319-322 attributes 561 s to psl211_endpoints.v, where log-probe_p1b_endpoints.txt is 561.997 at fuel 380, log-probe_p1b2_endpoints_small.txt is 561.422 at fuel 220 and log-T3-endpoints-compile.txt measured the file itself at 568.116, making this the third carrier of REVIEW-T3 findings 1 and 3 and the only one outside the endpoint closure, so the only cheap one. Two comment-accuracy items: psl211_alldecks_static_obsE is glossed at its i \in C branch only at :196-200 and :53-54 while the lemma is an if ... else ord0 and audit N2 makes that default load-bearing, and the file states the same shape correctly for psl211_exec_content_trace at :305; and the s5_models.v cite at :30 is :105, a docstring line, where the Definition is :106. Three recording items: the funext drop at :475-477 is correct because psl211_alldecks_exec_view_instE is a function equality where pgl27's bridge is pointwise, and is unrecorded in file and plan; the naming note at :29-31 records the precedent without naming psl211_alldecks_sampleP as the spelling departed from, which audit N31 asks for and which psl211_alldecks_sampleP_E at :158 makes a reader want; and the seatT/cardT conversion rationale at psl211_alldecks.v:659-661 did not travel with the notations. Three cosmetic: Key results invert one pair, exec_viewE at :385 listed before secret_expectedE at :375 where all twelve others ascend; cut image at :54 against shuffle image at :199 for the same pgg_rho g i; census at :253 against the file's nine count, which is the drift REVIEW-T4 recorded in psl211_alldecks.v and which commit 099aca0 cleared from that file during this review, leaving :253 the last carrier in the psl211 cone. The review was taken at bfc8e39 and 099aca0 landed at 13:48 while it ran, comment-only on psl211_alldecks.v with every changed line inside a comment, psl211_models.v byte-identical at both commits and psl211_models.vo the same 88209 bytes after the rebuild, so the sweep taken before it stands. Verified clean: zero lines over 80 in 501 lines; Definitions ten names in declaration order matching the ten Definitions; no banned word and no status jargon, no effort estimate, no template slot and no absolute line number in any rendered body; all thirteen cost and encoding rationales in non-rendered (* *) comments at :318, :330, :355 and inside the witness term at :456; all three Local Open Scope lines used; 2 * 132 * 6! * 6! is 136857600 and landed at psl211_alldecks.v:174, the group's 660 landed at psl211_closure.v:224; and every T6 alias this file owes has a landed constant of that role, with the witness correctly absent from the alias surface as it is in pgl27_analysis.v's 45 Definitions and content_trace correctly left to a type pin rather than a value pin | thirty-two declarations: twelve Closed under the global context, two at exactly propositional_extensionality and functional_extensionality_dep, eighteen at the boolp trio |
| B7a | instances/psl211/psl211_models.v (cycle fix) | 851c0a2 | compliant. The cycle is real: pgg_tableau.v:92 Requires pgg_analysis_manifest, so with psl211_analysis in the manifest's Require Export the chain psl211_models to pgg_tableau to pgg_analysis_manifest to psl211_analysis to psl211_models closes. The fix breaks the first link, :94 dropping pgg_tableau and keeping pgg_analysis_status, and removes psl211_exact_witness, the only declaration needing ExactWitness. The witness moves to the row file, which is where the other three instances build theirs: MkExactWitness over instances/ returns pgl27_rows.v:189, s5_rows.v:257 and five_card_rows.v:271 and nothing else. Everything else in the file is comment: 33 insertions and 38 deletions, of which the import line, the header Definitions entry and the twenty-line witness account for the deletions and every other changed line sits inside a comment. Six of the ten REVIEW-T5 findings are applied: the 561 s attribution now names probe P1b2 at 561.4 and the file's own compile at 568.1, matching log-probe_p1b2_endpoints_small.txt at 561.422 and log-T3-endpoints-compile.txt at 568.116; the s5_models.v cite is :106; the funext drop is recorded as a (* *) line naming the real difference, that psl211_alldecks_exec_view_instE is already a function equality where pgl27's bridge is pointwise; the ord0 default is added to both carriers of the static_obsE gloss; census is now count and grep -c census is 0; and the Key-results inversion is swapped | approved with fixes, none blocking Task 7. Four REVIEW-T5 findings remain open and the commit message does not say which: #5 the naming note still does not name psl211_alldecks_sampleP as the spelling departed from, #6 the seatT/cardT conversion rationale still did not travel from psl211_alldecks.v:659-661, #8b cut image at :58 against shuffle image at :202 for the same pgg_rho g i, and #10 cut_distE's comment still ends on a spectral arm that does not exist. One new item: the two (** *) bodies at :379 and :438 name psl211_rows.v, a file that does not exist at 943c836, which is layout narration plus a forward reference in a rendered body where the header's own (* *) box at :36-40 already carries the fact. All five sit in one file and should land as one comment-only commit after Task 7, since touching this file rebuilds the facade, the manifest, the client, the pgg_tableau pair and the row files. Zero lines over 80; no banned word | not re-swept; the file's own set moves only by the removal of psl211_exact_witness, which REVIEW-T5 measured at the boolp trio, taking the T5 tally of 12 Closed / 2 / 18 trio to 12 / 2 / 17 over 31 declarations. The twelve aliases of this file that the facade exposes were measured through the facade in B8 |
| B8 | instances/psl211/psl211_analysis.v + manifest + client + scripts | 9904cfe, 943c836 | compliant on all eight Task 6 checks. The facade is twenty-one Definitions, all at column 0, grep -c "^Definition" 21 against zero indented, which is the rule audit #28 folded into the plan and which profile_facade_check.sh:258-259 enforces with an ^Definition anchor whose strip_comments preserves column position on purpose; every body is the landed constant of the plan's :1956-1991 list name for name and target for target, with the @ on exactly the marked entries, the section-7 TransferStatus ascription present, and no proof body anywhere; retention follows pgl27_analysis.v:304-363 exactly, two value-level erefl pins on the two program-layer aliases and seven spelled type ascriptions, one per section, all Timeout-guarded. Manifest: all six edits landed, the Require Export wrapped to two lines for width, the ninth documented row block at :564-632 with row 1's sixteen fields plus the four-column capability table plus the level justification, the row Definition at :812-823 character for character with the plan and landing after s5_row_word rather than inside its docstring which was audit-fold blocking finding 3, the twelve-card checker block at :1614-1736 grouped by the seven pgl27 dividers with a Check for all twenty-one aliases every one of which is a spelled type ascription except the section-7 erefl on a status constructor, so no Check compares two run facts and none compares two observed executions, the no-capability row, the five pins matching the eight existing rows field for field with none forcing apr_observed, and the unit-indexed family application beside s5's. Client: eight Checks, seven sections plus the typed family, section 7 represented by the typed status as :11-13 prescribes, plus the observed value, the four remaining distinct observers, the row Check, and all six count sites. Scripts: both tables landed, the shell script read and not run because it resolves the repository as checkout/.. and prints fatal: not a git repository here, the python regression run twice into the scratchpad at 18 PASS and 0 failed, confirming the implementer's 18/18. Freeze rule holds: none of the five touched .v files is in the 35-file endpoint closure. All five reported deviations are right and four need no action; pgg_instance in the exported vocabulary is load-bearing, ex_expected being a field at protocol/pgg_instance.v:302 that the manifest spells unqualified at :1697 | approved with fixes, none blocking, six findings, none requiring a file edit before the full build. One manifest line: the no-capability table lists cut_distE but not content_traceE or exact_coalition_distE, all three being the same bridges field of row 9 and none in a capability line, inherited from the plan's enumeration and harmless because the checker block pins all twenty-one aliases anyway. One record item: the implementer's assumption summary says correctness is Closed where section 5 splits, observed_recovers Closed and secret_expectedE at the trio, which REVIEW-T5 A5 and audit-fold finding 8 both predicted structurally. One placement note: _CoqProject:200 lands after psl211_recovery.v rather than immediately after psl211_models.v, satisfying the Files line literally and mattering to nothing since make derives order from coqdep. Verified clean: zero lines over 80 in all four files; no apex, gate or posit in any form; no status marker, effort estimate, template slot or absolute line number in any rendered body; the header is pgl27's with the dealer paragraph added and the plan-task token phase-H1 dropped, which is an improvement on the precedent; the all-decks dealer is named in four places and never drifts; the capability table uses the closed notion vocabulary, exact privacy for both privacy lines; the missing-premise field says none with its reason as the Absent-capabilities preamble demands of a StaticExecutedOnly row; one word per concept holds across the block | psl211_row_alldecks exactly the boolp trio, measured rather than by analogy, which the audit fold recorded as unmeasured at #56; the twenty-one aliases split nine Closed under the global context and twelve carrying an Axioms block, seven of the twelve read back as exactly the trio and five captured only as present |
