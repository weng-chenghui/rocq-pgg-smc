## Findings

| # | Claim (spec location) | Verdict | Evidence | Fix |
|---|---|---|---|---|
| 1 | C2, every table row converts to a PGL group element and an outside permutation is rejected (`design.md:143`) | GO | `table_group_bridge.v:96-100` proves `pgl27_table_perm_mem` for every indexed row. `table_group_bridge.v:104-139` defines the transposition of positions three and four and proves `pgl27_outside_permN`. Rocq MCP reports `pgl27_outside_permN` closed under the global context. | None. |
| 2 | C4, the nat deal agrees pointwise with `orbit_encode` and a changed deal is rejected (`design.md:145`) | GO | `table_group_bridge.v:186-190` proves `pgl27_code_deal_orbit_encodeE` for both secrets and every position. `table_group_bridge.v:194-202` proves `pgl27_changed_dealN` for the alternative swap at position two. Rocq MCP reports `pgl27_changed_dealN` closed under the global context. | None. |
| 3 | C6, listed coordinates determine the masked view and repeated entries are harmless (`design.md:147`) | GO | `view_collision_probability.v:23-40` proves the equivalence for arbitrary sequences without a uniqueness premise. The four representative corollaries are at lines 69-113. The mutation at lines 115-156 omits a masked coordinate from the restriction and proves the weakened restriction cannot determine that coordinate. | None. |
| 4 | C8, the actual `pgl27P` ambiguous-event probability equals the census collision ratio (`design.md:149`) | GO | `ambiguous_probability.v:482-525` expands the actual `pgl27P` distribution and proves the generic probability equation. The representative equations are `pgl27_ambiguous_probability_harmonicE`, `pgl27_ambiguous_probability_equianharmonicE`, `pgl27_ambiguous_probability_fiveE`, and `pgl27_ambiguous_probability_sixE` at lines 530-578. The nearby non-definitional and changed-numerator checks at lines 584-608 make the result sensitive to its event and count. | None. |
| 5 | C8 and C10 use the same ambiguity event (`design.md:149-152`) | GO | `probe_view_definitions.v:14-31` defines `pgl27_code_coalition`, `pgl27_view_codes`, and `pgl27_ambiguous_views` once. Both probability and entropy files import these definitions. `posterior_entropy.v:303-311` identifies its support predicate with membership in that shared set. Rocq MCP printed the harmonic C10 theorem with right-hand side `(v \\in pgl27_ambiguous_views R rep_harmonic)%:R`, matching C8's event. | None. |
| 6 | C10, every reachable posterior entropy is exactly zero or one (`design.md:151`) | GO | `posterior_entropy.v:314-328` proves the generic reachable-view result from per-secret injectivity. The four conditional injectivity results are at `view_collision_probability.v:251-308`. The four representative entropy theorems are at `posterior_entropy.v:338-390`, and `headline_connection.v:24-84` restates them directly. The premise requires nonzero view probability, so unreachable views cannot make the claim vacuous. Rocq MCP reports only the standard boolp assumptions for the harmonic headline theorem. `posterior_entropy_mutations.v:18-57` supplies separate witnesses showing that injectivity and reachability premises matter. | None. |
| 7 | C11 composes the probability and posterior results for all four representatives (`design.md:152`) | GO | `headline_decomposition.v:33-47` applies `mutual_info_binary_ambiguityE` to the shared event. Lines 52-99 derive the harmonic, equianharmonic, five-card, and six-card mutual-information equations from the matching C8 and C10 results. Rocq MCP printed the harmonic type as `I = 1 - pgl27_collisions rep_harmonic / 336`. Its assumptions are the standard boolp assumptions plus `pgl27_secret_uniform`. An independent compile exited 0 and printed the same assumptions for all four equations. | None. |
| 8 | Only the allowed uniform-secret support is admitted (`design.md:178-181`, `design.md:223-224`) | GO | A search over all nine probe `.v` files finds exactly one `Admitted`, at `headline_decomposition.v:28`, for `pgl27_secret_uniform` declared at lines 26-28. No other `Admitted`, `admit`, `Axiom`, or `Abort` occurs in the probe sources. MCP assumptions on the C2, C4, C10, and concrete-real results do not include this support. It occurs only in C11's decomposition equations. | None. |
| 9 | C12 has a concrete standard-real witness (`design.md:51-53`, `design.md:153`) | GO | `concrete_real_instance.v:3` imports MathComp's `Rstruct`. Lines 26-35 instantiate the harmonic probability equation at `Rdefinitions.R`. Lines 40-54 instantiate the harmonic posterior equation at the same carrier. Rocq MCP checks both theorem types and reports the standard `Rstruct` classical-real assumptions listed by the spec. An independent compile exited 0 and printed those same assumptions. | None. |
| 10 | The first version covers exactly four representatives and does not claim transport to all coalitions (`design.md:26-40`, C14 at `design.md:155`) | GO | The public C8, C10, and C11 results are each stated only for `rep_harmonic`, `rep_equianharmonic`, `rep_five`, and `rep_six`. No probe theorem quantifies over every coalition of sizes four through six or silently derives an orbit-invariance result. | None. |
| 11 | The connected artifacts are consistent after the ordered rebuild (`design.md:226-227`, acceptance at `design.md:233-238`) | GO | Root's ordered `make -f Makefile.rocq -j1` completed with exit 0. This audit did not rebuild any upstream `.vo` alone. It independently compiled the two final connected files, `headline_decomposition.v` and `concrete_real_instance.v`, with the probe-local `_CoqProject` mappings and the specified opam switch. Both exited 0. Upstream artifact timestamps through `headline_connection.vo` remained earlier than these two final outputs. | None. |

## Blocking findings

None.

## What I compiled

- Root supplied an ordered `make -f Makefile.rocq -j1` result with exit 0 for all nine probe files, including C12.
- I independently compiled `headline_decomposition.v` with every `-R` and `-Q` entry from the probe-local `_CoqProject` and opam switch `/Users/cheng-huiweng/Projects/coq`. It exited 0 in 6.89 seconds. All four mutual-information equations printed exactly the allowed uniform-secret support assumption and the standard boolp assumptions.
- I independently compiled `concrete_real_instance.v` with the same mappings and switch. It exited 0 in 6.94 seconds. Its two wrappers printed the standard `Rstruct` classical-real assumptions acknowledged by the spec.
- Before considering the final compile evidence, Rocq MCP checked the theorem types and assumptions for `pgl27_outside_permN`, `pgl27_changed_dealN`, `pgl27_reachable_view_entropy_harmonic_memE`, `pgl27_mutual_info_ambiguity_harmonicE`, `pgl27_ambiguous_probability_harmonic_realE`, and `pgl27_reachable_view_entropy_harmonic_realE`.
- I did not run `make -q` because active Rocq MCP `pet` processes were present. The two final-file compiles establish consistency without changing upstream dependency timestamps.

VERDICT: GO
