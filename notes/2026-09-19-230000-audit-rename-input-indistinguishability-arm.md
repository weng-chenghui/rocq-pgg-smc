# Adversarial audit of bf42b4d — arm rename SpectralDecay -> InputIndistinguishability

Read-only. Comment prose only. No file edited, nothing compiled, no make run.

## Verdict

**NO-GO as it stands.** One clause of prose was silently deleted by the rewrap
(F1) and one sentence attributes a secret-independence claim to the arm, which
the arm does not make (F2). Both fixes are one edit each. Everything else in
the commit's ~46 rewritten passages checks out against the declarations, the
nine remaining "spectral number" sites all trace to a genuine eigenvalue bound,
no site calls the one-cut row's exact 1/50 spectral, no banned vocabulary
appears, and the arm's prose name is used consistently file-wide.

Method note. Besides reading each declaration, I ran a word-level diff of the
comment prose of all seven files between `bf42b4d^` and `bf42b4d`, after
applying the identifier map. That diff shows exactly one substantive deletion
(F1) and otherwise only the expected rename substitutions, so no other passage
lost or gained content.

## Findings

| id | sev | file:line | quoted text | problem, with evidence | replacement |
|----|-----|-----------|-------------|------------------------|-------------|
| F1 | MUST | instances/psl211/psl211_spectral_constancy.v:78-80 | "The psl211_blockline1_ prefix names block line one and the comparison with block line zero at one chirality, as psl211_perdeck_ of chiralities at one deal." | The rewrap deleted the clause "instances/psl211/psl211_models.v names the comparison between the two". `git show bf42b4d^:instances/psl211/psl211_spectral_constancy.v` carries it; the word-level prose diff reports this as the commit's only substantive deletion. The sentence is now ungrammatical and the pointer to psl211_models.v is lost. | Restore, rewrapped: `(* secret. The psl211_blockline1_ prefix names block line one and the         *)` / `(* comparison with block line zero at one chirality, as psl211_perdeck_ of    *)` / `(* instances/psl211/psl211_models.v names the comparison between the two      *)` / `(* chiralities at one deal. In a proof script a leading C is a fiber          *)` |
| F2 | MUST | manifest/pgg_tableau_syntax.v:25-26 | "the ideal cut, the mixing distance and the ideal's secret-independence as five named things rather than one record" | The fifth component `Hk` of `mk_indistinguishability` (same file, 153-159) has type `forall R idx C, (#|C| < profile_k (instance_profile (projT1 x)))%N -> forall y y' : ex_inputT (projT1 (projT2 x)), fdistmap (static_coalition_obs C y) (u R idx) = fdistmap (static_coalition_obs C y') (u R idx)`. It is constancy in the whole run argument, below the threshold, not independence from a secret: `IndistinguishabilityCert` has no secret field, unlike `ExactWitness` with `ew_secret` (pgg_tableau.v:127-135). psl211_spectral_constancy.v:192-199 states the difference explicitly: "where the run argument carries data outside the secret as well ... the field asks for constancy in that data too and is stronger than the privacy the instance claims." Also synonym drift inside this file: the declaration comment at 137-141 calls the same component "the constancy of a coalition's view of the ideal cut in the run argument". | "the ideal cut, the mixing distance and the constancy of a coalition's view of the ideal cut in the run argument as five named things rather than one record" |
| F3 | SHOULD | manifest/pgg_tableau.v:27-29 | "while the input-indistinguishability arm concludes a variation distance between the readings of two run arguments, bounded by the certificate's mixing epsilon" | The proposition a row carries is `PortProp c p` = `IndistinguishabilityPropAt cert (odflt (cert_eps cert) (c R))` (378-384), and `cert_eps cert := sw_bound_eps (ic_b cert) + sw_bound_eps (ic_b cert)` (360-363). The mixing epsilon is `sw_bound_eps (ic_b cert)`, the number in `ic_close : var_dist (sw_rho_dist ic_b) ic_ideal <= sw_bound_eps ic_b` (151). The bound is that epsilon **twice**, and `RepricePayload` (652-659) forces any repriced number to equal `cert_eps cert`, so the factor two is never removed. The factor is load-bearing: psl211_spectral_constancy.v turns eps >= 1/1320 into a published 1/660 by it. | "while the input-indistinguishability arm concludes a variation distance between the readings of two run arguments, bounded by the certificate's mixing epsilon taken twice, one for each argument" |
| F4 | SHOULD | instances/kim2025/five_card_rows.v:40-44 | "Each certified row is written in both of the forms the tree uses, once at the certificate's own spectral number and once repriced to the constant a text quotes, two to the minus thirty-ninth for the repeated row and one twenty-fifth for the one-cut row." | The second form is not the first form's certificate repriced; it is a different certificate. `five_card_row_repeated39` (781-786) carries `kim_centi_cert40`, whose marginal bound is the constant 2^-40 (five_card_mixing.v:507-512), while `five_card_row_repeated_indistinguishability_tableau` carries `kim_centi_cert` at sqrt 5 * (1/80)^7; `kim_centi_cert_eps_lt` (690-698) proves `cert_eps (kim_centi_cert R idx) < 2%:R ^- 39` strictly, so the two forms publish different numbers. For the one-cut row the second number is strictly smaller: `five_card_row_biased_inv25` carries `kim_biased_cert_exact` at the exact 1/50 (five_card_mixing.v:540-546), published as 1/25 = 0.04, against sqrt 5 / 40 = 0.0559 for the certified tableau. `conclude`'s obligation is an equality, `cert_eps cert = odflt (cert_eps cert) (c R)`, so a reprice can only rename a certificate's own number and never change it. | "Each certified row is written in both of the forms the tree uses, once at the certificate's own spectral number and once at a second certificate whose marginal bound is a constant, the row then publishing that constant twice under one name, two to the minus thirty-ninth for the repeated row and one twenty-fifth for the one-cut row." |
| F5 | SHOULD | instances/psl211/psl211_spectral_constancy.v:4-7 | "psl211_spectral_constancy: the input-indistinguishability certificate's constancy field at the twelve-card chirality instance, refuted in both run modes" | No sentence is false, and the opening paragraph (9-16) is exact against the record. The mismatch is the file name: it is now the file's only "spectral" token, and the arm it describes holds no spectral object. `IndistinguishabilityCert` (pgg_tableau.v:145-156) has fields ic_b, ic_Hd, ic_ideal, ic_close, ic_const, with no eigenvalue and no gap. A reader sent here from psl211_rows.v:43 arrives at a path naming a proof method the file never uses, and the header cannot explain the name without the meta narration the project's comment rule bars. | Keep the header line as written and rename the file in a follow-up commit, e.g. `psl211_constancy.v`, updating `_CoqProject` and the cross-reference at instances/psl211/psl211_rows.v:43. If the name must stay, no header sentence repairs it: the defect is in the name, not the prose. |
| F6 | NOTE | manifest/pgg_tableau.v:140-141 | "and the constancy of a coalition's reading of the ideal cut in the run argument" | `ic_const` (152-156) is quantified only over `C` with `(#|C| < profile_k (instance_profile A))%N`. The sentence says "a coalition" with no restriction. NOTE and not MUST because the file header at line 24 does say "Both arms speak only of a coalition below the privacy threshold". | "and the constancy, at every coalition below the privacy threshold, of the reading of the ideal cut in the run argument" (the phrase already used at five_card_rows.v:560-562 and psl211_spectral_constancy.v:192-193, so it adds no new wording) |
| F7 | NOTE | instances/kim2025/five_card_rows.v:540-541 | "The exact one-cut distance of kim_one_cut_centiE, one fiftieth, is under the spectral bound the certificate publishes, sqrt 5 over eighty." | Two small mismatches. (i) `kim_biased_exact_le_eps` (543-544) is `1 / 50 <= sw_bound_eps (kim_biased_marginal_bound R)`, non-strict, while "is under" reads strict. (ii) sqrt 5 over eighty is the certificate's marginal-bound epsilon, not what a row publishes; seven lines above, the same file writes "A row built on it publishes twice that". The spectral attribution itself is sound (see the coverage list). | "The exact one-cut distance of kim_one_cut_centiE, one fiftieth, is at most the bundle's spectral number sqrt 5 over eighty, which the one-cut certificate carries in its marginal-bound field. The certificate therefore overstates the distance it certifies by about two fifths, and the gap is the price of quoting the bundle's number rather than the exact one." |
| F8 | NOTE | manifest/pgg_tableau.v:575-577 | "give the two-argument variation bound at the certificate's own epsilon" | The statement is `indistinguishability_tail ... : IndistinguishabilityPropAt cert (cert_eps cert)`. "the certificate's own epsilon" also reads as `sw_bound_eps (ic_b cert)`, which is half of it. The file does define `cert_eps` as "A certificate's own bound" (357-359), so the phrase is defensible on its own, but together with F3 the reader meets two sentences pointing at two different numbers in the same words. | "give the two-argument variation bound at cert_eps, the certificate's marginal-bound epsilon twice" |
| F9 | NOTE | instances/psl211/psl211_spectral_constancy.v:251-252, 670-671, 721, 949-950 | e.g. "differently at the two chiralities of the deal psl211_perdeck_deal. The run / argument of this / mode is a whole deck description" | Cosmetic. Four comments were rewrapped by inserting the longer arm name and leaving orphan short lines instead of reflowing the paragraph. Same at 670-671 ("shuffle law is the"), 721 ("instances/psl211/psl211_profile.v"), 949-950 ("The dealt run"). | Reflow each of the four paragraphs to the file's column. No wording change. |

## Checked and correct (coverage)

**manifest/pgg_tableau.v**
- 22-30 header on the two certify statements: the arms are not comparable, the exact arm concludes independence unconditionally at every real field. Matches `ExactProp` (318-338), which has no epsilon. Correct except F3.
- 37-39 "indistinguishability_tail feeds the certificate's cut-carrier distance and its ideal constancy to var_dist_fdistmap_transfer" — matches the proof at 583-589 (`apply: var_dist_fdistmap_transfer`, then `ic_close`, then `ic_const`).
- 44-47 "only conclude returns a tableau ... moves only the real the input-indistinguishability arm's proposition mentions" — matches `conclude`/`PortProp`: the exact branch carries no numeric parameter.
- Table entries, all identifiers verified to exist at the exact spelling: IndistinguishabilityCert (Record 145), certify_indistinguishability (612), indistinguishability_tail (580), view_indistinguishability_of (745), plus the untouched restate (692), PublishedRowAt (702), tableau_left_unit (469). Descriptions match the declarations; "certify_indistinguishability == the statement adjoining an input-indistinguishability certificate" uses the same shorthand as the certify_exact entry, which likewise elides that the payload is one certificate per field and index.
- 158-162 `SecurityPort`: "a row certifying input indistinguishability asserts nothing about mutual information" — `IndistinguishabilityPropAt` (346-354) contains no `I(...)` term. Correct.
- 341-345 `IndistinguishabilityPropAt`: "below the threshold, two run arguments give coalition readings of the cut within variation distance c. The bound is a parameter rather than the certificate's own sum" — matches the `<= c` type and PortProp's `odflt`.
- 137-144 the record comment's field-by-field reading, and "Five fields and not the two of a marginal bound alone" — correct against 145-156 (see F6 for the one omission).
- 544-549 `IndistinguishabilityPayload`; 605-612 `certify_indistinguishability`; 650-654 `RepricePayload` (`unit` for the exact branch, one identity for the other); 656-661 `conclude`; 742-746 `view_indistinguishability_of`, whose body `proj2 (published_thm r)` is literally the body of `view_secrecy_of` (737-739), so "the same projection" is exact.

**manifest/pgg_tableau_syntax.v**
- 67-68 "The tokens inputs, terminates, publish, vm_compute, ExactIndependence and InputIndistinguishability follow a literal and stay identifiers" — the notations at 378-392 do place `'InputIndistinguishability'` after the literal `'certify'`, and the constructor of that name exists at pgg_tableau.v:166, so it stays usable by name.
- 82-84 table `mk_indistinguishability` — Definition at 145.
- 137-143 the declaration comment's five components match b / Hd / u / Hc / Hk, and "the fourth component is the only inexact one, and the fifth is exact" matches `Hc` being the `<=` and `Hk` an equation.
- 256-260 `ExactLeakAt`: "It attaches to the exact arm alone, so a program certifying input indistinguishability carries no such claim" — the definition takes an `ExactPayload`.

**instances/kim2025/five_card_rows.v**
- 30-39 what the arm certifies at these rows: for every coalition of at most one of five seats and every two committed pairs, the two readings are within the published number, both pushed along the same `sa_cut_dist`. Matches `IndistinguishabilityPropAt` at profile_k = 2. The ideal is `sa_cut_dist (five_card_sample R)`, which five_card_mixing.v:374-379 calls the uniform rotation law. The disclaimers ("not independence of the reading from the secret", "conditional on a coalition of fewer than two seats", "says nothing about the full reveal") are all correct.
- 61-63 the s5 cross-reference: instances/s5/s5_rows.v:64-73 does claim the constancy fails at every ideal and for a reason no proof can remove, so "there the constancy an input-indistinguishability certificate asks for is false" reports the s5 header faithfully. s5 argues it in prose and compiles no refutation, which that file says itself.
- 97-99 "kim_centi_cert, kim_biased_cert == each Kim row's certificate at its own bundle's spectral number" — both marginal bounds come from `fc_kim_security_bundle`, whose epsilon is `Num.sqrt 5%:R * (kim_lambda2 eps) ^+ L`, and `kim_lambda2 := 5%:R / 4%:R * `|eps|` is the second-largest eigenvalue modulus (five_card_kim.v:372-373), with `kim_spectral_convergence` the convergence theorem. Genuinely spectral.
- 103-108 and 133-140: all six renamed table identifiers exist — five_card_row_repeated_indistinguishability_tableau (598), _biased_ (608), _rowE (619, 625), _publishedE (641, 654).
- 440-443, 452-455, 520-524: the three cross-references to the renamed certified programs; each names a program that exists and the surrounding claim is unchanged.
- 556-563 `kim_centi_cert` and 565-569 `kim_biased_cert`: the listed fields match the constructor arguments in order, and "the same two terms as in the repeated row's certificate" is exact — both certificates pass `sa_cut_dist (five_card_sample R)` and `@five_card_static_obs_const R`.
- 591-597, 604-607 the two certified tableaux; 614-618 and 624 the rowE lemmas; 630-636 the Fail; 639-640 and 652-653 the publishedE lemmas. All match their statements, and the IdealFinite justification is flagged as a claim against a stated criterion rather than a checked fact, which `publish` bears out.
- 666-670 `five_card_pow2_39_split`: "An input-indistinguishability certificate publishes its marginal bound twice, once for each of the two committed pairs" — exactly `cert_eps`.
- 680-682 and 699-701: "twice the bundle's spectral number at word length seven" and "at word length one, sqrt 5 over forty" — `kim_centi_cert_epsE` and `kim_biased_cert_epsE` give sqrt 5 * (1/80)^7 twice and sqrt 5 * (1/80) twice, and 2 * sqrt 5 / 80 = sqrt 5 / 40. Arithmetic and spectral attribution both correct.
- 729-735 `kim_centi_cert40`: "two to the minus fortieth in place of the spectral expression" — `kim_centi_marginal_bound40` (five_card_mixing.v:507-512) carries the constant 2^-40.
- 797-801 `five_card_row_biased_inv25`: "at the same transfer status as the row at the spectral number" — the row at the spectral number is five_card_row_biased_indistinguishability_tableau at sqrt 5 / 40, and both publish IdealFinite.
- **Specifically checked and clean: no site calls the one-cut row's exact 1/50 spectral.** `kim_biased_cut_mixing_exact` and `kim_biased_marginal_bound_exact` derive from `kim_one_cut_centiE`, a direct computation, and the file calls them "the one-cut row's two exact per-card-position numbers" (790-792) and "the exact number one fiftieth" (774). The eight "spectral" tokens in the file all point at the `fc_kim_security_bundle` epsilon.

**instances/pgl27/pgl27_rows.v**
- 48-49 table entry for pgl27_word_cert.
- 153-156 `pgl27_static_obs_funE`: "the arm compares two laws obtained by pushing a reader forward along a distribution on cuts, so it needs the reader as one function" — matches the two `fdistmap (static_coalition_obs C _) (sa_cut_dist sa)` terms.
- 221-225 `pgl27_word_view_const`: "the half of the input-indistinguishability arm that spends no mixing bound" — the lemma is an equality of pushforwards at the uniform ideal, so it costs nothing.
- 240-251 `pgl27_word_cert`: the five fields match; `pgl27_word_mixing` (pgl27_mixing.v:1048-1051) is `var_dist (rho_from_words_weighted ... 200 ...) (`U pgl27_G_pos) <= 2%:R^-40` with no hypothesis, so "an unconditional theorem about the walk whose bound is 2^-40" holds; the ideal field is `U pgl27_G_pos`; `pgl27_view_law_const` (pgl27_word_privacy.v:168) is an equality. "the only inexact quantity anywhere in this row is the walk's 2^-40" holds.
- 578-583 the Fail comment on the two arms' projections: `ExactProp` (pgg_tableau.v:321-322) takes C then the cardinality proof, `IndistinguishabilityPropAt` (349-351) takes C, x, x' then the cardinality proof, so the rejection happens at the position described; and for this instance `ex_inputT` is the Boolean secret, so "a distance between the readings of two secrets" is right.

**instances/psl211/psl211_rows.v**
- 40-44 "Not claimed. An input-indistinguishability row for this dealer. A certificate's constancy field asks that a coalition below the privacy threshold read the ideal cut the same way at every run argument" — exactly `ic_const`.

**instances/psl211/psl211_spectral_constancy.v**
- 9-16 the opening paragraph: five fields, the fifth `ic_const`, "a coalition of fewer than profile_k seats read the certificate's ideal cut the same way whatever the run argument" — verbatim the type at pgg_tableau.v:152-156.
- 33-42 the quantitative paragraph, 1/1320 and 1/660 — consistent with `psl211_alldecks_no_small_eps_cert`'s type and with `cert_eps` being the epsilon twice.
- 55-63 the "Not claimed" paragraph: the arithmetic checks. The variation distance of the uniform law on {perm 'I_12} from `U psl211_G_pos` is 660 * (1/660 - 1/12!) + (12! - 660)/12! = 2 * (1 - 660/12!), and infotheo's `var_dist`, the sum of absolute differences, is bounded by 2. The paragraph labels itself argued and not compiled and names the one compiled premise.
- 95-96 key-results entry — Lemma `indistinguishability_cert_reading_constancy` at 205.
- 192-199 `coalition_reading_constancy`: matches the Definition, including the point that the run argument may carry data beyond the secret.
- 201-209 the restatement lemma: "is the fifth field of IndistinguishabilityCert read at the certificate's own ideal" — the proof is `exact: ic_const cert`.
- 248-256 `psl211_alldecks_constancy_false`; 665-672 `psl211_alldecks_cert_ideal_close`, whose statement is `var_dist ((`U psl211_G_pos)) (ic_ideal cert) <= sw_bound_eps (ic_b cert)`, matching the comment; 694-710 `psl211_alldecks_no_small_eps_cert`, where "an epsilon of at least 1/1320, the value 1/1320 itself not excluded" is exactly the contrapositive of `2 * eps < 1/660 -> False` at #|pgg_G psl211_M| = 660; 715-718 the source comment naming `IndistinguishabilityPropAt`; 717-725 `psl211_alldecks_no_zero_eps_cert`; 769-772 "no ic_Hd through which a certificate's ideal could be held near it"; 946-953 `psl211_dealt_constancy_false`.

**instances/s5/s5_rows.v**
- 54-73 "two of the five parts of an input-indistinguishability certificate are out of reach at this instance" — the record has five fields, and the three available and two missing are each named and correctly identified. The rewrite also fixed the article, from the left text's "a SpectralDecay certificate".

## Cross-cutting checks

- **Statement-comment discipline.** No meta narration, status marker, effort estimate or proof strategy was introduced. The word-level prose diff shows nothing added anywhere beyond the rename phrases, so the commit introduces no "renamed", "formerly", or "now called".
- **Banned vocabulary.** Zero hits for the project's banned vocabulary list across all seven files (word-boundary search; longer words that merely contain a listed word were excluded).
- **One word per concept.** The arm is called "input-indistinguishability arm" (9+3+2 occurrences), "input-indistinguishability certificate", "input-indistinguishability row", "input-indistinguishability port", and, where it is not an attributive modifier, "certifying input indistinguishability" (pgg_tableau.v:161-162, pgg_tableau_syntax.v:259, pgl27_rows.v:578-579). No "the second arm", no bare "indistinguishability arm", no "decay" anywhere. No drift.
- **Header tables.** Every changed entry in pgg_tableau.v and pgg_tableau_syntax.v names an identifier that exists at that exact spelling, checked by extracting declaration names from the whole tree; likewise the six changed entries in five_card_rows.v, the one in pgl27_rows.v and the one in psl211_spectral_constancy.v.
- **The nine "spectral number" sites.** All trace to `Num.sqrt 5%:R * (kim_lambda2 eps) ^+ L`, where `kim_lambda2` is defined as the second-largest eigenvalue modulus (five_card_kim.v:372-373) and the bound comes from `kim_spectral_convergence`. Every one is a genuine eigenvalue-derived quantity. The two file-name occurrences (psl211_spectral_constancy.v:4, psl211_rows.v:43) are the kept path; see F5. The s5_rows.v:64 occurrence is about that instance's own spectral theorem on endpoint marginals, unrelated to the arm, and is correct.
